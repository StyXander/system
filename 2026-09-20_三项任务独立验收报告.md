# 三项任务独立验收报告

验收日期：2026-09-20。对象：本轮方案书修订、知识底座白灰插画、三状态卡中文说明。

## 一、结论

| 任务 | 结论 | 主要依据 |
| --- | --- | --- |
| 1 方案书同步当前系统 | 暂不通过，已有更新可保留 | 十三章与新增功能已同步，PDF 15 页；字体字号未满足用户要求，图1/图7仍为旧英文状态卡 |
| 2 知识底座白灰插画 | 本地验收通过 | 新素材真实加载，四视口完整显示，白灰底色协调，无横向溢出或浏览器错误 |
| 3 P2/E2 等中文说明 | 部分完成，暂不通过 | P2与金额说明可读；真实E2缺具体理由，缺失值反例产生不实肯定说明，现有前端验收脚本仍有失败项 |

本轮仅生成验收证据与报告，未修改业务代码、方案书或原始运行记录；未重启服务、未新增付费模型调用、未部署。结论限本地交付，不等于线上完成或正式竞赛发布通过。

## 二、任务一的检查与返工项

检查文件：02_最终确定方案/审迹智链_AuditTrace_初赛方案书_系统同步修订版_2026-09-20.docx 及同名 PDF。

已确认：PDF 15 页；13 个一级标题；39 个二级标题；补充 R2、知识来源计数、三个独立状态、本地与线上版本差异及正式评估边界。DOCX 未发现批注部件、插入删除修订及隐藏文字标记。交付 PDF 已重新渲染并逐页查看，未发现大面积文字遮挡或空白页。未重导出 DOCX；本次视觉结论针对交付 PDF，字体结论来自 DOCX 样式与直接格式。

### A1 字体字号不符合明确要求

用户要求正文宋体小四（12磅）、标题黑体三号（16磅）。正文 Body Text 为宋体12磅，一级标题为黑体16磅，但39个二级标题使用黑体15磅。表格大量文字为 Noto Serif CJK SC 8磅，部分身份表为宋体8.5/10磅；封面也保留 Noto 系列字族及多种字号。题注为宋体10.5磅。

因此不能把“正文段落符合”写成“整个文档格式符合”。把教师母版的15磅标题照旧保留，不能覆盖本次用户的16磅明确要求。恢复字号后可能超过15页，应通过内容压缩和版面调整解决，不能继续缩小正文来凑页数。

证据：artifacts/three-task-acceptance-20260920/font-audit.json、document-summary.json，以及 page-01.png、page-05.png、page-14.png。

### A2 截图仍是中文改造前的界面

PDF 第3页图1、第11页图7中仍直接显示 screening_status、numeric_gate、coverage_matrix 等英文技术字段。图注如实注明了2026-09-19历史运行，但这两图承担的是当前系统展示任务，尚未满足“页面修改完成后再更新最终截图”的交付要求。

修复顺序：先完成任务三的真实数据兼容与验收，再用同一历史运行在新界面中的真实显示重新截图。图注明确原始运行日期与本次截图日期，不能将历史结果回显写成新模型运行。替换截图后重排目录、导出PDF并重新逐页检查。

图6目前为复核判断、资料缺口和程序截图；仍需补充一张能说明当前页面整体结构或证据回查的真实画面，具体可在原有图位中替换整合，不必盲目增加页数。

### A3 当前测试数字需要收口

方案书及修改清单写“前端合同75项通过”。本轮重跑 verify_demo_outcome_judge.mjs 返回1项失败、1组真实产物向量跳过，不能将此前的75项通过继续作为当前工作树全绿证明。可以保留为带时间与版本的历史结果，最终定稿应更新为修复后的实测结果。

### A4 人员信息为提交前待补项

PDF仍有28处“待填写”。保留未知信息是正确做法，不把它列为AI应自行补造的缺陷；但该文件仍不能当作人员信息齐全的实名提交件。真人补齐后再次导出、检查页数与签名显示。

## 三、任务二的检查

新素材：assets/official-v4/illustrations/knowledge-evidence-neutral-v3.webp，1448×1086，73918字节。页面真实引用此文件，旧素材仍保留。左列使用#F5F6F7背景，已取消整图投影，边缘柔化未裁掉主体。

四视口：1440×1000、1024×768、768×1024、390×844。均通过真实FastAPI页面检查并逐张查看截图；桌面左右排布、小屏上下排布正常。统计仍显示活跃12/登记13、类别9、校验12/12。没有页面异常、失败请求或横向溢出。

本次截图：artifacts/three-task-acceptance-20260920/*-knowledge.png。此前折叠按钮鼠标、键盘及减少动态效果检查结果保存于 artifacts/knowledge-neutral-20260920/acceptance-final.json；本轮再核对页面和当前素材，没有发现新增问题。

通过范围为本地图片与局部样式，不代表已发布到Render。

## 四、任务三的检查与返工项

已完成部分：P2默认说明已改成通顺中文；金额重要性单独说明；三个“查看判定依据”可使用Enter/空格展开收起；打印前自动展开。四视口均无错误或溢出。原始技术字段保留在依据详情中，默认卡片不再铺满字段名。

### C1 真实E2未交付具体中文理由

使用真实任务 DEMO-RUN-05D8EC8137A8 恢复既有完成结果，运行编号 RUN-V7-A923212497D0。真实页面E2显示：

“后端给出证据闭合状态 E2，但本次运行未随状态记录控制项读数值，页面不补造具体计数。”

这句没有编造事实，但没有回答用户希望理解的“为什么部分闭合、还缺什么”。展开后仍保留旧技术说明。

两项独立原因已确认：

1. 当前8000服务的 /openapi.json 中 EvidenceState 仍只有 state、label、reasons、boundary，没有 factors；磁盘 schemas.py 已新增该字段，说明当前服务未加载该修改。
2. 该历史运行原件自身也没有 evidence_state.factors。即使重启服务，Pydantic也不能凭空补回旧记录从未保存的控制项。

返工要求：确认服务加载当前版本；对旧结果从已有结构化原始证据中构建只读展示依据，或生成可追溯的派生展示结果。保留原始记录及哈希，不解析技术说明字符串猜数量，不篡改旧运行或谎称新模型执行。然后用该真实任务验证E2中的直接证据记录数、待验证解释数、缺口数和资料请求数。

证据：live-api-run.json、live-api-task.json、running-evidence-schema.json、1440-badges.png、390-badges.png。

### C2 缺失值会被解释为已通过或不存在缺口

在真实浏览器调用页面实际公开的纯函数，传入隔离反例（不写入任何业务记录）：

- explainEvidence('E2', [{factor:'data_gaps', value:['缺客户回款资料']}]) 在没有numeric_gate和coverage_matrix读数时仍输出“数字校验已通过”及“0项记录包含当前企业直接证据”。
- evidenceFactorRows([{factor:'data_gaps',value:null}]) 将null显示成“无／本次未登记资料缺口”。
- priorityFactorRows([{factor:'blocked_row_proxy',value:null}]) 将null显示成“无受阻规则”。

这些是缺失数据反例，不能冒充真实运行失败；但原计划明确要求覆盖空值、旧版及缺字段，因此属于验收范围内的缺陷。hasFactors只检查是否存在任意因子，不能证明所有必要控制项齐全。E1/E2说明不应只凭等级代号断言数字通过；未知数量必须显示未提供，不能默认为0。

修复后应补有意义的缺失值/未知值测试，并验证后端原始等级、处置及导出数据不受影响。证据：browser.json 中 vectors。

### C3 前端验收脚本仍失败

check_frontend_contract.mjs 通过：187个唯一ID、190个引用、2个脚本。

verify_demo_outcome_judge.mjs 退出码1：命名红线“无风险”一项失败，并跳过1组本机真实产物向量。源码中的相关用法是“不构成无风险认定”等否定边界，当前脚本使用简单字符串扫描造成误报；不能据此认定页面已经宣称企业无风险，也不能忽略失败后仍报告全绿。

可通过调整边界文案或改进语义断言消除误报，但不能直接删除保护测试。重跑后才更新方案书和交付清单中的当前测试结论。原始日志见 verify_demo_outcome_judge.log。

## 五、本轮实测与边界

- 后端专项：test_evidence_closure.py、test_planning_priority.py、test_knowledge_sources.py，共68项通过。
- 前端基础契约通过；业务呈现验收脚本1项失败、1组跳过。
- 真实浏览器：同一历史完成任务在四视口恢复显示；3个依据区键盘开合与打印展开通过，控制台/HTTP/请求错误及横向溢出均为0。
- 数字缺失反例来自页面现有纯函数，明确与真实运行观察分开。
- DOCX结构、字号、批注等检查及交付PDF15页视觉检查已完成；未代填人员信息、未代签。
- 本轮未重跑全量后端、未执行新模型链、未验证线上站点、未重新导出Word报告；不将这些项目记为通过。
- document-summary.json记录关键交付物SHA-256，用于限定本次结论对应的文件版本。

建议返工顺序：先修任务三的服务版本与历史记录兼容、缺失值保护和测试；然后替换方案书截图、统一字体字号并重新导出。任务二保留即可。
