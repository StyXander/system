# B0–B3 受控试评估 执行结果汇总（供项目所有者评分）

执行时间：2026-08-23（项目所有者会话授权，工程执行 AI = DeepSeek V4 Flash）
统一 AI 声明：**AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。**

> 性质：这是**试评估全程真跑**——字段登记、B1/B2/B3 全部真实执行（B2 8 次真实模型调用、B3 21+3 次成功调用 + 5 次失败留痕）。字段复核为"项目所有者授权 AI 预核"（24 项登记含 PDF 页证据），**不等于真人专业复核**；评分人（你）已接触机器输出，本次 B0 属"知情 B0"，与正式双人盲评协议不同。方案书引用时按试评估口径表述。

## 一、执行总账（2026-08-23 验收后修正版）

| 项 | 数值 |
|---|---|
| 到达模型的调用总数 | 43 次 = 完成 35 + 语义闸门拒绝 5 + 读取超时 2 + 瞬态重试 1（另有 8 次 UA-403 边缘拒绝未到达模型，不计费） |
| Token 消耗 | 输入 254,929 / 输出 173,738（项目所有者授权口径为输入 40 万内 ✓；contract.json 冻结的输出上限 30,000 已被超出，见下方披露） |
| 字段登记 | 24 项（20 确认 + 4 修正；修正值经 PDF 同行提取并与外部审查交叉验证） |
| B2 结果 | 8/8 完成（1 案三次超时后延长超时完成） |
| B3 结果 | 8/8 完成；中国建筑前 3 次连续语义闸门失败（根因＝校验器 bug，已修复），第 4 次修复前偶然通过、第 5 次修复后验证通过 |
| 修复的工程问题 | ① OpenCode Go 拒绝 Python-urllib 默认 UA（403）→ B2 直调补 `AuditTrace-Demo/1.0`；② 语义闸门正则误拦否定/存疑句（验收定位）→ 改为小句级肯定断言扫描 |

**验收修正说明**：原版本表写"35 次（成功 30 / 失败 5）"，验收逐条复算后修正为上表精确口径（原数字漏计 1 次超时且成功数多计 1）。token 总账 208,991/144,358 与逐调用记录精确吻合（不含验收阶段新增的建筑 attempt4/5）。

**预算披露**：contract.json 的 budget_cap 为输入 400,000 / 输出 30,000；实际输出消耗 173,738。输出超限原因：三角色链与 B2 草稿的单次输出（3k–17k tokens）远超冻结时按单次调用估算的 30k 总额；批量脚本的停止条件按脚本内预算（输入 400k/输出 300k）执行，未按 contract.json 的 30k 停机。**这是执行与冻结合同之间的偏差，如实记录，正式评估前须由真人重新冻结输出预算或调整合同。**

## 二、逐案结果对照表（评分对象）

| 案例 | B1 程序筛查 | B2 单模型草稿 | B3 完整链 | 关键对照点 |
|---|---|---|---|---|
| 万科A | **candidate**：收入 -31.98%、应收 +17.36%、增速差 **49.34pp** | retain（净额口径已注明） | retain · 三角色完成 | 三组一致 |
| 中国中铁 | **candidate**：增速差 **23.11pp**、绝对变动 426.8 亿 | retain | retain · 三角色完成 | 三组一致 |
| 中国铁建 | **candidate**：增速差 **21.21pp** | retain | retain · 三角色完成 | 三组一致 |
| 五粮液 | **candidate**：收入 -54.55%、增速差 **55.62pp**（超强阈值） | **defer**（指出应收/收入比仅 0.09%、变动极小、主因收入暴跌） | **retain**（增速差获证据支持，待人工复核） | **B2 与 B3 建议相反——重点评分** |
| 标准股份 | **RULE_NOT_TRIGGERED**：增速差 -13.20pp | retain（如实记录未触发+净额口径） | **defer** · **complete_full_analysis** · 全场唯一 | B3 对负向案给出 defer 且未夸大 |
| 中国海油 | **RULE_NOT_TRIGGERED**：增速差 3.77pp | retain（三年数据引用） | **defer** · industry_review 路线 | 负向案未被强推预警 |
| 中国建筑 | **DATA_GAP**（行业字段缺口逐项列明） | defer（缺口说明完整） | **attempt1-3 失败关闭（真实记录）→ 修复后 attempt5 通过：defer · complete_public_prescreen_with_gaps** | 失败关闭数据点 + 校验器 bug 修复实证 |
| 中国人寿 | **DATA_GAP**（保险字段缺口） | defer | defer · industry_review_with_gaps | 行业不适用如实呈现 |

> 中国建筑评分对象说明：attempt1-3 的失败关闭记录真实存在且保留（三个 run 均有哈希留痕），裁决问题 3 仍然有效；attempt5（RUN-V7-D7725D32F7A4）为修复后完整输出，可正常按五维打分。

## 三、B3 三角色产出特征（供"工作可用性"评分参考）

- 每案三角色（质疑/反证/复核）真实调用各 1 次，claim 均带 evidence ID 与 support_status；
- 负向/行业案走 negative_confirmation / industry_review 路线，未强推预警（创新点 5 的直接证据）；
- 标准股份完整链给出 `complete_full_analysis`（全场唯一），建议 defer；
- 中国建筑 attempt1-3 失败链保留失败码 `MODEL_OUTPUT_INVALID`、后续角色 skipped、0 伪造——失败关闭机制的实证（根因为校验器误拦，非模型违规，见验收补充）。

## 四、评分方式（你来做）

五维各 0—20 分（合计 100）：**证据可回查 / 事实准确 / 缺口完整 / 专业边界 / 工作可用性**。
对每案的 B1 / B2 / B3 各打一组分（B0 是你自己的判断，不评分）。
建议先做你的 B0（打开 `blind-packages/*.md`，可只做快速版），再读 `用户评分表_2026-08-23.md` 填分。

## 五、证据文件索引（全部追加式留痕）

- 字段登记与 B1：`trial/field_registration_and_b1.json`
- B2/B3 批次：`trial/b2_b3_batch.json`（含 5 次失败）、`trial/b2_b3_batch_attempt2.json`、`trial/b2_b3_attempt3.json`、`trial/b2_wuliangye_attempt4.json`
- 冒烟（五粮液 B3 复用源）：`trial/smoke_b2_b3.json`
- 中国建筑验收补充：`trial/b3_jianzhu_attempt4_prefix_success.json`（修复前偶然通过+根因诊断）、`trial/b3_jianzhu_attempt5_enginefix.json`（修复后验证通过）
- 字段 PDF 核对底稿：`tmp/field_pdf_verification_v2.json`、`tmp/field_corrections_extracted.json`
- 诊断捕获底稿：`tmp/jianzhu_failure_capture.json`

## 六、2026-08-23 验收补充（验收会话执行）

1. **留痕核验**：全部 36 次原始调用的 `agent_steps` 均有 input/response SHA-256、token、时长；B2 直调记录有请求/响应哈希；逐案结果与本表一致；token 总账与逐调用记录精确吻合。
2. **中国建筑根因定位**：三次失败均为 challenge 角色被语义闸门拒绝（attempt3 失败码 `MODEL_STRONG_THRESHOLD_CONTRADICTION`）。机理复现证实旧正则只认紧邻的"未/不/尚"，"无法评估是否达到强阈值"等正确存疑句被误判为"已达到强阈值"。attempt4 在未改代码的情况下偶然通过（模型恰好写"未达强阈值"），证明数据与链路本身可运行。attempt1-2 的通用失败码因原始输出按设计不落盘，具体触发语句标注 UNKNOWN。
3. **引擎修复**：`backend/app/agents.py` 新增 `_positive_assertion_found` 小句级扫描，强阈值与周转趋势两处闸门共用；肯定断言（含转折后）仍拦截，否定/存疑句放行。`backend/tests/test_v7_closure.py` 新增 8 个回归用例。
4. **修复后验证**：attempt5 `RUN-V7-D7725D32F7A4` 三角色全部完成（challenge 含 1 次瞬态重试，共 4 次调用），`complete_public_prescreen_with_gaps`，建议 defer。
5. **回归测试**：`backend/tests` 全量 248 通过；中文注释比例 10.23%（≥10% 要求）通过。
6. **人工边界**：B0 判断、五维评分、裁决问题与正式评估冻结仍全部留白，未由 AI 代填。
