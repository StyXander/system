# 本评估目录已作废（SUPERSEDED）

作废日期：2026-08-22（外部独立审查后整改）

`EVAL-20260822-COMPETITION-8CASE-V2` 的合同、仪表盘、盲包与评分账本存在以下确认缺陷，**不得用于发放 B0 或执行 B2/B3**：

1. **B0 盲性泄露**：`blind-packages/packages.manifest.json` 含各案预期分层（strong_direct_candidate / rule_not_triggered / industry_not_applicable）。
2. **哈希缺陷**：盲包 manifest 的 8 个 SHA-256 按 LF 字符串计算，实际文件为 CRLF 字节，逐字节复算 8/8 不一致。
3. **字段冻结失真**：64 个候选字段全部 pending 却被当作冻结事实写入合同与盲包正文；其中万科A（应收 4.00 元）、中国中铁（-5,000 元）、中国铁建（12,000 元）已被外部审查对照官方年报确认错误。
4. **RAG 冻结错误**：把 `question_set()` 顶层键当作问题编号，未冻结真实问题文本与问题集哈希。
5. **账本缺陷**：跨表公式未对中文工作表名加引号（渲染 #NAME?）；表头样式加在空白行。
6. **配套问题**：旧 `freeze_evaluation_v3.py` 可污染新评估目录（已退役）；AI 参考评分未封存（已移至 sealed-ai-reference 并勘误）。

## 仍然有效的部分（保留原因）

- `trial/b1_trial.json`：8 个真实仅计算运行（0 次模型调用），run 编号与结果真实，可作为工程证据引用；但其输入基于本目录的未复核候选字段，**不是正式 B1 记录**。
- 本目录整体作为"冻结失败与整改"的历史证据保留，遵循追加式留痕原则不做删除。

## 后续

继任评估编号：`EVAL-20260822-COMPETITION-8CASE-V3`。生成前置条件：真人完成《字段回页复核工作底稿》（`outputs/evaluation_v4/field-review/`）的全部字段复核。冻结、盲包与账本由整改后的脚本重新生成。

统一 AI 声明：AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。
