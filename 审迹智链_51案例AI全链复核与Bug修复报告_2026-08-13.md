# 审迹智链 51 案例 AI 全链复核与 Bug 修复报告

> [!IMPORTANT]
> **技术验收限定说明**：本报告为传输、Schema 与留痕的技术验收记录（51/51 HTTP 200、153 次 provider call），仅作为传输通道与工程契约的留痕技术证据，不构成字段正确性、风险识别能力或模型有效性的评估证据，也不等于当前事实闸门代码下的正式 B3 成绩。

审查日期：2026-08-13  
任务类型：工程实现、专业边界与竞赛演示的全链复核  
审查范围：根目录正式网页及其真实后端接口、`backend/app/`、测试、50 家公开公司物化种子、标准样例、历史 51 案外部模型验收记录和运行说明。未改写正式网页内容，未部署，未再次调用付费外部模型。

AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。

## 一、结论

51 个案例已经具备进入完整 AI 路线的工程能力，历史批次也证明 51/51 均真实完成 Challenge、Counter、Review 三角色调用。但历史结果使用的是新字段质量闸门上线前的候选，只能证明接口、Schema 和留痕链可运行，不能证明 51 家字段正确、风险识别准确率或有效 B3。

本轮从输入、字段提取、RAG、模型调用、缓存、隐私、公开 Demo、反向代理、报告和浏览器交互重新审查并修复。最终源码回归为 **201 passed、1 warning**。当前最大未完成事项不再是“能不能跑”，而是 **128 条自动字段候选的真人回页确认、专业规则签字、正式评估冻结和当前代码下的受控外部模型复验**。

## 二、已修复问题

### P0：自动字段误提取会污染 51 案分析输入

问题：启发式 PDF 提取可能把附注序号、页码、期间、阈值等数字误当成金额或比例；部分跨年值相差一个数量级以上仍会进入规则和模型证据。

修复：新增语义和数值质量闸门；可疑候选保留给人查看，但从确定性规则和模型证据中失败关闭。跨年数量级闸门只作用于启发式 PDF 行，不错误拦截人工/内置可信字段。全量回归曾捕获标准样例经营现金流合法跨年变化被误拦截，随后补上该限定和回归测试。

结果：50 家物化公司共 394 条候选，当前拦截 **128 条、涉及 30 家**，其中 **72 条**为启发式跨年数量级异常。确定性分布为 `DATA_GAP=30`、`RULE_NOT_TRIGGERED=17`、`candidate=3`。

### P0：模型输出缺少持久化前隐私闸门

问题：输入侧已有扫描，但模型返回内容若包含高风险号码，可能在 Schema 通过后进入运行记录或 API。

修复：模型输出在 Schema/语义校验后、保存前再次执行隐私扫描。命中时返回 `MODEL_OUTPUT_INVALID / MODEL_OUTPUT_SENSITIVE_DATA`，不保存原文，只记录类别和字段路径，并停止后续角色。

### P0：完整分析存在失败后继续或演示绕过边界

问题：全案例模式中的 RAG、隐私或许可失败曾可能被 Demo 路径放宽；演示路径还可能自动替案例打开模型许可。

修复：RAG、隐私和模型许可在所有完整分析路线统一失败关闭；Demo 不再代开许可。匿名公开 Demo 仅允许内置样例，拒绝任意 ZIP、补充资料、字段确认、人工审批/导出、缓存写操作及非种子流水线创建。

### P1：模型缓存可能复用过期语义并虚报本次成本

问题：旧缓存指纹没有完整覆盖规则、阈值、路线、确定性结果和证据；回放还可能沿用来源运行的 token、耗时、调用次数或嵌套运行 ID。

修复：缓存键覆盖引擎/规则版本、阈值、路线、确定性结果和实际证据。命中时重绑当前运行 ID，记录来源运行和来源用量，但本次 token、耗时和 provider call 归零，`external_model_call_performed=false`。

### P1：失败调用与重试的审计计数不真实

问题：Schema/策略校验失败前实际已调用 provider，却可能记为 0；瞬时错误重试也只记一次。

修复：`AgentStep` 新增实际调用标志和尝试次数。成功、瞬时重试、Schema 失败、输出策略失败均按真实 HTTP 尝试留痕；调用前拒绝和未执行角色保持 0。失败响应仍保留哈希、token 和耗时，不保存被隐私闸门拒绝的正文。

### P1：公开部署身份、跨域和配额配置可被误配

问题：无条件信任转发头会允许伪造客户端身份；宽松 CORS 或弱配额密钥会削弱公开模型限额。

修复：默认同源，CORS 仅接受无路径、查询、用户信息或通配符的精确 HTTP(S) origin；只有直连节点位于明确可信 CIDR 且跳数匹配时才采信代理链，拒绝 `/0`；公开 Demo 使用真实模型时强制至少 32 字符配额密钥。

### P1：并发缓存占位和配额预约可能形成陈旧状态

问题：超时请求可能删除后来所有者的缓存占位，或在配额拒绝/异常后留下等待点；过期账本预约也会长期占用额度。

修复：缓存占位加入 TTL 和所有者事件身份，超时后原子重获，旧所有者不能清除新占位；配额异常释放占位；账本采用公开命名空间并清理过期预约。每 IP 限流键会整体裁剪过期项。

### P1：其他一致性问题

- 补充资料重跑不再重复登记同一证据。
- 行业字段确认允许专业化字段 ID，不再被基础财务字段枚举误拒绝。
- 最终草稿继续保留待核证据边界，不把待核内容写成已证事实。
- 确定性验收脚本强制关闭外部模型和密钥，避免本地测试意外产生费用。
- `.env.example` 改为安全默认，并补充 CORS、可信代理和公开配额配置。

## 三、51 案外部模型证据边界

严格清单：`outputs/external_model_acceptance/current.json`。

| 项目 | 核验结果 |
|---|---:|
| 唯一案例 | 51 |
| HTTP 200 / `external_live` / `model_success` | 51 / 51 |
| 完成三角色的最终案例 | 51 |
| 最终 provider 调用 | 153 |
| 首次成功 / 批内重试成功 / 后续定向补跑 | 45 / 3 / 3 |
| 风险候选 / 行业边界 / 数据缺口路线 | 15 / 19 / 17 |
| 输入 / 输出 token | 1,524,131 / 90,867 |
| 汇总调用耗时 | 887,919 ms |

这些数字只适用于历史 `agent_prompt_v3` 技术批次。它不是当前字段闸门代码的外部模型验收，不是 51 家专业结论，也不能填入 B3 人工评分。

## 四、最终验收

| 验收项 | 结果 |
|---|---|
| 后端全量测试 | `201 passed, 1 warning, 146.22s` |
| 本轮专项测试 | `21 passed` |
| Python 编译 | 通过 |
| 中文注释/docstring | `1737/17325 = 10.03%`，通过 |
| 前端契约 | `210 unique ids, 369 refs, 9 views` |
| JavaScript 语法、CSS 对比度、差异卫生 | 通过 |
| 50 家确定性演示检查 | 50 家、RAG 50/50、来源缺失 0；30/17/3 分布 |
| 历史外部模型严格清单校验 | 51/51 通过；本轮未发起新调用 |
| 真实浏览器 | 1440×1000、1024×768、768×1024、390×844 全部 HTTP 200；console、失败请求、横向溢出、axe 均为 0 |
| 桌面/手机交互 | 字段 12 行、RAG 1167 块、检索 5 条、仅计算运行成功、provider 调用 0 |

浏览器证据位于：

- `artifacts/review-2026-08-13-browser/current-audit/audit-summary.json`
- `artifacts/review-2026-08-13-browser/current-interactions/interaction-summary.json`

## 五、仍需完成

### P0：必须由真人完成

1. 回原年报页面确认、修正或拒绝 128 条被拦截候选，至少双检高影响公司和所有跨年数量级异常。
2. 完成 R1 专业签字、案例冻结、T0/baseline/模型/提示词/指标/预算冻结及 B0—B3 人工评分；当前评分必须保持空白。
3. 补齐主办方最新正式竞赛手册原件并逐条核对。本工作区目前没有找到可作为最高优先级依据的正式原件。

### P1：人工字段复核后再做

1. 经预算和数据传输授权，使用当前代码对冻结案例做一次受控外部模型复验；不得沿用历史 51 案作为专业验收。
2. 重新验收 Render、Supabase/RLS、真实登录 consent、反向代理来源 IP 和线上配额；本轮只验收了本地 FastAPI。
3. 正式发布前制定模型运行记录和公开年报全文的保留、删除与再分发规则，并留存人工批准。

### P2：工程改进

1. 升级 Starlette TestClient/httpx 兼容路径，消除当前唯一弃用 warning。
2. 若未来扩为多实例部署，把当前进程内 IP 限流迁移到共享存储；单实例部署暂不受影响。
3. 把当前浏览器验收脚本纳入稳定 CI，并为公开代理配置增加真实部署环境的冒烟测试。

## 六、交付边界

本轮没有改写根目录正式网页内容，没有调用新的付费模型，没有提交、推送或部署，也没有修改历史封存原始 JSON。源码、测试、物化种子、机器事实源、运行说明和本报告已同步；正式专业判断仍由真人完成。
