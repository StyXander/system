# 三份 AI 建议评审 与 24 小时改进计划

> ## ⚠️ 本文件的实施方案已被取代，且含一处已撤回的错误结论
>
> **请改读** `2026-09-19_综合执行方案_多AI并行.md`（W00-W25，多 AI 并行版）。
> 本文件的**评审部分（§一、§二）与证据链仍然有效**，但以下两处**作废**：
>
> 1. **§三 缺陷三「blocked 字段仍被 R1 采用」——错误，已撤回。**
>    实测：`cases.py:1934-1944` 的 `calculation_ready_financial_rows()` 已按 `candidate_quality_issues` 过滤，
>    并由 `main.py:1559,2524`、`cases.py:1973,2064` 调用。全库 53 次天文 `growth_gap` 全部落在
>    2026-08-10~08-13，09 月 0 次；6 个受影响案例 09-08 运行实测均为 `DATA_GAP` / `gap=None`。
>    我方错误源于 grep `candidate_quality_status`（实际过滤走 `candidate_quality_issues`）后，
>    据"搜不到"推断"未执行"——即「grep 无命中 ≠ 功能缺失」。
>    因此 **§四 P3 与 §五阶段一 F3 不得实施**，改为只读复现验证（新方案 W17）。
>    残余问题降级为文案透传（新方案 W16）：6 个案例落到 `DATA_GAP` 后页面只说泛化"资料不足"，
>    未透出系统内部已有的精确诊断原文。
>
> 2. **§五 阶段三的分级编号 F1/F2/F3/F4 与缺陷编号 F1-F4 冲突**，已改为 **P1/P2/P3/P4/S/G**（采纳 GPT 意见）。
>
> 另：本文件 §四 中「15 → 9 可用演示面」的**结论方向仍成立**（6 个案例确实不可用作主风险案例），
> 但**机制描述错误**——不是因为它们会输出假 candidate，而是因为它们正确落到 `DATA_GAP`。

日期：2026-09-19。状态：**评审有效，实施方案已作废**。
评审对象：GPT 建议、Gemini 建议、`opus修改建议 .md`。
时间前提已变更：**不再是"下午录视频"，最少有 24 小时**。因此本文件按"先修真伪、再显层级、最后才考虑分级"重排优先级。
姊妹文件：`2026-09-19_录视频前突击修复与演示方案.md`（缺陷一/二的完整证据链）。

AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。

---

## 零、总评

三份建议**都读到了源码，都给出了正确的案例选择（五粮液），都在"分级不要叫风险评级"这一点上与我一致**。
但**三份都漏掉了系统里真实存在的四个缺陷**，其中两个会让你在镜头前说假话、一个会让答辩翻车、一个正是你最烦的"总是资料不足"的直接成因。

而且三份里有**两处会直接导致演示自相矛盾的事实错误**，必须先纠正再用。

| | GPT | Gemini | Opus |
| --- | --- | --- | --- |
| 读源码深度 | 深（主链清楚） | 中（读了 run 文件） | 中（读了 schema 层） |
| 案例选择 | ✅ 五粮液 | ✅ 五粮液 | ❌ 标准股份（理由部分错误） |
| 财务数字准确性 | ✅ 全部对得上 | ✅ 对得上 | ⚠️ 未给数字 |
| Agent 结论准确性 | ❌ **重大错误** | ❌ **虚构对话** | ✅ 标准股份 `defer` 说对了 |
| 分级方案设计 | ✅ **三份里最好** | ⚠️ 专业词汇好，无实现 | ❌ 有专业硬伤 |
| 发现真实缺陷 | 0/4 | 0/4 | 0/4 |
| 危险建议 | 无 | ❌ **建议用历史结果录屏** | ⚠️ 建议前端 JS 算评级 |

---

## 一、必须纠正的事实错误（照抄会当场翻车）

### 1.1 GPT：把标准股份的 `defer` 接到了五粮液头上 —— 严重

GPT 原文：

> 五粮液这个案例其实已经证明你有"AI 灵魂"……所以 Reviewer 最终给的是 **`defer` 暂缓判断**，补资料后复核。

GPT 整条演示叙事（"AI 没有照单全收 → Reviewer 不贸然认定风险 → defer"）建立在这句上。**实测为假。**

五粮液 35 次真实 live 运行的角色结论分布（`backend/runtime/runs/`，过滤 `execution_mode=external_live` ∧ `cache_hit=false` ∧ `model_check.status=model_success`）：

```
review.status             : retain × 35          ← defer 出现 0 次
review.ai_recommendation  : retain × 35
顶层 ai_recommendation     : retain × 35
review.analysis_conclusion: risk_candidate × 34, additional_procedure_required × 1
challenge.status          : candidate × 35
counter.status            : candidate × 34, defer × 1     ← 全库唯一一个 defer 在 counter，35 次里 1 次
```

`defer` 真实存在，但属于**标准股份**（见 §1.3）。

**后果**：你若照 GPT 稿子念"Reviewer 没有贸然下结论，给了暂缓判断"，屏幕显示 `retain`（前端文案 `RECOMMENDATION_LABELS.retain = "建议保留"`，`demo-app.js:141`），当场自相矛盾。

**正确叙事**（同样有力，且是真的）：
> Counter 角色提出了两条实质抗辩——白酒经销"先款后货"结算方式、应收绝对变动仅 39.89 万元且占收入 0.09%。
> Reviewer 认为**这两条抗辩本身都还是"待验证假设"**（`support_status: unverified_hypothesis`），
> 因此既不采纳抗辩撤销候选、也不升级为风险认定，而是 `retain`（保留候选待人工复核），
> 并列出必须取得的四项资料。

`retain` 不是"没态度"，它是**"证据不足以改变判断，所以维持候选并指明补什么"**——这恰恰是审计计划阶段唯一正确的处置。

### 1.2 Gemini：虚构了 Agent 的具体台词 —— 严重

Gemini 原文：

> **立论 Agent（Challenge）**：敏锐指出"营收腰斩但应收账款反而上升，严重违背常理，存在跨期确认收入或延期回款风险"

这不是系统输出，是 Gemini 写的剧本。真实 `review.output.claims[0].text` 是：

> 程序筛查事实：应收增速1.07%、收入增速-54.55%、增速差55.62%，应收绝对变动398,857.56元，未评价金额重要性。

语气、措辞、强度全不一样。**而且系统有硬约束禁止这种表述**：`agents.py:364,546` 明令 Agent 不得改写确定性数字，`_validate_deterministic_fact_language` 会复核草稿。"严重违背常理""存在……风险"这类断言很可能过不了闸门。

另外 Gemini 把三个角色命名为 **"Challenge / Counter / Arbiter（质检仲裁）"**。真实角色是 `challenge / counter / review`，前端显示为 **"质疑 Agent / 反证 Agent / 复核 Agent"**（`demo-app.js:135-139`）。**系统里没有 Arbiter。** 念错角色名，评委一对界面就露馅。

### 1.3 Opus：标准股份说对了一半，但两个硬错误

**说对的**（我实测确认，且要更正我自己先前的统计口径错误）：

```
RUN-V7-F7BEF8DA5846 / 3FC0FACC00A5 / B655B4D7EAEE   三次均为真实 live
  model_check.status = model_success, execution_mode = external_live, cache_hit = False
  R1: RULE_NOT_TRIGGERED   growth_gap = -0.13197 (-13.20pp)
      revenue_growth = -24.45%   ar_growth = -37.64%
  R2: DATA_NOT_COMPARABLE
  route = evidence_gap_review
  ai_recommendation = defer        ← Opus 与 GPT 的 -13.20pp、defer、DATA_NOT_COMPARABLE 全部准确
  run_completeness = complete_full_analysis
```

> **更正我自己**：我在上一轮说"标准股份 live = 0"，那是我的过滤口径错误——我只统计了命中 `candidate` 的运行，而标准股份三次 live 的 R1 是 `RULE_NOT_TRIGGERED`，被排除了。标准股份**有** 3 次成功的真实 live 运行。

**Opus 硬错误一**：

> 推荐演示标准股份：**这是唯一通过全部 10 项门槛的 Level A 案例**

实测 `backend/competition_demo_cases.json`：

```
Level A: 3 个 → 标准股份、五粮液、中国海油
Level B: 12 个
```

**Level A 有 3 个，不是 1 个。** Opus 的"唯一"不成立。

**Opus 硬错误二**：

> 当前输出只有四类定性标签（`risk_candidate` / `no_trigger_confirmed` / `data_gap` / `industry_boundary`）

`schemas.py:173-179` 的枚举是 **5** 个，Opus 漏了 `additional_procedure_required`。
而全库 2342 次运行的实际分布是：

```
data_gap                     439 次
industry_boundary            179 次
additional_procedure_required 107 次   ← Opus 漏掉的这个，出现 107 次
risk_candidate               110 次
no_trigger_confirmed           0 次     ← Opus 列的这个，一次都没出现过
```

Opus 列出了一个从未发生的标签，漏掉了一个发生 107 次的标签。而这个漏掉的标签正是 §三 缺陷四的主角。

### 1.4 Gemini：一条会直接违规的建议 —— 必须拒绝

> **避免现场重跑耗时**：可以在界面中加载已经跑好的历史结果（五粮液 `CNINFO_000858_T0_20260430`），这样录屏丝滑不卡顿。

**不要这么做。** 三重问题：

1. `AGENTS.md §5` 与 09-18 计划都明确：**不能把缓存回放算作实时调用**。
2. 前端 `degradedReason()` 会把 cache_replay 显示成"已复用历史分析结果 · 本次新增 0 次模型调用"（`demo-app.js:1636-1641`）——录进视频就是自曝没真跑。
3. 你**已经授权真实模型调用**，且后端 `full_analysis_ready: true`、五粮液有 35 次成功 live 记录。没有任何理由用回放。

真实跑一次五粮液约 80 秒（`RUN-V7-684E88DF0FBC` 实测 `duration_ms=80777`；今天三次 2~8 秒的是缓存命中后的角色调用）。**这个等待本身就是演示的一部分**：屏幕上跳动的 token 数、`response_sha256`、3 次 provider 调用，是通用聊天机器人给不出的画面。

---

## 二、三份建议里真正值得采纳的部分

### 2.1 GPT 的"综合审计关注卡" —— 三份里最好的设计，采纳

GPT 提出的卡片结构远优于 Opus 的 A1-C2 裸评级，因为它承载**推理过程**而不只是一个字母：

```
触发信号 / AI反证 / 当前未解决矛盾 / 当前判断 / 什么能改变判断 / 下一步程序
```

其中 **"什么能改变判断"** 是全场最有价值的一栏——它把"资料不足"从**系统的无能**翻译成**系统的专业**：不是我不知道，是我明确知道什么证据能改变结论。这一栏直接回答了你最初的崩溃点。

我在此基础上加一栏 GPT 没有的：**"本次处置及其依据"**（`retain` / `downgrade` / `defer` + `reason_for_status` 原文）。因为 §1.1 证明现有数据里处置是有区分度的，只是没被展示。

### 2.2 GPT 对"像 Python 脚本"的源码级解释 —— 准确，采纳

> `schemas.py` 把 `RuleId` 限制成 `Literal["R1","R2"]`；Agent 状态只有 `candidate/retain/downgrade/defer`……当前 AI 在架构中的角色确实更像**证据约束下的复核员**，而不是"综合风险决策层"。

这个判断与我独立读码的结论一致（`schemas.py:15,180,190`；`agents.py:353-391,546,551`）。
GPT 还准确指出前端把结果压成四类（`deriveAuditOverview()`，`demo-app.js:794`；标签在 `108-133`）。

**但 GPT 的结论我要改一个字**：GPT 说这是"AI 没用对"。
不是。**这是刻意的架构选择，而且是正确的选择**——`AGENTS.md §3/§5` 要求确定性事实闸门、禁止模型改数字。
真正的问题不是"AI 权限太小"，是**AI 已经产出的高质量内容被前端压平了**（见 §三 缺陷四）。

### 2.3 GPT 指出的"价值藏在第二三屏" —— 准确，采纳

`coverage_matrix.py`（认定—证据—程序覆盖矩阵）、`evidence_fitness.py`（本企业直接证据/准则依据/类比监管背景/未核验背景四级）、`anti_confirmation.py`（反确认偏差记录）、AP-01~AP-06 程序库——**这些确实是护城河，且确实没被前台讲出来**。

### 2.4 Gemini 的 CAS 1211 专业词汇 —— 采纳其语言，不采纳其实现

Gemini 提醒"千万不要用证券评级代码"是**对的**，与我和 GPT 一致。
它建议改用 **RoMM（重大错报风险）/ CAS 1211 / PBC 清单** 等地道审计语言，这个方向对。

但它的四级矩阵（特别风险/中度关注/低风险/资料受限）里，**"资料受限"不能和前三级并列成一个维度**——见 §2.5。

### 2.5 Opus 的六级评级表 —— 有两处专业硬伤，不采纳原案

**硬伤一：把数据缺口当作严重度升级条件。**

Opus 表格：`C2 = C1 条件 + 有严重数据缺口/重要性超标`；同时 `A2 = R1 未触发但……有少量数据缺口`。

这在专业上站不住：**数据缺口意味着你无法评估严重度，不是严重度更高。**
把"缺资料"往上升一级会得到荒谬结果——一家什么都查不到的公司被评为"特别关注 C2"，而一家查清楚了没问题的公司被评为"A1"。**信息越少，评级越高**，任何审计教授都会当场质疑。

正确做法：**缺口是独立维度，不参与严重度升降**，只决定"是否出级"。缺资料 → 不出级，出资料清单。这也是 Gemini 那套矩阵同样犯的错误。

**硬伤二：发明了一个没有签字的新阈值。**

`A2 = 增速差接近阈值(>0.08)` —— **0.08 是 Opus 现编的**。
现有冻结阈值只有 `r1_gap_threshold=0.15`、`r1_strong_gap_threshold=0.30`、`r1_absolute_threshold=0.0`、`r2_min_gap=0.0`（`schemas.py:73-81`）。
新增 0.08 需要真人冻结与专业依据，`AGENTS.md §5` 明确"正式实验前由真人冻结……阈值"。**今天下午/今天之内都不该引入。**

**硬伤三（次要）：`risk_score` 0-100 分 + 雷达图。**
无验证权重的百分制是**假精确**，在会计评委面前是减分项；雷达图属于视觉重构，`AGENTS.md §7` 要求先有书面设计理由，且禁止"同质化数据卡"作为默认审美答案。

---

## 三、三份建议全部遗漏的四个真实缺陷

> 完整证据链见姊妹文件 §一、§四 P3。此处只列结论与影响。

### 缺陷一：数字闸门误杀，把成功的真实运行判为不完整

`numeric_gate.py:113-131` 的 `_evidence_sources()` 只读结构化 `row["value"]`，而 `rag_evidence` 行的 `value` 恒为 `None`。
模型从年报第 89 页正确引用 `41,813,685.32` 并绑定 `RAG-...-2025-P0089-C00`（我实测确认该数字确实在该片段原文里），闸门仍判"关键财务数字无来源"→ `main.py:4310-4312` → `run_completeness = "incomplete_numeric_claims"`。

**你 09-19 的 13:09 / 13:10 / 13:13 三次失败全部由此产生，且三次卡在同一个数字上。**

### 缺陷二：前端对该状态无分支，显示出关于自己系统的假话

`assets/` 全量搜索 `incomplete_numeric_claims`：**0 命中**。
`outcomeFromRun()`（`demo-app.js:1592-1611`）→ `degraded`；`degradedReason()`（`1617-1632`）→ 落到兜底分支 `evidence_incomplete`；
文案（`1654-1659`）：**"模型状态登记为 model_success，但本次没有可核验的调用留痕"**。

实际有 `provider_call_count=3`、`input_sha256`/`response_sha256` 齐全、3/3 角色 `provider_call_performed=true`。
**这句话是假的。你"觉得系统没用"的直接来源就是这句话。**

### 缺陷三：抽取质量阻断标记无人执行

`candidate_quality_status` 在 `backend/app` 内**写入 2 处（`cases.py:1885-1886`、`1930`），读取 0 处**。
系统自己诊断出"自动提取金额的原始值仅为 1元，疑似附注号、序号或叙述数字"并打了 `blocked_pending_human_confirmation`，
R1 仍拿 `value=1.0` 算出 `growth_gap = 93 亿百分点` 并标为"强候选"。

设计依据是 `field_extraction.py:436` 的注释：「公开预筛允许带着明确缺口继续运行」。
影响面：**50 个目录案例中 30 个含 blocked 字段；15 个演示案例中 6 个中招**（立讯精密、中国石化、万华化学、长江电力、中国石油、紫金矿业）。**五粮液 blocked = 0，是唯一干净的。**

### 缺陷四（本轮新发现）：前端把"需执行额外程序"坍缩成"资料不足"

`demo-app.js:831-833`：

```js
if (conclusion === "additional_procedure_required") {
  conclusion = dataGaps.length || requestedMaterials.length ? "data_gap" : "risk_candidate";
}
```

`additional_procedure_required`（需执行额外程序）是一个**正当、可执行的审计结论**，全库出现 **107 次**。
但只要本次运行有任意 `data_gaps` 或 `requested_materials`，它就被**改写成 `data_gap`**，
前端随即显示 `AUDIT_OVERVIEW_META.data_gap.label = "现有资料不足，补充材料后判断"`（`demo-app.js:121-126`）。

而系统**按设计总会产出待索取资料清单**（那是交付物之一），所以 `dataGaps.length || requestedMaterials.length` **几乎恒为真**。

> **这就是你"有时候是资料不足要补充资料"的直接成因。**
> 不是规则太少，不是模型不行——是前端把一个有内容的结论**重写成了最弱的那句话**。

同时注意 `no_trigger_confirmed`（"暂未发现需提升关注的程序信号，维持常规核查"）在 2342 次运行中出现 **0 次**。
`AUDIT_OVERVIEW_META` 为它留了位置，实际几乎不可达——**你的系统从来没能对任何一家公司说出"这家没问题"**。这才是"层级没有区分"的真正机制。

---

## 四、你要的"层级区分"已经存在，只是被压平了

这是本轮最重要的结论。**不需要发明 A1-C2，先把已有维度还原出来。**

现有数据里已具备区分度的维度（全部已计算、已落盘、零新增数学）：

| 维度 | 来源 | 取值 |
| --- | --- | --- |
| 规则触发强度 | `risk_card.screening_strength`（`main.py:2327,2355`） | strong(≥30pp) / standard(15–30pp) / none |
| **复核处置** | `ai_recommendation`（`schemas.py:190`，前端标签已存在 `demo-app.js:141`） | retain 建议保留 / downgrade 建议降级 / defer 建议暂缓 / not_applicable |
| 反证角色独立意见 | `agent_steps[counter].output.status` | candidate / defer … |
| 结论类别 | `analysis_conclusion`（`schemas.py:173-179`） | 5 档 |
| 持续性 | `metrics.sustained_periods`（`main.py:2297-2299`） | 1 / 2 |
| 重要性 | `metrics.materiality_assessment`（`main.py:2300-2311`） | 达到或超过 / 低于 / 未评价 |
| 证据充分度 | `evidence_fitness.py` 四级 + `coverage_matrix.py` | 已有能力，未聚合展示 |

**实测两个 Level A 案例已经落在完全不同的格子里：**

| | 五粮液 `CNINFO_000858_T0_20260430` | 标准股份 `STD_DEV_T0` |
| --- | --- | --- |
| R1 | `candidate`，gap **+55.62pp**（strong） | `RULE_NOT_TRIGGERED`，gap **-13.20pp** |
| R2 | 未选用（`rule_ids: ["R1"]`） | `DATA_NOT_COMPARABLE` |
| route | `risk_candidate` | `evidence_gap_review` |
| **ai_recommendation** | **`retain` 建议保留** | **`defer` 建议暂缓** |
| completeness | `complete_public_prescreen` ×30 | `complete_full_analysis` ×3 |
| blocked 字段 | **0** | 0 |
| 前端总览应为 | 建议列入重点核查 + AP-01/03/06 | 现有资料不足 + AP-02/04/06 |

**"retain vs defer"、"+55.62pp vs -13.20pp"、"strong vs 未触发"——这就是层级。**
双案例对比演示所能证明的差异化，比任何新发明的六级字母都强，而且**全部是真实跑出来的，不需要写一行新逻辑**。

---

## 五、24 小时改进计划

排序原则：**先让系统不说假话 → 再让已有层级显形 → 最后才考虑新分级 → 全程不引入未签字阈值。**

### 阶段一 · 修真伪（约 3.5 小时，最高优先，必须先做）

| # | 缺陷 | 改点 | 工时 |
| --- | --- | --- | --- |
| F1 | 闸门误杀 | `numeric_gate.py:_evidence_sources()`（113-131）增加从 `rag_evidence` 原文抽取数字；**收紧条件：仅接受当前 claim 的 `evidence_ids` 实际绑定过的 chunk** | 50min |
| F2 | 前端说假话 | `demo-app.js` `degradedReason()`（1617-1632）在 `evidence_incomplete` 前增 `numeric_gate_rejected` 分支；`DEGRADED_COPY`（1635-1660）增对应真话文案 | 25min |
| F3 | blocked 字段仍被采用 | `main.py` `_r1_result()`（2198-2375）/`_r2_result()`（2378-2494）入口守卫：任一输入字段 `candidate_quality_status == "blocked_pending_human_confirmation"` → 返回新状态 `DATA_BLOCKED_PENDING_HUMAN`，透传已登记的 `candidate_quality_issues` 原文。同步改 `schemas.py` 枚举、`_select_ai_analysis_route()`（3124-3142）、`partial_prescreen`（4042-4049）、前端文案、`delivery.py` | 40min + 回归 |
| F4 | `additional_procedure_required` 被坍缩 | `demo-app.js:831-833` 停止无条件改写；`AUDIT_OVERVIEW_META` 增 `additional_procedure_required` 条目（如"需执行额外程序后判断"，`procedureIds` 按实际依赖选取） | 30min |

**F4 是四个里性价比最高的**：改动最小，直接消灭"总是资料不足"的观感，且让 107 次历史运行的真实结论第一次被正确显示。

**F3 的代价必须先接受**：6 个演示案例从（假的）`candidate` 变成 `DATA_BLOCKED_PENDING_HUMAN`，可用演示面 **15 → 9**，"资料受限"类结果**会变多**。换来的是评委点任何案例都看不到 93 亿百分点。

**阶段一验收（`AGENTS.md §7` 硬性要求）**
1. 新增单测：F1 三条路径（已绑定 chunk→traced / 未绑定 chunk→仍 unverified / 任何 chunk 都没有→仍 unverified）
2. F3 单测：blocked→不出 candidate；`human_review.decision=accepted` 后→恢复可出 candidate
3. **五粮液回归：blocked=0，必须仍是 `candidate` + gap 0.5562 + `retain`，不得被误伤**
4. 长江电力/紫金矿业/立讯精密实测：不再出现 1e7 量级 `growth_gap`
5. `backend/tests/` 全量 + `scripts/check_chinese_comments.py`（≥10%）+ `git diff --check`
6. 真实浏览器四视口（1440×1000 / 1024×768 / 768×1024 / 390×844）、控制台零错误、失败请求检查、截图人工查看

### 阶段二 · 让已有层级显形（约 3 小时，不新增任何阈值）

**S1 · 综合审计关注卡**（采纳 GPT 结构 + 我加的一栏）
每个待核查事项渲染为一张卡：

```
【处置】retain · 建议保留            ← ai_recommendation，前端标签已存在
【触发信号】收入 -54.55%，应收 +1.07%，增速差 55.62pp（强阈值 30pp）
【AI 反证】① 经销"先款后货"结算方式或可解释背离（年报第10页）
          ② 账面余额与净额口径差异或影响增速可比性（年报第89页）
          两条均标记 support_status = unverified_hypothesis
【未解决矛盾】为何收入下降过半而应收基本稳定？绝对变动仅 39.89 万元、占收入 0.09%
【本次判断及依据】保留候选待人工复核 —— reason_for_status 原文
【什么能改变判断】取得收入骤降原因、主要客户结构、账龄、期后回款、信用政策后重新评估
【下一步程序】AP-01 / AP-03 / AP-06
```

数据来源全部已存在，**这张卡是纯渲染工作**，零新增计算、零新增专业判断。
改点：`demo-app.js` 新增渲染函数（挂到 `deriveAuditOverview()` 之后）、`styles.css` 沿用现有 token、`delivery.py`（369-374）同步进备忘录。

**S2 · 把第二三屏的护城河提到第一屏**（采纳 GPT §5）
- 认定—证据—程序覆盖矩阵（`coverage_matrix.py`）
- 证据适配度四级（`evidence_fitness.py`）
- 反确认偏差记录（`anti_confirmation.py`）——**这条尤其要展示**：它证明系统主动去找过"正常解释"，是通用大模型绝不会做的事

**S3 · 双案例对比演示**
五粮液（retain / strong / +55.62pp）+ 标准股份（defer / 未触发 / -13.20pp / R2 不可比）。
两个都是 Level A、都有真实 live 成功记录、blocked 字段都是 0。**这是"层级区分"最便宜也最有力的证明。**

### 阶段三 · 若仍要显式分级（约 3 小时，可选）

**只用已有字段的组合导出，禁止新阈值。** 名称用 **「审计关注优先级」**（GPT 的命名，优于我的"核查优先级"），绝不叫"风险评级"，绝不出现"无风险"。

| 级别 | 条件（全部来自已有字段） | 含义 |
| --- | --- | --- |
| **F1** | `candidate` ∧ strength=strong ∧ recommendation=retain | 强信号，复核后保留 → 优先核查 |
| **F2** | `candidate` ∧ strength=strong ∧ recommendation∈{downgrade,defer} | 强信号但复核有保留 → 优先核查+补强 |
| **F3** | `candidate` ∧ strength=standard | 中度信号 → 常规核查跟进 |
| **F4** | `RULE_NOT_TRIGGERED` ∧ 数据完整 ∧ 无 blocked | 未触发候选（**正面结论**） |
| **S** | 无候选 ∧ recommendation=defer | 暂缓判断，补资料后复核 |
| **G** | `DATA_GAP` / `DATA_NOT_COMPARABLE` / `DATA_BLOCKED_PENDING_HUMAN` | **资料受限，不出级**，只出资料清单 |

关键设计约束（针对 Opus/Gemini 的硬伤）：
- **`G` 是独立出口，不参与 F1-F4 的升降。** 缺资料 → 不出级，而不是升到最高级。
- `materiality_assessment` 作为**并排标注**，不折进级别（五粮液是"未评价"，折进去会导致 F1 永远拿不到）。
- **不做 0-100 分**，**不做雷达图**。

实测归属：**五粮液 = F1**，**标准股份 = S**（若 R2 不可比主导则 G）。两个案例自动落在不同级，无需造数据。

需要你以队长身份签字确认这六档口径。改 `schemas.py` 会让已有的 `signoff_stale_requires_reapproval` 继续 stale（本来就是 stale，不新增损害，但要知情）。

### 明确不做

- ❌ 不加 R3/R4/R5 新规则（GPT 也持此意见，且 `RuleId` 是 `Literal["R1","R2"]`，牵连 schema/路由/前端/测试全链）
- ❌ 不引入 0.08 之类未签字阈值
- ❌ 不改 15pp / 30pp 冻结阈值
- ❌ 不放松 Agent 的 claim 数量与 evidence_id 绑定约束
- ❌ 不用缓存回放录制
- ❌ 不做 0-100 评分、雷达图、整体视觉重构
- ❌ 不把 `incomplete_*` 或 `DATA_BLOCKED_*` 直接改成 `success`

### 时间预算

| 阶段 | 工时 | 累计 |
| --- | --- | --- |
| 阶段一 F1-F4 + 全量验收 | 3.5h + 2h 验收 | 5.5h |
| 阶段二 S1-S3 | 3h | 8.5h |
| 阶段三（可选） | 3h | 11.5h |
| 真实模型跑双案例 + 录制 | 1.5h | 13h |

24 小时够做完三个阶段并留缓冲。**若时间被压缩，砍序：阶段一 → S3 → S1 → 阶段三 → S2。**
阶段一是底线：不做的话，视频里会出现一句关于系统的假话（缺陷二）和可能的天文数字（缺陷三）。

---

## 六、录制脚本修正版（替换 GPT/Gemini 的版本）

**幕一 · 提出质疑（30s）**
"把五粮液 2025 年报丢给通用大模型，它会给你一段流畅的话。你无法验证其中任何一个数字，也无法把它归档为审计底稿。"

**幕二 · 真实运行（90s）**
选五粮液 → 完整分析 → **真实调用，不用回放**。镜头停在：3 次 provider 调用、`input_tokens`/`output_tokens` 跳动、`response_sha256`。
出结果：收入 -54.55%、应收 +1.07%、**增速差 55.62pp，超强阈值 30pp**。
点开任一数字 → 年报 PDF 具体页码。

**幕三 · 三 Agent 对抗（90s，全片核心）**
按**界面真实角色名**讲：质疑 Agent / 反证 Agent / 复核 Agent（**不要说 Arbiter**）。
- 质疑 Agent：提出核心待核查主张
- 反证 Agent：给出两条**公司特定**的正常解释——白酒经销"先款后货"、账面余额与净额口径差异；两条都被标为 `unverified_hypothesis`
- 复核 Agent：**`retain` 建议保留**（**不要说 defer**），理由是"正常解释均为待验证假设"，并列出四项必须取得的资料

台词：**"注意，系统没有把异常直接等同于风险，也没有因为存在合理解释就撤销候选。它把两条抗辩都标为待验证假设，保留候选，并明确说明什么证据能改变这个判断。"**

**幕四 · 对比标准股份（45s，证明层级区分）**
同一套链跑标准股份：R1 增速差 **-13.20pp，未触发**；R2 因经营现金流跨期变号判 `DATA_NOT_COMPARABLE`；复核 Agent 给 **`defer` 建议暂缓**。
台词：**"同一条链，两家公司，两个不同处置：一个 retain，一个 defer。系统不是对所有公司输出同一句话。"**

**幕五 · 自我拒绝（45s，可选但极强）**
若阶段一 F1/F2 已完成，展示数字闸门：模型写下的每个财务数字都必须追溯到已登记来源，追溯不到就不发布为完整结果，并在页面上如实说明是哪个数字、为什么。
台词：**"通用大模型永远不会拒绝自己。我们把'不能证明的话不许说'写成了硬闸门。"**

**收尾逐字念：** "AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。"

---

## 七、需要你决定的五件事

1. **F3 的代价是否接受**：可用演示面 15 → 9，"资料受限"结果变多。（上一轮你已选"现在就修根因"，但当时我还没量出这个代价）
2. **F1 动的是专业控制项**：数字闸门可追溯来源从"仅结构化字段值"扩到"已绑定 evidence_id 的年报原文片段数字"。是否以队长身份确认此口径？
3. **F4 是否照我建议改**：`additional_procedure_required` 不再被改写成 `data_gap`。这会让"需执行额外程序"第一次作为独立结论显示——**这是消灭"总是资料不足"观感的关键一改**。
4. **阶段三是否要做**：六档「审计关注优先级」（F1/F2/F3/F4/S/G）口径你是否认可并签字？是否同意"数据缺口只决定出不出级、不参与升降"这一修正？
5. **`no_trigger_confirmed` 零次出现**是否需要处理：你的系统目前**从未能对任何公司说"这家没问题"**。是接受（审计计划阶段本就不做无风险认定），还是要让"未触发 + 数据完整 + 无 blocked"这条路径真正可达并显示 F4？

---

## 八、本轮核验所用命令与文件

```
backend/runtime/runs/*.json                     2342 个全量扫描
  五粮液 35 次 live 的角色结论分布                → 证伪 GPT 的 defer 主张
  RUN-V7-F7BEF8DA5846 / 3FC0FACC00A5 / B655B4D7EAEE → 证实标准股份 3 次 live、gap -0.13197、defer
  RUN-V7-43AD8C03C3A7 / BB86D15EEE1E / D8933A6BBDDB → 09-19 三次失败，同一数字 41,813,685.32
  RUN-V7-2164293EAD03（紫金）/ 1F0A9182F569（长江电力）/ 454296D35670（立讯）→ blocked 字段实测值
backend/cache_seed.materialized.json            → candidate_quality_status / raw_excerpt / 影响面 30/50、6/15
backend/competition_demo_cases.json             → Level A = 3 个（证伪 Opus 的"唯一"）
backend/app/numeric_gate.py:113-131,204-234
backend/app/main.py:2198-2494,3124-3142,4042-4049,4286-4312
backend/app/cases.py:1885-1886,1930             → 阻断标记写入点（读取 0 处）
backend/app/field_extraction.py:412-443         → 第436行设计决定原文
backend/app/schemas.py:15,73-81,160-163,173-179,180,190,214-262
assets/official-v4/demo-app.js:100-148,794-848,1592-1660   → 第831-833行为缺陷四
opus修改建议 .md
```

**我自己的错误更正**：上一轮称"标准股份 live = 0"，实为过滤口径错误（只统计了 `candidate` 命中的运行）。标准股份有 3 次成功真实 live 运行。

未核验项：五粮液 `materiality_assessment="未评价金额重要性"` 的成因（`main.py:2300-2311`）；
GPT 提到的"B0 盲评包"是否可用及其实际内容；`additional_procedure_required` 在 `delivery.py` 导出链中是否也被降级处理；
阶段一 F3 对 439 次历史 candidate 命中的完整回归影响面。
