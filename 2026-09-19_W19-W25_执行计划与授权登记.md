# 2026-09-19 · W19—W25 执行计划与授权登记

状态：**执行终态（2026-09-19）**。本文件登记四项口头授权的书面化、工作分解、预算与回滚。
合回 main 已完成（fast-forward 至 `98e296b`），合并后冒烟通过。预算已回填，见 §四。
上位文件：`docs/CONTRACT_2026-09-19.md`（含 §九 裁决 R1—R5）、《2026-09-19_综合执行方案_多AI并行.md》、`AGENTS.md`。

AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。

---

## 一、队长四项指示的书面化（2026-09-19 晚）

| # | 口头指示 | 书面化口径 |
| --- | --- | --- |
| 1 | 先写详细 plan 再做 | 本文件；执行中偏离本计划即停并报告 |
| 2 | 授权 DeepSeek 调用 | 解锁 W21/W22/W23（原 D9 口径 ≈10 次）。**预算：目标 ≤12 次，硬上限 15 次**（含失败重试；达到上限即停并报告，不得自行追加）。模型固定 `deepseek-v4-flash` 官方直连；Key 只存在于环境变量，不入源码、配置示例与 Git。每次调用落 run JSON 与台账 |
| 3 | 记得补注释 | `backend/app` 中文注释比例当前 10.49%（红线 ≥10%，余量 0.49pp）。规则：**凡本轮触碰的 `backend/app` 文件，净增代码必须同步净增中文注释/docstring，每次提交前跑 `scripts/check_chinese_comments.py`**，跌破 10% 即视为提交阻断 |
| 4 | 工作树最终合回 main | 确认。策略：`remediation-2026-09-19` 上每完成一个阶段做一次检查点提交；W21 授权后先跑一次冒烟，**若 fresh live 成功则 Golden Run 绑定冒烟后的 HEAD**；W25 前完成合回 main（fast-forward），合并后跑一次冒烟确认，不推送、不部署 |

## 二、分支与提交策略

- 工作分支：`remediation-2026-09-19`（当前 HEAD `4f5a5c9`，main 在 `e17e007`，merge-base = `e17e007`，领先 main 恰好 1 个提交，可干净 fast-forward）。
- 检查点：P0 补注释 → W19 → W21 冒烟（若通过，此 HEAD 为 Golden 候选基线）→ W22+W23 → W24 收尾+W25 → 合回 main。
- 红线复述：不改 `competition_demo_cases.json`、`release_records/**`、既有历史 run JSON；不引入新阈值；`RuleId` 不变；不推送、不部署。

## 三、工作分解

### P0 · 中文注释补强（无逻辑改动）
- 对象：`planning_priority.py`、`evidence_closure.py`、`numeric_gate.py` 的 09-19 新增段、`main.py` 新增挂载段。
- 验收：`check_chinese_comments.py` 明显高于 10.49%（目标 ≥11.5%）；相关测试文件全绿；提交阻断检查。
  **终态：2819/26658 = 10.57%，硬门槛 ≥10% 达标；≥11.5% 目标未达成**（补齐需约 280 行注水注释，
  会降低可读性，故不补）。达标与达到目标两项分开登记于 PROJECT_STATUS.md。

### W19 · 三端逐字段比对【必做，串行】
1. **静态比对（无服务器）**：以 `backend/tests/fixtures/run_contract_mock.json`（轨 A 用当前 HEAD 代码从 `RUN-V7-43AD8C03C3A7` 算出）为输入，逐项核对六个契约字段（`planning_priority`/`evidence_state`/`disposition`/`materiality_display`/`execution_badge`/`numeric_gate_summary`）在后端 JSON ↔ 前端契约断言（`verify_demo_outcome_judge.mjs`）↔ `demo-app.js` 读取路径三处一致。
2. **真实后端比对**：模型全关启动 8010 实例，读取 `RUN-V7-1FF5B1149044`（确定性备用，P2/E2/defer），抓取 `/api/demo/runs/<id>` JSON 与 `GET /api/runs/{run_id}/report.docx`（`main.py:6977`），解包 docx XML 逐字段比对；再对一份**旧 run（无新字段，如 `RUN-V7-45BFA3297C39`）**验证三端优雅降级（显示「未提供」而非报错/编造）。
3. **浏览器渲染比对**：4 视口加载该 run，截图 + DOM 断言（复用轨 C 的 W24 脚本），核对页面显示值与 run JSON 逐字一致。
- 完成标准：三端零分歧；分歧无法当场对齐 → 按方案回滚到合并前 commit。

### W20 · 分层测试【必做】
按方案第 6 章顺序：7 组针对性测试（逐项记录命令+退出码）→ 全量 `pytest backend/tests -q` → `git diff --check` → 中文注释检查。执行前读每个脚本参数，确认不触发付费调用。**不引用历史通过数**。

### W21 · 五粮液 3 次 fresh external_live【必做，付费 3 次】
- 前置冒烟：先 1 次完整分析验证链路，通过后才计为第 1 次（冒烟本身即第 1 次，不额外浪费）。
- 每次记录：run_id、execution_mode、cache_hit、provider_call_count、三角色状态、ai_recommendation、analysis_conclusion、numeric_claim_trace.passed、run_completeness、duration_ms、tokens。
- 判据（方案 §7）：growth_gap ≈ 0.5562、screening_strength=strong、trace.passed=true、complete_public_prescreen、retain。任一不达 → 记录具体数字回 W01，**不得放宽闸门**。

### W22 · Demo Case Matrix【必做，付费 ≤6 次】
- 四类起步：①五粮液（risk_candidate/retain）；②未触发常规型（P4 案例，先从免费 deterministic 通道筛候选再定）；③`industry_boundary` 候选中国海油；④`data_gap` 候选标准股份（R2 `DATA_NOT_COMPARABLE`）。
- 硬约束：不以旧 manifest 猜当前结果；6 个 blocked 案例只作第④类或 W16 文案演示；**案例选定最终由队长签字**。

### W23 · Golden Demo Run 冻结【必做，付费 1 次】
- 在最终 HEAD（注释补强完成后）重跑五粮液 fresh live，冻结：run_id、commit SHA、model ID、prompt version、三角色状态、provider call 哈希、Numeric Gate 状态、证据页码、Review recommendation、timestamp。
- Golden Run 是验收基线，**不是** cache replay 的借口；录制时必须真实重跑。

### W24 收尾 · 浏览器复验
- 用新 HEAD + 新 run 复跑轨 C 的 4 视口 × 状态矩阵（`outputs/track-c-w24-20260919/` 脚本可重跑）；`cache_replay` 本机不可达，维持"未验，不计通过"口径。
- 截图人工查看；控制台/网络/溢出/键盘/reduced-motion 逐项过。

### W25 · 录制脚本终稿
- 五幕结构 + 三处更正：五粮液说 **retain**（绝不说 defer）；角色名 **质疑/反证/复核 Agent**（无 Arbiter）；`defer` 只用于标准股份。
- 口播与屏幕逐字一致；收尾逐字念 AI 声明；**真实运行，不用回放**。

### 合回 main
- `git checkout main && git merge --ff-only remediation-2026-09-19`；合并后冒烟（启动 8000 + 打开页面 + 一次免费 deterministic 运行 + 合同测试）；不推送。

## 四、预算台账（执行终态，2026-09-19 回填）

单位说明：**"次数"指完整链运行次数**；provider 调用次数另列。此前未写明单位，现统一。

| 项 | 完整链次数（计划） | 完整链次数（实际） | provider 调用（实际） | 备注 |
| --- | --- | --- | --- | --- |
| W21 五粮液×3 | 3 | **4** | 12 | 含 1 次旧 `:8000` 诊断运行 `RUN-V7-9F95459E6259`（发现契约字段未挂载） |
| W22 矩阵 | ≤6 | **3** | 9 | 标准股份 / 中国海油 / 长江电力 |
| W23 Golden | 1 | **0** | 0 | 复用 W21 的 `RUN-V7-901E9144479`，未额外消耗 |
| 机动（失败重试/补验） | ≤2 | **1** | 3 | 缓存态补验 `RUN-V7-8C272297D1EB`，未命中缓存 |
| **合计** | **≤12** | **8** | **24** | 未触硬上限 15；W21/W22 验收子集为 6 次/18 次 |

不得把 6/18 的验收子集当作整轮总消耗。全部 `external_live`、`cache_hit=false`。

## 五、回滚与失败处置

- W19 发现三端语义分歧无法当场对齐 → 回滚到 `4f5a5c9`。
- W21 出现 `incomplete_numeric_claims` → 记录未通过数字，回到 W01，不放宽闸门。
- 任一脚本疑似触发计划外付费调用 → 立即中断该脚本并报告。
- 全量测试出现无法当场解释的失败 / 注释率跌破 10% / 历史 run 读取失败 → 触发方案第 11 章全局回滚条件。

## 六、暂不处理（明确排除）

- `docs/review-assets/2026-09-15_realname_identity_restore.json`（9-15 遗留，未跟踪，待队长单独处置）。
- `run_contract_mock_degraded.json`（R5 已授权轨 A，非 W19 阻塞项）。
- W11(b) 路由根治：赛后欠账，见 `PROJECT_STATUS.md`。
