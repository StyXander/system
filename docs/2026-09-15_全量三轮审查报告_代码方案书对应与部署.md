# 审迹智链全量三轮审查报告（代码自审 · 方案书 · 方案书↔代码对应 · 网站部署）

日期：2026-09-15。HEAD：`9369d2c`。工作树：脏（47 个受控文件处于"已删除未提交"，另有 `index.html`、`assets/official-v4/{demo-app.js,styles.css}` 在审查期间被另一会话持续改写）。

统一 AI 声明：AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。

## 0 方法与三遍口径

- **第一遍（广度扫描）**：四条独立审查线——后端全模块静态自审、方案书主张盘点（含 .docx 解包取正文）、部署声明面与线上只读实测、前端内容与接线核对。全部只读。
- **第二遍（逐条自验）**：对第一遍每一条承重结论用 `grep`/`sed`/实测 HTTP/脚本复算独立复核。**推翻 2 条、修正 4 条**（见 §3）。
- **第三遍（反向复核）**：对本报告结论找反证、复跑项目自带的门禁命令、清点未覆盖范围（见 §6、§7）。
- 我未执行的：浏览器交互与多视口截图、任何 POST 分析任务（会产生真实模型调用）、git 写操作。provider 调用 0 次。
- 第三遍已执行全量测试：`pytest backend/tests -q` → **1 failed / 503 passed / 1 skipped，267.22 秒**（HEAD `9369d2c`，工作树含另一会话对 `index.html`/`demo-app.js`/`styles.css` 的未提交改动）。唯一失败是 `test_delivery_package_build.py::test_judge_package_can_build_without_legacy_office_files`；我单独复现了它调用的 `scripts/build_delivery_packages.py --package judge`：进程退出码 1，自带日志给出"最终状态：PARTIAL""本次处理 1 个目标包，其中 **0 个包成功**"。该包内容依赖 `outputs/evaluation_v4/.../blind-packages/`，而该目录正是 R-01 被删除的文件，**高度相关但未做 restore 后复跑确认**。

## 1 四条必须先处理的结论

**R-01（P0，证据完整性）** `outputs/` 与 `artifacts/` 两个目录在工作树中**整体不存在**。`git status` 显示 47 个受控文件被删除、未提交，共 8863 行删除：`outputs/evaluation_v4`（31，含 15 案盲评包）、`outputs/evaluation_v5`（4，含 `EVAL-20260826-B1B3-CURRENT-R3` 的队长人工评分与澄清件）、`outputs/external_model_acceptance/current.json`、`outputs/evaluation_v3/current.json`、`archive/2026-09-02`（10，含历次验收报告）。已验证这些文件在 HEAD 中完好（`git cat-file -e` 两例均通过），`git log --diff-filter=D -- outputs/` 无删除提交，工作树也没有对应的新位置或未跟踪副本。
影响：方案书 §8.1、发布记录与 `docs/` 多处引用的原始记录此刻在磁盘上读不到；评委或队友从本机复验会直接失败。恢复方式是一条只读性可逆命令（`git restore outputs/ artifacts/ archive/`），属用户文件处置，我没有代为执行。

**R-02（P1，部署真实态）** 线上 `GET https://audittrace-demo.onrender.com/api/demo/bootstrap` 实测（我本人执行，HTTP 200，34,692 字节）：`bootstrap_ready=true`、`model_readiness.full_analysis_ready=true`，15 案中唯一非就绪的就是**推荐第一位** `STD_DEV_T0`，其 `rag.status=not_built`、`source_status=source_missing`；另 14 案就绪。根因链：`*.pdf` 被 `.gitignore` 排除 → `render.yaml:7` 无条件带 `--allow-missing-sources` → 官方 403 即降级。前端 `demo-app.js` 有 featuredReady 告警，属部分缓解，但整体就绪位仍为 true。

**R-03（P1，人工门禁被绕过）** `cases.py:1347-1360 / 1370-1378 / 1605-1613`（三处我本人复验）：人工"更正"只写 `row["value"]` 与 `row["pdf_page"]`，**不更新 `row["candidate"]`**；而下一次字段重提取时，`same_candidate` 判据比较的正是 `candidate.value/pdf_page/document_id`，判为"候选未变"后把 `human_review` 与 `human_review_history` 整体 deepcopy 继承到新行，同时把 `value` 重置为机器候选值。净效果：**真人校正值被静默回退，行记录却仍带着真人的确认留痕**。这与 AGENTS.md §3"AI 不得代签、不得伪造人工复核"和 §5 的人工边界直接冲突，且没有任何测试覆盖该路径（该函数的测试只断言 confirm/reject 分支）。

**R-04（P1，人工复核可被自称）** `schemas.py:315-321`：`reviewer_type` 默认值就是 `"human"`，`reviewer` 允许空串，`reviewed_at` 由请求给定。`main.py:6192` 的 `POST /api/runs/{run_id}/review` 确实做了租户与案例写权限校验（`_identity_tenant`、`authorize_case_write`）并拒绝对共享演示实例改状态，但**服务端没有任何"这一步出自真人"的证据绑定**；`delivery.py:123/248` 与 `main.py:1854` 判断能否正式缓存/导出时，只看存下来的 `review.status` 与 `export_approved`。因此任何具备合法会话的自动化调用方（脚本、demo worker、带租户 Cookie 的程序）一次 POST 即可把自己标成人工复核并解锁正式导出，全仓无一条测试覆盖该情形。详见 §2.7 C-27。

## 2 第一遍发现全集（按线分类，标严重度）

### 2.1 后端代码自审

| 编号 | 位置 | 事实 | 影响 | 级别 |
| --- | --- | --- | --- | --- |
| C-01 | `knowledge_rag.py:112`、`main.py:3317` | `query_text` 取自 `context["question_text"]`，全仓**无任何写入方** | 生产相关性只来自 `KB-R1/R2` 受控词，逐案不变；真实问题文本没进门禁（详见 §3 修正） | P1 |
| C-02 | `main.py:923` vs `:2136`、`:1682`、`:2551` | 同一字符串 `supabase_persisted_verified` 既是 `_validate_sources` 跳过本机文件与 SHA-256 复算的开关（`1682`/`2551` 处用法正当：`if local_rows is None`，确为持久化来源），又被 `_public_case()` 当作展示层默认值无条件填给任何缺 `source_mode` 的字段 | **标签失真**：未校验的字段在公开 JSON 里显示成"已持久化验证"。第三遍已证实两条路径不相接，**不是校验绕过**（P1→P2/P3） | P3 |
| C-03 | `auth.py:282-295`、`supabase_adapter.py:186-189` | `demo_completed_result_durable = demo_mode=="supabase"`，而 `demo_mode` 只看环境变量开关，不看 `SUPABASE_URL/SERVICE_ROLE_KEY` | 无凭据也宣称"结果可跨重启读取"。线上 `configured=true`，暂未表现，语义仍错 | P2 |
| C-04 | `render.yaml:7`、`public_catalog_bootstrap.py:56-67` | 目录种子失败只 print 不 `sys.exit`，退出码恒 0；`--allow-missing-sources` 无条件带 | 构建可在"缺原件/半发布"下静默通过 | P2 |
| C-05 | `启动审迹智链.bat:31` vs `render.yaml:7` | 本机装 `backend/requirements-lock.txt`，线上装开区间 `backend/requirements.txt`（其中还含 `pytest`） | 验收版本≠线上版本，构建不可复现；生产多装测试依赖 | P2 |
| C-06 | `main.py:761-769`、`:804-831`、`render.yaml` | 未配 `AUDITTRACE_TRUSTED_PROXY_HOPS/CIDRS`（要求成对），startCommand 无 `--proxy-headers` | 每 IP 额度 `AUDITTRACE_MODEL_RUN_LIMIT=2/15min` 可能塌缩为全站 2 次（**代码推得，未双客户端实测**） | P2 |
| C-07 | `rag.py:598/611/624/631` | `status()` 四处硬编码 `source_status:"source_available"`，不检查原件是否存在；`main.py:5030` 只为 `STD_DEV_T0` 纠偏 | 其他案例缺原件时 `/api/rag/status` 仍报 source_available | P2 |
| C-08 | `main.py:3275`、`test_batch5_privacy_consent.py:329,368` | `_run_rag_for_candidates` 是无生产调用方的别名，两条隐私测试打的正是它 | 补丁不生效，测试跑的是真实 `_run_rag_for_analysis`；名不副实（详见 §3 修正） | P2 |
| C-09 | `seed_catalog.py:23,175-176` | `_DEMO_RETRIEVALS` 进程内只增不删，匿名可达 `POST /api/rag/retrieve` 也写入 | 免费档内存风险 | P2 |
| C-10 | `knowledge_rag.py:175-177` + 实测 manifest | 13/13 条目均无 `excerpt_sha256`，命中里的 `content_sha256` 是本地现算，不做来源区分 | "摘要哈希可回验"被高估（哈希是自算的，不是登记的） | P3 |
| C-11 | `main.py:6792` | `str(error)` 直接作为 422 返回，OSError 文本含本机绝对路径 | 与 `source_cache.py` 的脱敏口径矛盾 | P3 |
| C-12 | `knowledge_rag.py:252-256`、`model_quality.py:54` | 计算的 `allowed` 从未使用；质量排除集缺 `"external_cached"`（`main.py:2861` 会写该值） | 前者死代码，后者靠计数归零侥幸挡住 | P3 |
| C-13 | 未接线模块 | `knowledge_ingest.py` 全部导出、`prescore.py:39`、`demo_run_tasks.py:731`、`rag.py:285/289/293`、整模块 `demo_worker.py`（render.yaml 未引用，其 18 条行为承诺零测试） | 死代码被当成能力存在 | P3 |
| C-14 | `test_competition_demo_plan.py:943-944` | 六阶段状态集合覆盖所有可能值 | 恒真断言，未验证阶段推进语义 | P3 |
| C-15 | `cninfo.py:65/648` | 100MB 上限 + 校验后 `read_bytes()` 全量入内存再 `fitz.open(stream=)` | 免费档内存风险（公共路径已被 `main.py:6040` 挡为仅命中种子） | P3 |

### 2.2 方案书

| 编号 | 位置 | 事实 | 级别 |
| --- | --- | --- | --- |
| D-01 | `27_…首页视觉同步版_2026-09-10.docx` 九章图7下方 | 声明写成"AI生成**分析**内容，…"，非逐字；同文档逐字版全文仅出现 1 次（我用解包正文计数验证） | P1（AGENTS.md §3 明令不得改写） |
| D-02 | 同上 表10 | "384 项测试通过、1 项警告"（截至 9-07）。当前 `--collect-only` 为 **505**；`PROJECT_STATUS.md:36` 还记着 1 例 `test_all_seed_cases_enter_three_role_external_route` 间歇失败且"根因未定位、不写成稳定全绿" | P1 |
| D-03 | 同上 表7 | Supabase 演示台账/RLS 列为"当前实现"，而 `current_release.json` 是 `pending_render_secret_and_production_smoke`、`competition_release_ready=false` | P2 |
| D-04 | 同上 表10 | "严格 axe 为 0 violation" 的证据目录 `artifacts/review-plan-20260907/browser-final/` 现已不存在（见 R-01）→ 该结论此刻不可回查 | P2 |
| D-05 | 同上 表10 | "15 案…与 51 案基础案例库"：15 案可核（`competition_demo_cases.json` case_count=15、45 份文档带 SHA-256）；51 案的验收报告文件正是被删除的 `archive/2026-09-02/` 内容 | P2 |
| D-06 | `15_…_V4_竞赛提交版_2026-08-25.md:12/16/170/171/178` | 一页叠三段历史口径；仍写 "B3 三案均完成三角色 `model_success`""9 条不可覆盖原始记录"（指向已不存在的 `outputs/evaluation_v5/…`）、"329 passed"、"qwen3.5-plus 7/10=70.0%" | P1 |
| D-07 | `15_ md §8.1` R1 签字 | `r1_signoff_20260825_r2.json` 存在但 `signer_name=null`、`confirmation_source=codex_user_explicit_confirmation`；签字自带"阈值/映射变更须重新确认"，而 09-09 已改字段列身份与检索口径 → 需真人重签 | P1（人工门禁） |
| D-08 | `12_方案书事实断言清单_V4.md` | 自称"唯一事实底座"，数字停在 08-22 的 241 passed，无 9 月条目 | P3 |
| D-09 | `26_…_竞赛核对修订稿_2026-09-08.docx` | 第 2 页含导师真实姓名与手机号（27b 已隐去）→ 26 不得作为提交稿 | P1（盲审/隐私） |
| D-10 | 版本口径 | AGENTS.md 只指 15_/16_ 为主文件，实际对外正文已是 27b；`16_` §2.3 仍裸写"当前质量窗口 7/10＝70.0%"未标 superseded | P2 |
| 正面 | 27b 全文 | **未发现**准确率/识别率/优于基线/提升 X% 类主张，未发现舞弊认定、审计意见、投资建议越界表述 | — |

### 2.3 前端与网站

| 编号 | 位置 | 事实 | 级别 |
| --- | --- | --- | --- |
| W-01 | `index.html:283` | `<span class="state success">同源生成</span>` 全仓仅此一处且 JS 从不改写；`renderDemoResult` 在 `outcome=failed_run` 时仍展开结果区 → 失败运行顶绿色成功态 | P2 |
| W-02 | `demo-app.js:2031` vs `:1818` | output 轴无条件标 `complete`，与同函数阶段6"未形成结构化成果"矛盾 | P2 |
| W-03 | `demo-app.js:1103` | "知识库台账尚未接入监管与准则来源"——实际 manifest 已登记 13 条/9 类；把"本次未命中"写成"尚未接入" | P2 |
| W-04 | `demo-app.js:2510-2563` | 多个 render 函数在同一 `try` 内，`byId` 缺节点即抛 → 前端合同错误被渲染成"本地后端不可用" | P2 |
| W-05 | `demo-app.js:1352/1363` | 调用 `/api/supplements/from-sample`、`/rerun-task`，后端 `require_authenticated`（`main.py:6969/7700`），而结果区按钮无 capabilities 门禁 → 匿名评委必失败 | P2 |
| W-06 | `demo-app.js:1575/1589` | 捕获了 `statusCode` 却从不读取，403 与 422/500 同为"可重试" | P3 |
| W-07 | `demo-app.js:28-99` | `STATUS_LABELS` 缺 `INDUSTRY_UNKNOWN/NOT_APPLICABLE/SOURCE_INCOMPLETE` → 界面直出英文原码 | P3 |
| W-08 | 声明与隐私 | 逐字标准声明在 `index.html:385`、`demo-app.js:17`、`styles.css:4728`（打印 `::after`）、`schemas.py:24/31` 各 1 次；但 `index.html:181`、`:246`、`demo-app.js:949` 是弱化变体；AI 草稿正文所在证据抽屉模态（`js:1992-2003`）内无声明。页面未见姓名/学校/手机号/内部路径 | P2 |
| W-09 | `index.html:443` | 图 alt"解析、风控、证据三个智能体"与同页 447-452、`demo-app.js:135-139` 的"质疑/反证/复核"三角色冲突 | P2 |
| W-10 | `styles.css` | 369 个类中 151 个在页面与 JS 中零引用，188KB 全量随首页下发 | P3 |
| W-11 | `styles.css:4763-4765` | 注释称"最浅文字抬到 #7d969d，实测 5.14:1"，实际最浅 token 是 `--at-ink-4:#626B79`；#7d969d 对白底仅 3.12:1 —— **注释记录的实测值与代码不符**（现值本身过 AA） | P3 |
| W-12 | 资产 | 未登记的 `overview-workbench.png`(861KB)、`cinematic/analysis-strata.webp`；已登记未被引用的 8 个插图 | P3 |
| 正面 | — | 案例 15/45、RAG 三分计数、成功率、来源类别计数、评估摘要、六阶段进度、token/调用次数均来自 API，非硬编码；降级/中断/取消/过期/台账不可用有独立文案；`RULE_NOT_TRIGGERED`→"程序未触发"，未写成"无风险"；`lang="zh-CN"`、skip-link、全局 `:focus-visible`、表格 caption+scope、`prefers-reduced-motion` 5 处、断点含 520px 且 `minmax(360px)` 已回落 | — |

### 2.4 部署实测（我本人执行的部分）

- `GET /api/health` → 200；`GET /api/status` → 200。线上 `deployment.commit=9369d2ce…` 等于本地 HEAD，`release_gate=blocked`，`signoff/human_scoring/fresh_production_b3/release_binding=false`，`signoff_stale_requires_reapproval`。响应无密钥、无本机路径、无手机号；CSP/HSTS/frame-ancestors/no-referrer 齐全。
- `GET /api/demo/bootstrap` → 200，34,692 字节（见 R-02）。
- `GET /api/internal/status` → 403；`/api/sources/nope.pdf` → 404；`/api/evaluations/current` → `pending_human_scoring_and_fresh_model_runs`。`/openapi.json` 公网 200（可枚举全部路由）。
- 免费实例响应 3–14 秒（本机代理链路另有 SSL EOF 干扰，不能据此判服务端故障）。
- 工作树 `index.html` 39,658 字节 vs 线上 39,147 字节（= `git show HEAD:index.html`）→ **本地未提交的前端改动未上线**，任何"页面已更新"的验收在提交+部署前不成立。

## 3 第二遍：对代理结论的推翻与修正

| 代理原结论 | 我的复核证据 | 最终判定 |
| --- | --- | --- |
| "发布记录引用的 `EVAL-…-V1.jsonl` 台账文件根本不存在" | `find backend/release_records/evaluations` → `EVAL-20260828-RELEASE-CANDIDATE-V1/dashboard.json` 与 **`raw_records.jsonl` 均在** | **推翻**。真实情况是台账存在、但 `human_scores/` 下只有 README 与 rubrics，人工评分为空 |
| "标准股份登记 2022 年报与清单矛盾，2022 仅存根目录 PDF" | `backend/.venv/… -c "from backend.app.data import ANNUAL_REPORT_SOURCES"` → `years: [2022,2023,2024,2025]`；`test_prepare_full_corpus.py:22` 亦断言 4 条 | **推翻**。方案书写 2022–2025 与代码台账一致；与"15 案×3=45"是两个不同口径，不构成矛盾 |
| "知识检索相关性门禁在生产中是常量，对所有案例所有规则一模一样" | `controlled_question_terms` 按 `KB-R1`/`KB-R1-R2`/`KB-R2` 展开不同 anchor 集合 | **修正为 C-01**：常量部分是 ticker/industry 缺失，规则集之间仍变化。缺陷成立但表述要收紧——问题文本没参与，不等于词集全静态 |
| "两条隐私闸门测试的隔离 RAG 是空操作" | `main.py:3275` 别名无生产调用方；`_execute_run` 真实走 `:3610 _run_rag_for_analysis`；两条测试仍在绿集内 | **修正为 C-08**：是"补丁目标不存在"，测试实际跑了真 RAG 并通过；属测试名不副实，不是漏放行 |
| "`/api/status` 与 `/api/rag/status` 同一时刻口径相反" | `main.py:5030` 有针对 `STD_DEV_T0` 的显式 `source_missing` 纠正分支（带解释注释） | **修正为 C-07**：缺陷准确说法是 `rag.status()` 硬编码 source_available，main.py 只修了标准案例一条路径 |
| "51 案报告文件被删除" | 属 R-01 删除事实的一部分，且该文件位于 `archive/2026-09-02/`（本就是归档目录） | **降级归并**：合并进 R-01 证据完整性，不单独作为口径失效 |
| （第三遍自我推翻）我在第二遍把 C-02 定为 P1"校验绕过" | `sed main.py:915-926 / 2128-2148 / 1676-1686 / 2544-2554` + `grep _validate_sources(` → 校验函数的输入来自 `:2667`、`:3514` 的行构造，与 `_public_case()` 响应构造器不相接 | **降级为 P3 标签失真**。持久化来源标记本身正当（`if local_rows is None`），问题只是展示层无条件套用同一 token |
| （第三遍亲自补验）当前评估指针 B0—B3 状态 | 直接读 `evaluations/EVAL-20260828-RELEASE-CANDIDATE-V1/dashboard.json`：`groups.B0/B1/B2/B3` **全部 `status=not_started`、`completed=0/3`**；`raw_records.jsonl` 全文**只有 1 行** `evaluation_initialized`，且 `model_id` 仍是 `qwen3.5-plus` | **证实**：15_ md §8.1 的"B3 三案 model_success""9 条原始记录"与当前指针直接矛盾（D-06 成立） |

## 4 方案书 ↔ 代码 ↔ 证据 对应矩阵

| 方案书主张（27b/15_） | 代码侧真实来源 | 证据状态 | 判定 |
| --- | --- | --- | --- |
| 15 案冻结演示清单、45 份登记年报带 SHA-256 | `backend/competition_demo_cases.json`（case_count=15） | 在库，未复算 | **有证据支撑**（复算未做，见 §6） |
| 三 Agent=质疑/反证/复核，顺序依赖 | `agents.py:641-644`（counter 读 challenge，review 读前两者） | 代码可回查 | **成立**；页面 alt 文案冲突见 W-09 |
| 案例隔离 RAG + 确定性规则 R1/R2 | `rag.py`、`industry_rules.py`、`cases.py` | 代码+测试 | **成立**（工程事实） |
| 字段候选需人工确认 | `field_extraction.py` `source_review_status=auto_extracted_pending_human_page_confirmation` + `cases.py:1795` 闸门 | 代码+测试 | **成立**，且 09-09 已收紧为"列身份不成立即缺口" |
| "384 项测试通过" | `pytest --collect-only` 实测 **505**（47 文件）；`TEST_INVENTORY.md` 记 502 | 数字已过时且口径不一 | **须更新为"当前 505 收集，全量重跑待做"**（D-02） |
| "严格 axe 0 violation" | 证据目录 `artifacts/…` 现已缺失 | 不可回查 | **表述过强 + 证据缺失**（D-04） |
| "B3 三案完成 model_success / 9 条原始记录" | 指向 `outputs/evaluation_v5/…`（工作树已删）；当前指针 `EVAL-20260828-RELEASE-CANDIDATE-V1` 实测 B0—B3 全为 `not_started 0/3`，`raw_records.jsonl` 仅 1 条 INIT | 目录不在盘上，且指针反证 | **必须重写为历史口径或先恢复文件**（D-06/R-01，第三遍已亲验） |
| "6/6 完整链、19 次调用、0 次失败关闭" | `PROJECT_STATUS.json.deepseek_direct_local_window_20260902` | 有登记，限定语在 JSON 里但 27b 正文未带 | **表述偏强**（须带"最终完成率非首过率、样本小、不外推生产"） |
| "Supabase 台账/RLS 当前实现" | `render.yaml`/`current_release.json` = `pending_render_secret_and_production_smoke` | 配置存在≠能力通过 | **过强**（D-03，AGENTS.md §1 明禁） |
| "R1 v0.4 队长已确认签字" | `r1_signoff_20260825_r2.json`：`signer_name=null`，线上标 `signoff_stale_requires_reapproval` | 自证 + 已过期 | **须真人重签**（D-07） |
| "本地不驻留全文" | 实测 `backend/runtime/pdf_store` 78 个 PDF、561.4 MB | 与代码事实冲突 | **须改为"评委代码包不含全文，开发机留有原件"**（D-11） |
| "结果可跨重启读取" | `auth.py:295` 只看开关不看凭据（C-03） | 线上 configured=true | 当前成立，但**依据写错** |

### 2.5 后端此前未覆盖模块（第三遍补做；标"✓"的为我本人复验）

| 编号 | 位置 | 事实 | 级别 |
| --- | --- | --- | --- |
| C-16 | ✓ `cases.py:1347-1360`、`:1370-1378`、`:1605-1613` | `same_candidate` 只比"机器候选值/页码/文档号未变"，成立时把 `human_review` 与 `human_review_history` 整体 deepcopy 继承；而人工校正写入的是 `row["value"]`（`:1608` 起，**不更新 `row["candidate"]`**）。下一次重新提取会用机器候选覆盖 `value`，复核留痕仍显示真人已确认 | **P1**：真人校正值被静默回退，留痕却归属人工 |
| C-17 | `cases.py:1315,1475` + `field_extraction.py:333,444` | 字段更新按传入 rows 全表重写 `financial_fields.json`，提取只覆盖 `requested_years`；换年度或 R1→R2 复跑（`pipeline.py:649-661`）会连带删除其他年度候选与复核历史 | P2（未复验） |
| C-18 | ✓ `release_evidence.py:383,385,398` | `run_record_status` 常量为 `"verified"`、`result_hash_verified` 常量为 `True`、`environment/provider` 取自被审记录自身的 context → `release_gate.py:117,121,150` 三项成为自证 | P2（发布门禁实质弱化，接近 P1） |
| C-19 | ✓ `provider_readiness.py:95,109,121`、`:526-529` | 通道判定用子串：`"api.deepseek.com" in url_lower` → `api.deepseek.com.evil.tld` 会被标"DeepSeek 官方直连"；`host = parsed.netloc` 可把 `user:pass` 带进 `provider_label`；`known_aliases` 仍含 `qwen3.5-plus` 且用双向子串判模型可用 | P2（身份门禁 fail-open） |
| C-20 | ✓ `signoff.py:44` 写"计价和分摊"，`backend/audit_procedure_map.json` 只有"计价分摊" | 字面不一致 → `coverage_matrix.py:51,143-145` 该认定恒 `not_applicable`；`:36` 还吞掉映射文件缺失/损坏，整矩阵可静默降级 | P2 |
| C-21 | ✓ `main.py:4686` 用 `not in {3 个 pending 名}` 判人工评分完成；`release_gate.py:79` 用白名单 `{complete,completed,model_success}` | 状态页 `ready_checks.human_scoring` 可比发布门禁宽松：`"n/a"` 之类值会显示"已就绪" | P1 表述风险（装配点零测试，见 T-03） |
| C-22 | `agents.py:915→937` 与 `:972-985` | review 输出先由 `_with_review_boundaries` 自动补写边界句，之后才检查"复核 Agent 遗漏…"，失败分支不可达；schema 锁 v2，challenge/counter 不检查 | P2（未复验） |
| C-23 | `anti_confirmation.py:60,63` + `delivery.py:473-478` | `reverse_evidence_search_performed` 只取决于 route 非 None，`hit_count=0` 也报"已执行反证检索"并原样打印 | P2（未复验） |
| C-24 | `demo_run_tasks.py:588-599` | 心跳线程首次异常即 `return`，且不像 `worker.py:103` 那样置失租标志 → 180s 租约到期后同一任务可被第二实例领取，产生重复付费调用 | P2（未复验） |
| C-25 | `numeric_gate.py:25,36,139,149` | 白名单前缀永不命中（token 只含数字与单位），"技术数字不参与核对"未生效；`knowledge_trace` 参数从未使用 | P3 |
| C-26 | `run_store.py:69-79`、`supabase_adapter.py:1910` | 复核读-改-写非原子，与 `save_run` 竞态可丢复核；目录列表 N+1 远程调用 | P3 |

### 2.6 测试质量与门禁覆盖（第三遍补做）

| 编号 | 位置 | 事实 | 级别 |
| --- | --- | --- | --- |
| T-01 | ✓ `test_signoff.py:168-173` | 断言 `signoff_status in {三个合法值}` 覆盖该字段全部取值，且该用例不套 fixture 直读真实签字文件 → **签字失效或漂移写进页面也不会红** | **P1** |
| T-02 | `test_signoff.py:29,64-68,79,125` | 夹具用被测的 `current_r1_config_canonical()` 生成期望值、`_sha256_file` 被钉成 `"fake-schema-hash"` 与记录对齐、版本函数被整体 monkeypatch → `signoff.py:155-158`（配置哈希不一致）、`:124-127`（结构非法）、`:64-66`（正则失败静默回落 `"0.4"` 的 fail-open）三条分支从不被执行 | P2 |
| T-03 | `release_gate.py:199-241`、`main.py:4584-4740` | `evaluate_release_ready` 13 条阻断分支零测试（测试只到 6 条）；`_release_fact_snapshot` 全仓零测试，而它是三处 `competition_release_ready` 的真实装配点（配合 C-21 的判据宽松） | **P1** |
| T-04 | `agents.py:982,985`、`main.py:4310-4312`、`evidence_fitness.py:51,62,127-135`、`evaluation.py:22-27` | "复核遗漏应收口径/趋势局限"两条事实闸门分支（现有用例的观察串同时含两段关键词，删掉仍全绿）；模型成功降级为 `incomplete_numeric_claims` 的接线；未知 evidence_id 降级；**B0—B3 定义无任何测试钉住**（"不得改名成工程指标"这条规则无兜底） | **P1** |
| T-05 | `test_delivery_package_build.py:16` | 断言 `CLEAN_ZIP.is_file()`，校验的是上次手工构建的旧 zip，不重新构建；源码被打包器排除项漏掉时仍绿 | P2 |
| T-06 | `test_competition_demo_plan.py:1570-1572,1611-1613` | 直接给模块全局 `_demo_task_probe_snapshot/_signature/_expires_at` 赋值，不经 monkeypatch、永不还原 → 后续用例的探测缓存被永久重置，结果取决于本机 `SUPABASE_*` | P2 |
| T-07 | `provider_readiness.py:643,666` + `conftest.py`（全文 6 行） | 进程级 `_current_snapshot` 对 `live_run`/`circuit_breaker` 两种来源**无视 TTL 与环境变量直接返回**；`reset_provider_readiness()` 的 autouse 夹具只存在于 `test_provider_readiness.py:22-26` → 51 案循环跑完即把熔断/就绪态留给后续用例。这与 `PROJECT_STATUS.md:36` 那条"根因未定位的间歇失败"高度吻合（**待定向复现确认**） | P2 |
| T-08 | 根 `conftest.py:46-62`、`pytest.ini:10` | `requires_full_corpus` 在评委环境一次跳掉 16 项，含 T0 时点过滤、跨公司隔离、来源哈希硬失败、缺配置不得伪造输出、公网配额，**无替代用例**；`repository_only` marker 没有任何 deselect/skip 钩子，只是注释 | P2 |
| T-09 | `test_signoff.py:193` | 全仓唯一 `pytest.skip`，查的文件名与 `signoff.py:32` 的回退名不一致 → 文件缺失即静默跳过 | P3 |

### 2.7 最后一批模块与"能否自称人工复核"（我本人复验）

| 编号 | 位置 | 事实 | 级别 |
| --- | --- | --- | --- |
| C-27 | `schemas.py:315-321` + `main.py:6192-6211` + `main.py:1854`、`delivery.py:123/248` | `HumanReviewRequest.reviewer_type` 是**客户端自述字段且默认值就是 `"human"`**，`reviewer` 允许空串，`reviewed_at` 可任意。`/api/runs/{run_id}/review` 有租户与案例写权限校验（`_identity_tenant`、`authorize_case_write`）和 `_reject_shared_demo_mutation`，**但没有把 `reviewer_type` 绑定到任何真人证据**；`delivery.py` 与正式缓存解锁只看存下来的 `review.status`/`export_approved`。结论：**已获授权的自动化调用方（含脚本、demo worker、带合法租户 Cookie 的程序）可以自称 human 并置 export_approved=true 解锁正式导出**，服务端无法区分。全仓无一条测试覆盖"自动化身份自称 human" | **P1**（AGENTS.md §3 明禁 AI 代签） |
| C-04b | ✓ `public_demo_bootstrap.py`、`public_catalog_bootstrap.py`、`demo_bootstrap.py` | 三个构建期模块**都没有任何 `sys.exit`/非零退出路径**（grep 零命中）→ `render.yaml:7` 的 `&&` 链对构建期失败完全不敏感 | P2（对 C-04 的加强） |
| C-28 | `evaluation.py:20-27` | `B0_B3_DEFINITIONS` 四条与 AGENTS.md §5 逐字一致（好）；`EVALUATION_DIRNAME` 仍写死 `"evaluation_v4"`，与当前评估指针所在目录体系不同源 | P3 |
| C-29 | `prescore.py:1,252` | AI 辅助预评分自带边界句"不是正式人工评分；队长最终评分栏保持空白"，`model_quality.py:87` 也只认 `status=="completed"` 的步骤 → 未发现把预评分读成人工评分的路径 | 无问题 |
| C-30 | `knowledge_sources.py:220-227`、`privacy.py:32-69`、`supplements.py:85-206`、`manifest_hash.py:27-47`、`auth.py:150-209` | `pending`/未激活来源被 `source_is_active` 挡在运行之外；隐私有 `HIGH_RISK_PATTERNS` 载荷扫描；补充资料有 15MB 上限、`Path(...).name` 归一与后缀分流；鉴权对无写权限角色抛 403，本机身份需显式 attach | 未发现缺陷 |
| C-31 | `worker.py`、`demo_worker.py` | 两者在 `main.py` 与 `render.yaml` 中**均无引用**（grep 零命中）→ 整模块执行器未接线，其内部承诺的行为无任何运行证据 | P3（与 C-13 同族） |
| C-32 | `industry_gate.py:255-277` | `fit_level/family` 均为 `unknown` 时把 confidence 记作 `insufficient_metadata`；但我未端到端追完"unknown 是否仍继续跑规则"，不下结论 | 待核验 |

## 5 我核过、确认没问题的边界（避免重复劳动）

CORS 解析与通配拒绝（`main.py:319-365`）、Cookie 强制 Secure/HttpOnly/Lax（`:426-435`）、XFF 仅成对配置+受信网段采信、路径越界防护（`pipeline.py:116-121`、`supplements.py:101-106`、`delivery.py:105`、`source_cache.py:136-142`、`/api/sources/{filename}` 白名单等值匹配）、异常脱敏与启动日志不打印密钥、巨潮域名白名单与逐跳复校、403 不重试、密钥未入库（`.env` 未跟踪，受控文件无密钥/手机号样式串）、中断语义一致性（180s 租约+60s 心跳+30s 结算节流，未见"自动续跑"被写成已实现）、`render.worker.example.yaml` 未被引用（不启用付费 Worker）。
**订正**：本报告第一遍曾把 `release_evidence.py` 与 `signoff.py` 记入"逐项失败关闭、无问题"，第三遍证伪——`release_evidence.py:383-398` 有三个常量自证字段（C-18），`signoff.py` 的三条关键分支被测试绕开（T-01/T-02）。该条已删除。
2026-09-09 那轮修的字段列身份、检索相关性门禁、PDF 身份硬校验、重试分类、统一受控下载器**均已入库并随 505 项测试在库**（`secure_download.py`、`test_field_column_identity.py` 等 7 个回归文件在 `git ls-files` 中可见）。

## 6 第三遍：仍未验证与需要真人决策

未验证（我不写成结论）：45 份年报 SHA-256 复算；巨潮/证监会/财政部 URL 联网可达性；年报页码与金额回原文；C-06 额度塌缩的双客户端实测；**浏览器多视口截图与控制台检查**（另一会话正在并发改写 `index.html`/`demo-app.js`/`styles.css`，此刻做视口验收得到的是移动靶）；C-32（industry_gate 判 unknown 后是否仍继续跑规则）未端到端追完；`agents.py` 提示词主体与 `supabase_adapter.py`（1934 行）仅抽读；R-01 恢复后需复跑交付包构建确认那条红。
覆盖情况：`backend/app/` 全部 36 个模块均已在三遍中被达到（其中 `industry_rules.py`、`corpus.py`、`prescore.py`、`model_quality.py`、`evaluation.py`、`privacy.py`、`knowledge_sources.py`、`supplements.py`、`auth.py`、`manifest_hash.py`、三个 bootstrap、`worker/demo_worker` 为第三遍补做的定向核查）；`backend/tests/` 47 个文件的断言质量已在 §2.6 给出高危清单，但非逐条穷尽。

需要队长或指导老师定夺（AI 不得代决）：R-01 的证据目录是恢复还是有意清理；26_ 含导师联系方式的稿件如何处置；D-07 的 R1 签字重签；D-06/D-11 的对外口径改写幅度；C-04/C-05 是否把线上构建改为按 lock 安装并让门禁失败即中断。

## 7 建议修复顺序（本报告未改任何业务代码）

1. 先修 R-03（人工校正值被回退却保留真人确认留痕）：`same_candidate` 判据必须把"已被人工处置过"的行排除自动继承，或让人工校正值同步写回 `candidate`。这条不修，其余人工门禁都可被无声架空。
1b. 同批修 R-04（人工复核可被自称）：`reviewer_type` 不得由客户端自述决定；`reviewer` 必须非空并与会话身份绑定；`export_approved` 应要求真人凭证（如一次性签名或后台人工动作），并补一条"自动化身份自称 human 必须被拒"的测试。
2. 止证据损失（R-01）：恢复 `outputs/`、`artifacts/`、`archive/`（或明确记录为有意删除并同步改文档引用）。恢复后复跑 `test_delivery_package_build.py` 确认评委包由 PARTIAL 转成功。
3. 再修"能力声明与实际依据脱钩"：C-03（durable 只看开关不看凭据）、C-07（`rag.status()` 硬编码 source_available）、C-18/T-01（发布证据三字段常量自证 + 签字测试恒真）、C-21（状态页人工评分判据比门禁宽松）。
4. 同步修 R-02 的就绪语义：主案例无 RAG 时 `bootstrap_ready` 不得为 true，或线上补齐原件缓存。
5. 前端假成功态：W-01/W-02/W-03/W-04/W-05（AGENTS.md §7 明禁硬编码掩盖缺失后端）。
6. 部署可复现性：C-04/C-05/C-06/C-19（供应商域名子串判定按 netloc 精确匹配）。
7. 文档与代码对齐：D-01/D-02/D-06/D-10/D-11，把 27b 与 15_/16_ 的数字与状态词一次性刷新。
8. 补测试：T-03（发布门禁 13 条阻断分支与 `_release_fact_snapshot`）、T-04（B0—B3 定义与两条事实闸门分支）、C-08/T-06/T-07（无效补丁与进程级全局态）。
9. 补齐 C-01（把真实问题文本接进生产 context）。

---

## 8 本轮实施结果（同日回填，全部经命令复验）

已修并验证：R-01（47 个受控文件 `git restore` 恢复，`git ls-files --deleted` = 0）；那条红测试（真因是 gitignore 的白名单源 zip 被清盘，用 `scripts/export_case_template.py` 重建，`test_delivery_package_build.py` 转绿）；R-03 与 C-17（`cases.py` 校正值/页码/定位/复核状态同源继承 + 按 field_id 合并保留未提取年度，扩写 `test_cninfo_pipeline.py` 的重跑断言）；R-04（`reviewer_type` 默认改 automation、姓名必填、会话可归属、服务端盖章，新增 `test_human_review_binding.py` 4 项）；C-03、C-19、C-21、C-18、C-07、C-04b（catalog 半发布退出码 1）、C-05 的 pytest 出生产；R-02（`bootstrap_ready` 改为按推荐案例 runtime_ready 推导并新增 `featured_cases_pending`）；W-01/W-02/W-03/W-05；T-01（签字恒真断言）。

复验数字：`pytest backend/tests -q` → **508 passed / 1 skipped / 0 failed**（48 文件、509 项收集）；`check_frontend_contract.mjs` ok（171 ids / 181 引用）；`node --check demo-app.js` ok。

被我自己推翻的两条：① "automation 能导出报告"不是洞——`delivery.py` 会给自动化报告加水印与 `core_properties` 声明，我据此撤销了误加的限制；② "红测试由证据目录被删导致"不成立，恢复后仍红，真因是未入库的源 zip。

仍未处理（保持原级别，未淡化）：C-05 的 lock/开区间不同源（改线上安装源风险高，留队长决定）、C-06 每 IP 额度塌缩（未双客户端实测）、C-09 无界缓存、C-13/C-31 未接线模块、C-16 之外的 `agents.py:982/985` 与 `anti_confirmation`/`demo_run_tasks` 心跳/`coverage_matrix` 词面不一致、C-22/C-23/C-24、T-02/T-03/T-04/T-05/T-06/T-07/T-08、W-04/W-06～W-12、D-02/D-03/D-04/D-05/D-06/D-07/D-08/D-10（D-01 已在 28 号实名版中恢复逐字声明）。

前端与文档：`index.html`/`demo-app.js`/`styles.css` 与 `02_最终确定方案/` 全部 docx 都不在本轮 git 变更清单内可回滚范围（`02_最终确定方案/` 整目录未纳入版本控制，`*.docx` 亦不在库），改动前请确认已自行备份。

AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。
