# AI-2 复核整改：评委包复现与发布门禁可靠性修复计划

> 计划日期：2026-09-03
> 触发：对《AI-2：评委复现与发布门禁可靠性计划》执行结果的外部只读复核
> 当前状态：执行中（本文件由复核结论落地，R 编号即执行顺序锚点）
> 统一AI声明：AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。

## 0. 要修的六件事

| 编号 | 问题 | 严重度 | 需执行通道 |
|---|---|---|---|
| R1 | 历史“19 项失败”从未拿到名单，根因写成定论 | 高（表述失真） | 是 |
| R2 | Phase 6 被改定义；“环境复现”门未过却计入完成 | 高（完成度失真） | 部分 |
| R3 | `requires_full_corpus` 挂在 0 个测试上；两条契约缺测 | 中（机制空转） | 是 |
| R4 | Phase 4 的 B3 判据写在测试内部，生产无实现 | 中（门禁可绕） | 是 |
| R5 | 登记数字与留存日志不一致；实测值硬编码进生成物 | 中（溯源断裂） | 是 |
| R6 | 交付物日期与旧口径混乱 | 中（评委可见） | 是 |

## 1. 硬约束：为什么代码改动必须与重建同批

评委复现包内**含有 `PROJECT_STATUS.json`**，随包快照哈希
`DF1B9CCAD5E3352CC8F4DF8021CCB95B9891EC57AC6E3A8D7EAE33FBBDDBBC8E`
（`交付包/交付包构建与独立复验日志.txt:11`、`:144`）。因此对 `PROJECT_STATUS.json`、
`docs/TEST_INVENTORY.md`、`scripts/` 白名单成员、`backend/**` 的任何编辑，都会让已交付 ZIP
与仓库内容脱钩，直到重新打包并重登哈希。R3/R4/R5 的代码改动必须与“重建包 + 重跑复验 + 重新登记”
在同一批完成；改一半就登记数字属于本项目 2026-09-02 已发生过的失误类型。

还原临时改动一律使用带时间戳的备份文件，**禁止 `git checkout -- <file>`**：
工作区含大量未提交改动，该命令会直接吃掉它们。

## 2. R1：把“19 项失败”从推断变成实测

1. 先找历史名单：全仓 `19 failed` / `321 passed` 只有计数（`PROJECT_STATUS.json:375`、
   `README_RUN.md:124`），`docs/` 零命中。到 `archive/2026-09-02/**`、
   `outputs/audit-20260828-independent/**`、`tmp/judge-*` 找 09-02 原始 pytest 输出；
   找不到即判“名单不可复原”，相关表述按 `UNKNOWN` 写。
2. 备份 `scripts/build_delivery_packages.py` 到 `tmp/build_delivery_packages.py.bak-<时间戳>`。
3. 临时把 `PACKAGE_DATA_FILES`（`:136-142`）置为空元组，打包评委包到临时目录。
4. 解包运行 pytest，完整输出存 `tmp/r1-repro-failures.txt`。
5. 判定：失败数恰为 19 且名单一致 → 根因成立，把名单与日志路径写入
   `tmp/judge-failures-20260903.md` 并替换推断句；否则根因**不成立**，`judge-failures:20`、
   `PROJECT_STATUS.json:375`、`handover` §2.1 一律改为“怀疑待证，实测仅能证明当前包 0 failed”。
   再单独摘 `release_records` 测其贡献。
6. 用步骤 2 的备份还原脚本；恢复后重新读取本轮实测结果，不再预设 `359 passed` 等旧数字。

验收：`tmp/r1-repro-failures.txt` 存在，且每项失败指向一个具体缺失文件。

## 3. R2：完成度表述纠偏

1. `tmp/handover-ai2-20260903.md:20` 把 Phase 6 写成“全量复算与交付交接 / PASS”。
   改为“未执行——计划 Phase 6 要求 `git archive` 快照 + 新建虚拟环境；当前改动全部位于未提交工作区
   （HEAD `be0a8d451dd4e3ed8766c335d156b3f59730660c`），从快照复算取不到本轮代码”。
2. `tmp/judge-failures-20260903.md:35` 的 warning 路径证明解包复验使用的是**仓库自带 venv**
   （`数智竞赛\backend\.venv\...`）。按计划 §4 验收矩阵“环境复现”行判**未过**，需在报告与
   `PROJECT_STATUS.json` 的 `judge_package` 段标 `environment_reproducible: 未验收`。
3. Phase 1 报告的 `342 passed, 1 skipped` 已被 15:18 的 359 取代：文件顶部加一行时点声明，不改写正文。

## 4. R3：全文闸门要么有对象，要么写清“当前无依赖项”

已核实 `requires_full_corpus` 只出现在 `conftest.py:36,52` 和它自己的元测试，生产测试零使用；
提到“标准股份”的测试用的是 `tmp_path` 假 PDF 或纯字符串（`test_v7_closure.py:189,222`、
`test_knowledge_sources_manifest.py:72`、`test_merged_bug_fixes.py:160`），不读根目录真实年报。

1. 二选一并写进报告：(a) 认定“当前无全文依赖用例”，Phase 2 结论改写为“闸门机制已建、作用对象 0 项；
   19 项分类中‘需全文’一类实测为空”；(b) 为真实依赖项（来源哈希复验、RAG 检索）加 marker 使其在包内显式 skip。
2. 补两条计划要求却缺失的契约到 `test_full_corpus_skip_gate.py`：
   - 真缺陷仍失败：`pytester` 造带 marker 且断言失败的用例，在有全文时 `assert_outcomes(failed=1)`，
     证明 marker 不会把真失败洗成 skip；
   - 本机不静默跳过：`test_repo_itself_never_skips_full_corpus`（现仅断言 PDF 存在，`:51`）
     改为实际统计 `requires_full_corpus` 跳过数并断言为 0。
3. 命名耦合收一处：`conftest.py:41`、`build_delivery_packages.py:192,208`、`prepare_full_corpus.py:57`
   各自硬编码“根目录 + `标准股份*.pdf`”。当前台账标题（`PROJECT_STATUS.json:503,510,517,524`）
   均以“标准股份”开头，**今天不是缺陷**；但台账标题一改就静默失配（下载成功、闸门仍判无全文）。
   抽 `corpus.is_local_corpus_available(root)` 三处共用，并加测试断言台账标题与该 glob 一致。

## 5. R4：B3 判据搬进生产代码

此前 `test_release_gate_contracts.py` 的 B3 判据曾定义在测试内部，存在测试自证风险；
当前已迁入生产模块，但仍要把该模块接入 API 和控制脚本，并补齐严格的证据校验。

1. 新建 `backend/app/release_gate.py`：`evaluate_b3_eligibility(evidence)` 与
   `evaluate_release_ready(...)`（读 dirty、HEAD、manifest、评估指针、真人批准），缺字段一律不合格。
2. `test_release_gate_contracts.py` 改为 import 生产函数并删除内联定义；四个缺失绑定量各加正反用例。
3. 连带约束：`check_chinese_comments.py` 只统计 `backend/app/*.py`，当前 `2380/23646 = 10.07%`，
   按推算新增 155 行无中文注释的生产代码即翻为 FAIL（推算值，须实测确认）。
   新文件每段配 ≥4 汉字注释行，改完立刻重跑并登记新比值。

## 6. R5：数字溯源与生成物内联常量

1. `PROJECT_STATUS.json:371` 记 `76.67s`，留存日志只有 `74.24s`（日志 `:272`、清单 `:8`）：
   改为 `74.24s` 并附日志路径；若坚持 76.67s 须重跑并留存当次完整输出。
2. `368 passed, 1 warning in 110.91s`（`:360`）全仓无对应日志：重跑全量存 `tmp/r7-repo-pytest.txt` 后登记。
3. `build_delivery_packages.py:249-251` 把 359/368 写死进 JUDGE_README 字符串，加测试即静默失真。
   改为构建时取本次实测 passed/skipped 与 `scripts/test_inventory.py` 收集数写入 README，
   不一致即 `RuntimeError` 拒不出包（同款 fail-fast 结构见 `:211-229`）。
4. `_judge_sources` 的 `required`（`:211-222`）必须继续包含 `PACKAGE_DATA_FILES` 三项；这只是防漏装的结构性守卫，不能写成历史 19 项失败的已证实根因；
   并入 `required`，使同类漏装成为结构性不可能。

## 7. R6：交付物日期与旧口径

1. ZIP 名 `04_审迹智链_评委代码复现包_2026-09-01.zip` 装 09-03 内容
   （SHA `83695C4E3A66D6FDDB766A352FAB5EC4B568746CE6C997A6D05D7B148E93FE33`）：
   按构建日期或 release_id 命名，清单保留 SHA 以便区分版本。
2. `build_delivery_packages.py:286` 清洁运行包 README 仍写“源码仓库 177 passed / 本包 171 passed”，
   与本轮 368/359 冲突；`交付清单与扫描报告.txt:3` 说“本次处理 1 个目标包”，`:5` 却把清洁运行包
   写成已复验完成。要么本轮真重建复验该包，要么在 `:5` 标注“上次构建遗留，未在本轮复验”。
3. 交真人确认后清理（本计划不自动删）：`tmp/judge-extract/`、`tmp/judge-inside/package-113624/`
   内各存 4 份年报全文副本（本轮 15:18 物化目录在 `%TEMP%`，这两处来源待核）；根目录 `.pytest_tmp2/`。
   全文副本留在仓库树内属外发风险；`ROOT.glob` 不递归，故不污染打包判据。清理前先确认无测试以其为 fixture。

## 8. 执行顺序

事实快照 → R4 生产门禁接入与契约测试 → R3 全文闸门核对 → R5 生成物守卫 →
清洁快照与新虚拟环境复验 → R1 历史分支探针 → 修正报告 → R6 重建唯一评委包。
生产 B3、重新签字、人工评分和最终批准另由真人授权后执行。
每步产物落 `tmp/` 并在报告给路径；未执行步骤写“未执行 + 原因”，不得沿用旧数字。

## 9. 复核确认**不是**缺陷的项（避免重复返工）

- 当前评委包 ZIP 成员清单无 `标准股份*.pdf`，全文边界守住；`:228-229` 另有“仍含全文即拒绝打包”的反向检查。
- `368 = 360 + 8`；包内 34 个测试文件 + 3 个仓库专属 = 37，与 `docs/TEST_INVENTORY.md` 一致。
- 三个 `repository_only` 剔除理由可核对：`test_jack_case_package.py:18-23` 依赖仓库内案例 ZIP。
- Phase 5 为真实现：`main.py:3367/3466/4338/4603`，`test_public_status_boundary.py:27/38/44/50` 有开关两路断言。
- 台账标题与根目录 glob 当前匹配（§4.3），属耦合而非现症缺陷。
- `test_signoff.py:193` 唯一 skip 的原因与交付边界一致，随包 README 已写明。

## 10. 交回真人

提交/推送授权、新建虚拟环境的授权与耗时预算、年报全文是否随包外发的书面确认、R1 重签、B0—B3 人工评分。
若 R1 复现结果与“漏装数据文件”不符，对外表述降级幅度由真人裁决，AI 不得选择更好看的说法。

## 11. 执行台账（2026-09-03 本轮，全部为实测）

| 项 | 状态 | 实测证据 |
|---|---|---|
| R1 | **测量无效，结论收回；历史根因仍 UNKNOWN** | 已测得：探针 ZIP 确无那三份只读数据（两次核对 `HAS_DATA=[]`），该解包树 `--collect-only` 仍收 364 项，说明收集阶段不受影响。**收回原判断**：行内原写的"pytest 退出码 0 → 已否定这一支"不成立——事后三次复现证明那次运行根本没执行测试（0 字节输出、不生成 junit，改相对路径与跑单文件同样如此），退出码 0 不是"无失败"的证据。`release_records` 分支与历史 19 项逐项名单仍未取得。正确复测口径：在系统临时目录按 `_verify_judge_archive` 同路径跑。 |
| R2 | 已完成 | `tmp/handover-ai2-20260903.md` Phase 1/3/6 三行加【复核修正】+ 新增 §5；`tmp/judge-failures-20260903.md` 加时点声明；`PROJECT_STATUS.json` 的 `plan_reference`/`environment_reproducible`/`failure_cause_status`/`root_cause_fixed`/`superseded_claim` 全部改写；`README_RUN.md` 顶部加 09-03 当前口径段并把 09-02 段标为历史溯源。 |
| R3 | 部分完成 | 新增 `test_gate_never_masks_a_real_defect`（marker 不得洗掉真失败）、`test_prepare_full_corpus_output_is_visible_to_the_gate`（台账标题↔闸门 glob 一致性）、`test_repo_itself_never_skips_full_corpus` 改为断言 marker 使用数为 0；`test_full_corpus_skip_gate.py` + `test_prepare_full_corpus.py` 共 7 项通过。遗留：`requires_full_corpus` 生产测试仍为 0 使用（已按“空集”如实登记，未选方案 b）。 |
| R4 | **生产模块已接入，待完整收口** | `backend/app/release_gate.py` 已接入 `main.py` 的 release snapshot；门禁契约与状态边界当前选择性测试 **21 项通过**。仍需把控制脚本接入同一判据，并在清洁快照中复验；当前 dirty、部署/证据 HEAD、新鲜 B3、签字和人工批准仍会阻断。 |
| R5 | 已完成并双向验证 | JUDGE_README 与 RUN_README 不再内联通过数；新增 `_assert_registered_matches_measured`，登记与实测不一致时拒绝出包；required 清单已补 ZIP 成员级检查。正式包仍需在清洁环境重建。 |
| R6 | 未完成 | ZIP 重命名与 `交付包/` 重建需要确认可覆盖/换名（现 `交付包/` 内仍是 359 项的旧包，与仓库已脱钩）；`tmp/judge-extract`、`tmp/judge-inside` 内的年报全文副本与 `.pytest_tmp2/` 未清理。 |

### 本轮新增的两个待查缺陷

1. **间歇性失败**：同一条全量命令一次出现 `1 failed`（`test_competition_demo_plan.py:798`，
   `CNINFO_601988_T0_20260330` 返回 `sensitive_data_blocked`），另两次全量与全部隔离复跑均绿。
   详见 `tmp/incident-20260903-间歇性隐私闸门阻断.md`，已把保留意见写进登记，未当作噪声。
2. **我的调用方式缺陷（撤回“应用劫持 stdout”的怀疑）**：把包解在仓库 `tmp/` 下手工跑 pytest 时得到“退出码 0 且零产物”（0 字节输出、不生成 junit，换相对路径、跑单文件都一样），而官方 `_verify_judge_archive` 与 AI-2 新建 venv 的复验都能正常打印完整摘要。结论：归因于我的解包位置/调用方式，不是应用缺陷；同时规定——**在这棵树下手工跑的包内测试结果一律不得用作证据**。

### 当前数字（均带留存日志）

- 源码仓库：373 passed（我独立复跑 163.46s，日志 `tmp/ai1-review-repo-pytest.txt`；含 AI-1 前端改动之后）。此前记录：368（整改前）→ 372（我改完后 run2）。
- 评委包：346 passed, 19 skipped, 0 failed；19 = 18 项 `requires_full_corpus`（`-m` 实测）+ 1 项队长签字原件。ZIP `04_..._2026-09-03.zip` SHA-256 我已独立计算核对为 `E39F7EF6…ACA47`，且包内 `index.html`/`demo-app.js`/`styles.css` 与磁盘字节一致。
- 中文注释：2416/23991 = 10.07%（新增 `release_gate.py`、`corpus.py` 后由 AI-2 复测；我此前登记的 10.11% 已过期）。
- 间歇性隐私阻断：已出现**两起**观察（`CNINFO_601988_T0_20260330` 与 `CNINFO_600000_T0_20260330`），单测与全新解包全量均通过，根因未定位；两份记录分别在 `tmp/incident-20260903-间歇性隐私闸门阻断.md` 与 `tmp/incident-20260903-评委包第二次间歇性隐私阻断.md`。不得宣称稳定全绿。
