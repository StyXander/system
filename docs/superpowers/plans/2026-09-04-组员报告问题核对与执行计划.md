# 组员审查报告问题核对与执行计划

日期：2026-09-04  
依据：`D:\WeChat\xwechat_files\wxid_kfjreunlf5522_3e35\msg\file\2026-09\审计系统优化审查报告.docx`、当前工作树、当前浏览器验收证据和 `backend/release_records/`。

## 1. 先给结论

组员报告有价值，但不能整份照抄。它把上一版网页、上一版方案书和当前候选版本混在了一起。当前核对结果分为四类：

1. **已被当前版本处理或属于旧事实**：网页硬编码“真实成功率 100%”、方案书“五案 5/5、16 次调用”、缺少证据挑战动作等。当前网页已经改为动态读取 15 案、45 份报告和实际样本状态；当前 V4 Markdown 已记录本机 6/6 条完整链、19 次调用，并保留生产通道未测量的限制。
2. **建议成立，直接落地**：把创新点写成可演示的 Standard Share 例子；把视频开头换成真实审计问题；在导出表格中补充计算过程；在方案书中加入基于当前证据的协作记录。
3. **建议成立，但要按当前证据改写**：报告给出的 Standard Share “应收账款账面余额 -14.27%、增速差 +10.18 个百分点”没有在当前运行证据中得到支持。当前运行使用的是报表列示净额，R1 应收增速为 -37.64%，营业收入增速为 -24.45%，增速差为 -13.20 个百分点，应收绝对变动为 -106,478,625.85 元；R2 因经营现金流跨期变号而标记为不可比。后续示例必须明确口径和证据编号。
4. **暂不直接实现**：网页“账面价值 / 账面余额”切换需要一组已核验的账面余额字段。当前案例只有净额/报表列示额，直接加开关会制造可用性假象；先保留为人工确认项。

## 2. 报告逐项核对矩阵

| 报告建议 | 当前核对 | 决策 | 执行动作 |
| --- | --- | --- | --- |
| 统一网页与方案书的 100%/6/6、5/5/16 | 网页当前为动态样本状态；V4 Markdown 已是 6/6、19 次。教师终稿仍有 329 passed、五案 5/5、16 次旧快照。 | 旧事实冲突，需修正式文档 | 不改网页显示逻辑；从教师终稿复制出修订版，更新可验证事实并保留生产门禁。
| 压缩 singleflight、FAISS、provider call、token、RLS、Supabase 等工程词 | 方向成立。网页和 V4 已先讲审计边界，但教师终稿仍可增加读者口径说明。 | 低风险编辑 | 在 V4 增加“评委阅读提示”，技术名词第一次出现时解释其审计用途；不删除能证明边界的技术证据。
| 给四个创新点各加 Standard Share 例子 | 当前 R1/R2、证据适配度、数字回查和反确认记录都已有真实输出。 | 直接落地，但不用未经核验的账面余额数字 | 在 V4 增加一页“标准股份端到端示例”，使用当前运行值和净额限制；把报告中的 +10.18 个百分点作为待核验备选，不写入现状。
| 效果评估不要只写 pending | 当前源码回归为 373 passed、1 warning；本机 DeepSeek 窗口为 6/6、19 次调用；正式评分、生产 provider probe、真实 B3 仍未完成。 | 直接落地且必须保留边界 | 更新教师终稿事实段，加入“可支持审计计划阶段初步筛查，正式效果验证待扩大样本”的限定句。
| 团队分工增加实际记录 | 角色和交付物已有，但教师终稿记录不够具体；真实成员姓名仍未提供。 | 直接落地为角色记录，不编造姓名 | 在 V4 和修订版方案书加入角色—证据—交付物记录，姓名继续使用 `待填写`。
| 视频开头从“02:13 熬夜”改为具体审计问题 | 当前脚本仍保留 02:13 深夜办公室，确实偏常见。 | 直接落地 | 改成标准股份 R1 的收入、应收口径和证据回查问题，AI 情境画面只做隐喻。
| 30 秒证据挑战必须显示点击回原文 | 脚本已有点击 evidence ID、页码和同一 run_id，但没有把 3–5 秒动作单独卡住。 | 小幅补强 | 增加明确时间片，要求录制时实际点击证据卡、原文页、返回结果。
| 四位成员说真实个人问题和解决办法 | 建议成立，但当前没有成员个人经历材料。 | 不能编造 | 改为基于已记录角色的具体工作句；保留“姓名及第一人称由本人确认”的录制门槛。
| 3:58–4:23 展示 RAG 不可用或 Agent 跳过 | 当前脚本已有失败边界和历史失败标识要求，但没有固定画面门槛。 | 直接写入录制验收项 | 增加真实失败/降级画面要求；没有新鲜失败运行时只用明确标注的历史证据，不拼接当前 run_id。
| 增加标准股份完整 walkthrough | 当前网页已有同一运行、R1/R2、证据适配度、数字回查和反确认区，但材料没有把它们串成一页。 | 直接落地 | 写入 V4 的演示页和视频分镜备注，数值以当前浏览器 JSON 为准。
| 增加“账面价值 / 账面余额”切换 | 当前案例应收字段为 `field_basis=net`，后端明确提示未达到账面余额目标口径。 | 暂缓 | 记录为待人工确认的产品需求；在拥有可核验 gross 字段和测试用例前不改 UI。
| PDF/CSV 增加计算过程 | JSON 已有 numeric claim trace 的计算映射；CSV 指标行只有原始值和存储口径。 | 直接落地 | 在指标行增加“计算过程”字段，输入值从同一运行 field_evidence 读取；无输入时显示缺口，不补数。同步现场样例 CSV。
| 视频增加手工 Excel 复算镜头 | 适合作为证据镜头，但目前没有新的录制文件。 | 写入脚本验收项 | 先加入录制清单，不伪称已完成；录制时用同一 run_id 的 R1 输入值复算并核对。
| 增加 Excel 对比表 | 报告表格本身有用，V4 尚缺一个面向评委的简表。 | 直接落地 | 在 V4 增加“手工 Excel 与 AuditTrace”对比，避免宣传式绝对化表述。

## 3. 执行范围与文件归属

### 3.1 方案书材料

- 保留老师原稿 `02_最终确定方案/23_审迹智链_AuditTrace_初赛项目方案书_老师终稿最终版_2026-09-01.docx` 不变。
- 新建一份修订副本，更新旧的 329/5/5/16 快照、当前模型窗口、效果评估边界和角色协作记录；不填写未知队员姓名，不改变老师终稿的章节结构和表格布局。
- 更新当前 V4 Markdown 的创新点、Standard Share 示例、评委阅读提示和 Excel 对比表。所有公式若出现，使用 `$...$` 或 `$$...$$` 定界。

### 3.2 网页与结构化导出

- 保留首页动态事实和真实成功率计算逻辑，不把样本窗口改成稳定成功率。
- 在 `assets/official-v4/demo-app.js` 中为结构化指标和现场样例 CSV 增加计算过程列。过程只使用运行对象中的字段证据和指标，不覆盖或重算后端结果。
- JSON 增加可读的计算过程映射；原有 numeric claim trace、失败关闭、AI 声明和生产门禁字段保持不变。
- 暂不增加口径切换控件，避免净额数据被误标为账面余额。

### 3.3 视频脚本

- 修改 `02_最终确定方案/21_审迹智链_五分钟演示视频脚本_V2_证据追踪叙事_2026-08-30.md` 的冷开场、证据挑战时间片和四人台词。
- 将失败画面、手工 Excel 复算和真实成员姓名确认写成录制验收条件，不把计划镜头写成已录制事实。

## 4. 分阶段执行步骤

1. **事实冻结**：读取当前 `PROJECT_STATUS.json/.md`、`current_release.json`、`current_evaluation.json`、V4 Markdown、教师终稿和最新浏览器 JSON，建立本计划中的数字基线。
2. **材料修改**：先改 V4 Markdown 和视频脚本，再从教师终稿生成修订副本。修改后逐段检查数字、模型状态、口径限制和统一 AI 声明。
3. **导出修改**：增加计算过程字段，补齐前端结构化表的列标题和现场样例 CSV，保持失败、取消、中断任务不可导出的边界。
4. **单元与静态验收**：运行 `node --check assets/official-v4/demo-app.js`、后端全量 pytest、中文说明比例检查和 `git diff --check`；新增导出字段要有最小回归断言。
5. **文档渲染验收**：按 documents skill 使用 `render_docx.py` 渲染修订版 DOCX，逐页检查标题、表格、页码、公式/声明和占位字段；原老师终稿不覆盖。
6. **浏览器验收**：启动本地服务，用同一运行检查四个视口、结构化表格、CSV 下载字段、证据抽屉、降级状态、键盘焦点、axe 和无横向溢出；若代码或网页发生变化，重新生成安全报告和评委包。
7. **交付同步**：重新构建评委复现包，扫描路径、密钥、年报全文边界并计算 SHA-256；更新三 AI 整合记录，只引用最新包和最新报告。

## 5. 验收标准

- 方案书中的当前窗口只写 `373 passed、1 warning` 和本机 `6/6、19 次调用`，并明确样本小、生产 provider probe/真实 B3/人工评分/最终批准仍待完成。
- Standard Share 示例明确：R1 当前运行使用净额/报表列示额，R2 因经营现金流跨期变号不可比；任何账面余额示例必须有独立字段证据后才能启用。
- CSV 指标行存在 `calculation_process` 列；收入、应收、增速差、绝对变动、现金流比率等指标能显示公式和实际输入字段证据，缺失输入时显示“输入值见字段证据/待补充”。
- 现场样例 CSV 与主运行 CSV 使用同一列语义；JSON、CSV、打印入口继续共享同一 `run_id` 和 AI 声明。
- 新 DOCX 保留老师终稿章节、表格和 `待填写` 字段；渲染无截断、重叠或错位。
- 浏览器四视口和主要状态的 axe、焦点、溢出、证据挑战及失败关闭验收通过；静态注入工具与应用 CSP 的冲突不得通过放宽 CSP 解决。
- 评委复现包与验收日志、扫描报告中的 SHA-256 一致；生产 readiness 仍按发布记录显示为未完成，不因本地测试通过而提前宣称正式发布。

## 6. 明确不做的事

- 不把组员报告里的 `-14.27%`、`+10.18 个百分点`当作当前运行事实。
- 不把确定性备用、缓存、历史窗口或本机成功率写成 Render 生产模型成功。
- 不编造队员姓名、个人经历、签名、老师意见或新的真实模型调用。
- 不为了让报告“看起来完成”而修改 `competition_release_ready=false`、人工评分 pending 或真实失败记录。
- 不覆盖老师终稿，不删除旧证据，不把计划中的视频镜头写成已完成录制。

## 7. 当前状态

本计划已完成本轮可自动执行范围。执行记录如下：

- 方案书、视频脚本、网页结构化表格/JSON/CSV 与教师终稿修订副本已落地；老师原稿未覆盖。
- `node --check assets/official-v4/demo-app.js` 通过；源码全量 `373 passed, 1 warning`。
- 真实浏览器导出验收通过：标准案例 15 行指标、现场样例 12 行指标，JSON/CSV/PDF 均可解析且含 `calculation_process` 与统一 AI 声明；证据见 `artifacts/final-integration-20260904/structured-output-after-report-v2/structured-output-audit.json`。
- 评委复现包已按新前端构建到 `交付包/04_审迹智链_评委代码复现包_2026-09-04.zip`，SHA-256 为 `AAB063217BC5E650E950C8F69F4FBD0E3C440DF250D0AC12B2E1ED459FE37446`，129 个成员、0 阻断项；包内独立复验 `346 passed, 19 skipped, 0 failed, 1 warning`。
- AI-3 安全与浏览器复验通过，报告为 `artifacts/final-integration-20260904/ai3-security-browser-v6/ai3-security-browser-report.json`；四视口 axe 均为 0 violations，公开/内部状态、CORS、路径穿越、密钥扫描和包扫描均通过。

仍保留为人工门槛的事项：账面余额字段及口径切换、真实成员姓名和第一人称录制、生产 provider probe、真实 B3、B0—B3 人工评分、R1 重新签字、最终发布批准。任何无法由当前证据支持的内容继续标记为待人工确认项。
