# 数据链路缺陷修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:executing-plans 逐任务实施。步骤用 `- [ ]` 跟踪。

**Goal:** 关闭审查报告 `docs/2026-09-09_代码与运行逻辑审查及开源对照优化方案.md` 中已实测复现的五处数据链路缺陷（C01—C05）与两处结构风险（4.1、4.2），并把它们固化为回归测试。

**Architecture:** 数据取值以"表头位置建立列身份"取代金额大小启发式；知识检索先做相关性门禁再接权威等级；网络层统一为受控流式下载器与按故障分类的重试。保留既有确定性事实层、案例隔离、来源哈希与人工确认边界，不引入新框架。

**Tech Stack:** Python 3.12 / FastAPI / httpx / PyMuPDF / pytest。

**本计划不执行 git commit 与 push**（AGENTS.md §6：未经用户明确要求不提交、不推送）。全部任务完成后由队长决定是否入库。

**统一复验命令：**

```bash
backend\.venv\Scripts\python.exe -m pytest backend/tests -q
backend\.venv\Scripts\python.exe scripts/check_chinese_comments.py
```

**顺序依据：** C01、C02 直接决定送给模型的数值与证据是否正确；C05 最小且不依赖其他改动；C03 影响公告筛选，放后面以免与 C01/C02 的测试改动混在一起判断；4.1/4.2 是同一下载链；C04 属于未接线基础设施，最后单独处理。

---

## 前置事实（已实测，不要重新推断）

| 编号 | 位置 | 实测行为 |
| --- | --- | --- |
| C01 | `backend/app/field_extraction.py:151-154`、`backend/app/cases.py:1829` | 本期 80／上期 120 → 采用 120；`calculation_ready_financial_rows` 返回 120.0，`candidate_quality_status=passed_technical_candidate`，R1 必需字段正是 `accounts_receivable`（`cases.py:1940`） |
| C02 | `backend/app/knowledge_rag.py:47-56,92-106`、`backend/app/main.py:3305` | 线上 `question_id` 是拼出来的 `KB-R1`；真实 13 条 manifest 下返回 8 条命中，**词命中全部为 0** |
| C03 | `backend/app/cninfo.py:541-547` | `sum((name_hit, code_hit, year_hit, annual_hit)) >= 3` 即通过，`year_hit=false` 仍返回 `passed` |
| C04 | `backend/app/knowledge_ingest.py:44-95` | `final_url` 未复校；期望 PDF 时只看 Content-Type；`MIN_DELAY_SECONDS` 定义后全仓零使用；三个导出函数无任何调用方 |
| C05 | `backend/app/cninfo.py:300-309` | 只重试 `{403,429}`；503 直接 `return response`；502/504 同样不重试；403 耗尽后抛 `CNINFO_RATE_LIMITED` |
| 4.1 | `backend/app/cninfo.py:482-491` | 非流式取 `response.content` 后才判 `MAX_DOWNLOAD_BYTES`（100MiB） |
| 4.2 | `backend/app/cninfo.py:385-393` | 撞满 `MAX_ANNOUNCEMENT_PAGES=10` 且 `hasMore=true` 时静默返回部分结果 |

**两个必须避开的连带陷阱：**

1. 知识检索返回空会走 `main.py:3631-3640` → `rag_error` → 完整分析失败关闭、不调用模型。因此 C02 的"零命中剔除"必须与受控问题词表同时落地，否则每次真实演示都失败关闭。
2. `backend/tests/test_competition_demo_plan.py:389` 断言 `_line_candidates(["应收账款","4","8,110,758,258.05",...], 0)[0][0] == 8_110_758_258.05`。旧的小整数让位排序不能被删除，只能降级为"仅影响候选可见性"，采用值改由列身份决定。

---

## Task 1: 字段列身份判定（C01，P1）

**Files:**
- Modify: `backend/app/field_extraction.py:117-210`
- Modify: `backend/app/field_extraction.py:300-320`（写入行时带上列身份字段）
- Modify: `backend/app/cases.py:1795-1834`
- Create: `backend/tests/test_field_column_identity.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_field_column_identity.py`：

```python
"""字段候选必须按表头位置确立列身份，禁止用金额大小代替本期/上期判定。"""
from __future__ import annotations

from backend.app.cases import (
    annotate_financial_field_quality,
    calculation_ready_financial_rows,
)
from backend.app.field_extraction import (
    FIELD_CONFIG,
    _find_page_candidate,
    _line_candidates,
    _scan_number_cells,
)

CONFIG = FIELD_CONFIG["accounts_receivable"]


def _row(candidate: dict, *, year: int = 2024) -> dict:
    return {
        "evidence_id": f"FIX_AR_{year}",
        "field_kind": "accounts_receivable",
        "year": year,
        "value": candidate["value"],
        "unit": candidate["unit"] or "元",
        "source_unit": candidate.get("source_unit") or candidate.get("unit") or "元",
        "field_basis": CONFIG["basis"],
        "document_id": "FIX-DOC",
        "pdf_page": candidate["page"],
        "locator": candidate["locator"],
        "raw_excerpt": candidate["raw_excerpt"],
        "column_identity": candidate["column_identity"],
        "extraction_method": "pdf_text_heuristic_candidate",
        "source_review_status": "auto_extracted_pending_human_page_confirmation",
    }


def test_small_current_period_is_not_demoted_by_magnitude() -> None:
    """本期 80、上期 120 时必须取 80；旧缺陷会为了排除附注号把 120 提到首位。"""

    candidate = _find_page_candidate(
        ["合并资产负债表\n单位：元\n项目 2024年末 2023年末\n应收账款\n80\n120\n"], CONFIG
    )
    assert candidate["raw_value"] == 80.0
    assert candidate["column_identity"] == "resolved_by_header_position"


def test_note_column_is_dropped_by_header_position_not_by_size() -> None:
    """中国海油式“附注列 + 两期”仍取本期，且依据是列位置。"""

    candidate = _find_page_candidate(
        [
            "2025年12月31日\n合并资产负债表\n人民币百万元\n项目\n附注\n"
            "2025年12月31日\n2024年12月31日\n应收账款\n（六）4\n32,415\n32,918\n"
        ],
        CONFIG,
    )
    assert candidate["raw_value"] == 32_415.0
    assert candidate["column_identity"] == "resolved_by_header_position"


def test_bare_note_number_without_header_is_ambiguous_and_blocked() -> None:
    """表头没有期间列时不得自动采用；旧可见性排序保留，但行必须被闸门阻断。"""

    pages = ["合并资产负债表\n单位：元\n应收账款\n4\n8,110,758,258.05\n7,293,628,386.69\n"]
    assert _line_candidates(pages[0].splitlines(), 2, term="应收账款")[0][0] == 8_110_758_258.05
    candidate = _find_page_candidate(pages, CONFIG)
    assert candidate["column_identity"].startswith("unresolved_")
    annotated = annotate_financial_field_quality(_row(candidate))
    assert annotated["candidate_quality_status"] == "blocked_pending_human_confirmation"
    assert calculation_ready_financial_rows([_row(candidate)]) == []


def test_legit_small_amount_in_wan_unit_is_not_flagged_as_note() -> None:
    """单位万元下的合法小金额，在列身份已确认时不能被“≤100 疑似附注号”误杀。"""

    candidate = _find_page_candidate(
        ["合并资产负债表\n单位：万元\n项目 2024年12月31日 2023年12月31日\n"
         "应收账款 80 120\n"], CONFIG
    )
    assert candidate["raw_value"] == 80.0
    annotated = annotate_financial_field_quality(_row(candidate))
    assert annotated["candidate_quality_issues"] == []


def test_reversed_compare_columns_still_take_header_first_period() -> None:
    """比较列倒序（先列上年）时，取表头顺序的第一列并保留期标签供人工复核。"""

    candidate = _find_page_candidate(
        ["单位：元\n项目 2023年12月31日 2024年12月31日\n应收账款 120 80\n"], CONFIG
    )
    assert candidate["raw_value"] == 120.0
    assert candidate["period_labels"][0].startswith("2023")
    assert candidate["column_identity"] == "resolved_by_header_position"


def test_scan_number_cells_keeps_document_order() -> None:
    """位置解析依赖文档顺序，扫描函数不得重排。"""

    lines = ["应收账款", "80", "120"]
    assert [value for value, _ in _scan_number_cells(lines, 0, term="应收账款")] == [80.0, 120.0]
```

- [ ] **Step 2: 运行确认失败**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_field_column_identity.py -q`
Expected: FAIL —`ImportError: cannot import name '_scan_number_cells'`

- [ ] **Step 3: 拆分扫描与排序，新增列身份判定**

在 `field_extraction.py` 把现有 `_line_candidates` 的扫描主体改名为 `_scan_number_cells`（去掉末尾的大小重排，其余逐字保留），并新增：

```python
# 列身份只能来自表头期间列与单元格位置；金额大小不参与列选择。
NOTE_COLUMN_LABELS = ("附注", "注释", "附注号")
PERIOD_LABEL_PATTERN = re.compile(
    r"\d{4}\s*年(?:\s*12\s*月\s*31\s*日|末|度|底)?|本期|上期|期末|期初|本年|上年"
)


def _line_candidates(
    lines: list[str], start: int, *, term: str = "", allow_percent: bool = False
) -> list[tuple[float, str]]:
    """旧入口：在文档顺序之上保留“小整数让位给明显金额”的可见性排序。

    该排序不再决定自动采用值；采用值由 _resolve_column 的列身份判定。
    """

    scanned = _scan_number_cells(lines, start, term=term, allow_percent=allow_percent)
    if not allow_percent and scanned and abs(scanned[0][0]) <= 100:
        material = [item for item in scanned if abs(item[0]) > 100]
        if material:
            return material + [item for item in scanned if abs(item[0]) <= 100]
    return scanned


def _header_layout(lines: list[str], start: int) -> dict[str, Any]:
    """回看有限表头，得到期间列数量、顺序与附注列是否在期间列之前。"""

    # 只回看四行，避免把上一张表的列头接到本字段行上。
    header_text = " ".join(lines[max(0, start - 4) : start + 1])
    labels = [match.group(0) for match in PERIOD_LABEL_PATTERN.finditer(header_text)]
    note_position = min((header_text.find(label) for label in NOTE_COLUMN_LABELS if label in header_text), default=-1)
    first_period = PERIOD_LABEL_PATTERN.search(header_text)
    return {
        "period_count": len(labels),
        "period_labels": labels,
        "note_first": bool(labels) and note_position >= 0 and note_position < first_period.start(),
    }


def _resolve_column(scanned: list[tuple[float, str]], layout: dict[str, Any]) -> tuple[int | None, str]:
    """按表头位置返回本期单元格下标；无法确立时给出可诊断的歧义原因。"""

    if not scanned:
        return None, "unresolved_no_cell"
    if not layout["period_count"]:
        return None, "unresolved_no_period_header"
    # 附注列在期间列之前且多出一个单元格时，按位置丢掉附注单元格，不按数值丢掉。
    offset = 1 if layout["note_first"] and len(scanned) > layout["period_count"] else 0
    if len(scanned) - offset < layout["period_count"]:
        return None, "unresolved_cell_count_below_period_columns"
    return offset, "resolved_by_header_position"
```

- [ ] **Step 4: 采用值改为列身份驱动**

在 `_find_page_candidate` 里，把 `candidates = _line_candidates(...)` 起的片段改为：

```python
                scanned = _scan_number_cells(lines, line_index, term=term, allow_percent=is_ratio)
                if not scanned:
                    continue
                cell_index, column_identity = _resolve_column(scanned, _header_layout(lines, line_index))
                if cell_index is None:
                    # 列身份未确立时沿用可见性排序，只保留候选可读性；
                    # 该值会被质量闸门阻断，不进入确定性计算。
                    value, raw_line = _reorder_for_visibility(scanned)[0]
                else:
                    value, raw_line = scanned[cell_index]
```

并把 `_line_candidates` 的重排主体提取为 `_reorder_for_visibility(scanned)` 供上面复用（`_line_candidates` 改为调用它，行为不变）。在候选字典里追加：

```python
                            "column_identity": column_identity,
                            "period_labels": layout["period_labels"],
                            "cell_values": [item[0] for item in scanned],
                            "adopted_cell_index": cell_index,
```

排序分里追加 `if column_identity.startswith("resolved"): score += 8`，使已确立列身份的页面优先于歧义页面。

- [ ] **Step 5: 把列身份写进行记录**

`extract_cninfo_fields` 的 `annotate_financial_field_quality({...})` 字典内，在 `"extraction_method"` 之前追加：

```python
                    "column_identity": candidate["column_identity"],
                    "period_labels": candidate["period_labels"],
                    "cell_values": candidate["cell_values"],
                    "adopted_cell_index": candidate["adopted_cell_index"],
```

- [ ] **Step 6: 质量闸门改为列身份驱动**

`cases.py:1795` 的 `financial_field_candidate_quality_issues` 内，在 `is_ratio` 分支之前插入歧义判定，并把 ≤100 分支限定为"列身份未确立时"才触发：

```python
    column_identity = str(row.get("column_identity") or "")
    identity_resolved = column_identity.startswith("resolved")
    if is_automatic_candidate and column_identity and not identity_resolved:
        issues.append(
            f"自动候选未确立本期列身份（{column_identity[len('unresolved_'):]}），"
            "表头期间列或附注列不可确认，须人工回页指定本期列。"
        )
```

```python
    elif source_value is not None and abs(source_value) <= 100 and not identity_resolved:
```

`ratio` 分支内的 `suspicious_ratio_context` 检查保持不变。

- [ ] **Step 7: 运行新测试与受影响既有测试**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_field_column_identity.py backend/tests/test_cninfo_pipeline.py backend/tests/test_batch6_industry_rules.py backend/tests/test_competition_demo_plan.py -q`
Expected: PASS（`test_cninfo_pipeline.py` 的 32,415 断言与 `test_competition_demo_plan.py` 的 8.11e9 断言都必须继续为真）

- [ ] **Step 8: 复跑原始反例**

Run: `backend\.venv\Scripts\python.exe docs/review-assets/20260909_logic_probes.py`
Expected: `small_current_amount` 的 `actual_raw_value` 变为 `80.0`（其余四项此轮不变）

---

## Task 2: 知识检索相关性门禁（C02，P1）

**Files:**
- Modify: `backend/app/knowledge_rag.py:47-56,75-144`
- Modify: `backend/app/main.py:3281-3323,3614-3642`
- Modify: `backend/tests/test_knowledge_sources.py`、`backend/tests/test_knowledge_sources_manifest.py`（补断言）
- Create: `backend/tests/test_knowledge_relevance_gate.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_knowledge_relevance_gate.py`：

```python
"""相关性先于权威等级：无关条目不得作为知识证据，零命中必须显式拒答。"""
from __future__ import annotations

from pathlib import Path

from backend.app.knowledge_rag import (
    build_retrieval_request,
    controlled_question_terms,
    retrieve_knowledge,
)
from backend.app.knowledge_sources import load_source_manifest

MANIFEST = Path(__file__).parents[1] / "knowledge_sources.manifest.json"

ENTRY = {
    "source_id": "FIXTURE-KB",
    "source_category": "auditing_standard",
    "title": "审计抽样程序",
    "publisher": "中国注册会计师协会",
    "official_url": "https://www.cicpa.org.cn/sample",
    "published_at": "2024-01-01",
    "document_id": "DOC-SAMPLE",
    "sha256": "a" * 64,
    "validation_status": "passed",
    "retrieval_excerpt": "抽样程序须记录总体和样本。",
    "retrieval_locator": "第 5 页",
    "excerpt_sha256": "b" * 64,
    "query_terms": ["审计抽样"],
}


def _request(**overrides):
    payload = {
        "case_id": "FIXTURE",
        "question_id": "KB-R1",
        "source_categories": ["auditing_standard"],
        "as_of_date": "2026-08-24",
        "cutoff_date": "2026-08-24",
        "snapshot_id": "SNAP-1",
    }
    payload.update(overrides)
    return build_retrieval_request(**payload)


def test_irrelevant_question_returns_nothing_instead_of_one_authoritative_entry() -> None:
    hits = retrieve_knowledge([ENTRY], _request(query_text="火星土壤的化学组成"))
    assert hits == []


def test_relevant_question_text_gates_in_the_registered_entry() -> None:
    hits = retrieve_knowledge([ENTRY], _request(query_text="应收账款审计程序 抽样"))
    assert [hit["source_id"] for hit in hits] == ["FIXTURE-KB"]
    assert hits[0]["relevance_matches"] >= 1


def test_rule_question_id_maps_to_registered_controlled_terms() -> None:
    terms = controlled_question_terms("KB-R1")
    assert "应收账款" in terms or "营业收入" in terms
    assert "kb" not in terms


def test_zero_limit_returns_no_hits() -> None:
    assert retrieve_knowledge([ENTRY], _request(query_text="审计抽样"), limit=0) == []


def test_live_manifest_returns_only_relevant_hits() -> None:
    entries, error = load_source_manifest(MANIFEST)
    assert error is None
    hits = retrieve_knowledge(
        entries,
        _request(
            query_text="营业收入 应收账款 增长 背离 审计程序",
            source_categories=[
                "annual_report", "csrc_penalty", "exchange_inquiry", "accounting_standard",
                "auditing_standard", "tax_regulation", "industry_report", "news", "macro_indicator",
            ],
            ticker="600302",
            industry="制造业",
        ),
        limit=8,
    )
    assert hits, "R1 固定问题必须能在真实清单上检索到相关条目"
    assert all(hit["relevance_matches"] >= 1 for hit in hits)
```

- [ ] **Step 2: 运行确认失败**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_knowledge_relevance_gate.py -q`
Expected: FAIL —`ImportError: cannot import name 'controlled_question_terms'`

- [ ] **Step 3: 建受控问题词表与 token 来源**

`knowledge_rag.py` 顶部新增常量与函数（`rag.py` 已登记固定问题词表，直接复用，不再造第二套问题定义）：

```python
KNOWLEDGE_RETRIEVAL_VERSION = "knowledge_retrieval_relevance_gate_v2"


def controlled_question_terms(question_id: str) -> set[str]:
    """把 KB-R1、RAG-Q1 等固定编号映射到已登记的中文问题词，不做外网搜索。"""

    # 延迟导入避免 knowledge_rag 与 rag 之间的循环依赖。
    from .rag import RAG_QUESTIONS

    wanted = {token.upper() for token in str(question_id or "").replace("KB-", "").split("-") if token}
    terms: list[str] = []
    for item in RAG_QUESTIONS:
        matched = str(item.get("question_id") or "").upper() in wanted or bool(
            wanted & {str(rule).upper() for rule in item.get("rule_ids") or []}
        )
        if matched:
            terms.extend([str(item.get("retrieval_query") or ""), str(item.get("title") or "")])
            terms.extend(str(term) for term in item.get("anchor_terms") or [])
    return {
        token.lower()
        for text in terms
        for token in re.findall(r"[A-Za-z0-9_]{2,}|[一-鿿]{2,}", text)
    }


def _searchable_text(entry: dict[str, Any]) -> str:
    """条目可匹配文本 = 标题 + 最小检索片段 + 登记的 query_terms。"""

    return " ".join(
        [
            str(entry.get("title") or ""),
            str(entry.get("retrieval_excerpt") or ""),
            " ".join(str(term) for term in (entry.get("query_terms") or [])),
        ]
    ).lower()
```

`_query_tokens` 改为以问题语义为主、标识为辅，并去掉只会污染匹配的 `case_id`：

```python
def _query_tokens(request: dict[str, Any]) -> set[str]:
    """相关性词来自问题文本与受控词表；公司代码与行业只作缩小范围的辅助词。"""

    raw = " ".join(
        [
            str(request.get("query_text") or ""),
            str(request.get("question_text") or ""),
            str(request.get("ticker") or ""),
            str(request.get("industry") or ""),
        ]
    )
    tokens = {
        token.lower()
        for token in re.findall(r"[A-Za-z0-9_]{2,}|[一-鿿]{2,}", raw)
    }
    tokens |= controlled_question_terms(str(request.get("question_id") or ""))
    return tokens
```

`build_retrieval_request` 增加 `query_text: str = ""` 参数并写入返回字典（`question_id` 仍作为编号保留，不再是词来源）。

- [ ] **Step 4: 排序改为相关性优先 + 零命中剔除**

`retrieve_knowledge` 中，把 `ranked = rank_candidates(...)` 到 `[: max(1, limit)]` 的循环改为：

```python
    ranked = rank_candidates(candidates, request)
    tokens = _query_tokens(request)
    if limit <= 0:
        return []

    scored: list[tuple[int, int, int, dict[str, Any]]] = []
    for ordinal, entry in enumerate(ranked):
        searchable = _searchable_text(entry)
        matches = sum(1 for token in tokens if token and token in searchable)
        if matches == 0:
            # 权威等级不能替代相关性：零词命中条目不得进入证据与上下文预算。
            continue
        layer = str(entry.get("layer") or LAYER_BY_CATEGORY.get(str(entry.get("source_category") or ""), "case_evidence"))
        scored.append((-matches, -AUTHORITY_RANK.get(layer, 0), ordinal, entry))
    scored.sort(key=lambda item: item[:3])

    hits: list[dict[str, Any]] = []
    for matches, _authority, ordinal, entry in scored[:limit]:
```

命中字典追加 `"relevance_matches": -matches`、`"retrieval_version": KNOWLEDGE_RETRIEVAL_VERSION`。`rank_candidates` 保持对外合同不变（仍按权威排序，供既有调用方使用）。

- [ ] **Step 5: 区分"无相关证据"与"知识库不可用"**

`main.py:3320` 把空结果码改名，避免与清单损坏混为一谈：

```python
        trace = retrieve_knowledge(entries, request, limit=8)
        if not trace:
            return [], summary, "knowledge_no_relevant_evidence"
```

`build_retrieval_request(...)` 调用处补 `query_text`：

```python
        query_text = " ".join(
            [
                str(context.get("question_text") or ""),
                " ".join(str(rule.rule_id) for rule in rule_results) or "R1",
            ]
        )
```

并把 `query_text=query_text` 传入（`question_id=f"KB-{'-'.join(active_rules)}` 保持，由 `controlled_question_terms` 展开成中文问题词）。

`main.py:3630-3641` 的失败关闭分支改为只对真实故障失败关闭：

```python
            if knowledge_error is not None:
                if knowledge_error == "knowledge_no_relevant_evidence":
                    # 无相关证据是合法结果：记为资料缺口继续，不拿无关权威条目充数。
                    evidence_gaps.append(
                        {
                            "type": "knowledge_no_relevant_evidence",
                            "message": "登记知识源中没有与本次固定问题相关且可回查的条目；本项按资料缺口保留，需人工补充权威依据。",
                            "detail": knowledge_error,
                        }
                    )
                    _progress("knowledge_retrieval", "not_applicable", "知识检索无相关命中；已按资料缺口继续。")
                else:
                    rag_error = knowledge_error
                    evidence_gaps.append(
                        {
                            "type": "knowledge_retrieval_unavailable",
                            "message": "知识库检索未完成；完整分析失败关闭，本次不调用外部模型。",
                            "detail": knowledge_error,
                        }
                    )
                    _progress("knowledge_retrieval", "failed", f"知识检索失败（{knowledge_error}）。")
```

- [ ] **Step 6: 更新既有知识测试**

`test_knowledge_sources.py:181` 的 `build_retrieval_request` 增加 `query_text="收入确认 实质性程序"`，使断言在相关性门禁下仍成立；`test_knowledge_sources_manifest.py:106` 的 `question_id="sales_receivable"` 同步补 `query_text="销售与收款 营业收入 应收账款"`。两处 `assert hits` 保持。

- [ ] **Step 7: 运行知识链与检索测试集**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_knowledge_relevance_gate.py backend/tests/test_knowledge_sources.py backend/tests/test_knowledge_sources_manifest.py -q`
Expected: PASS

Run: `backend\.venv\Scripts\python.exe scripts/run_knowledge_retrieval_test_set.py`
Expected: 脚本正常产出 `results.json`；拒答用例仍返回空，相关用例命中数不因门禁归零。逐项核对后再继续。

---

## Task 3: 巨潮请求按故障分类重试（C05，P2）

**Files:**
- Modify: `backend/app/cninfo.py:60-70,255-322`
- Create: `backend/tests/test_cninfo_retry_policy.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_cninfo_retry_policy.py`：

```python
"""重试按故障分类：5xx 瞬断可恢复，403 立即退出，429 尊重 Retry-After。"""
from __future__ import annotations

import httpx
import pytest

from backend.app.cninfo import CNInfoClient, CNInfoError

SLEEP_CALLS: list[float] = []


class Clock:
    """注入时钟：不真的等待，只记录计划等待时长。"""

    def __init__(self) -> None:
        self.now = 0.0
        SLEEP_CALLS.clear()

    def monotonic(self) -> float:
        return self.now

    def sleep(self, seconds: float) -> None:
        SLEEP_CALLS.append(seconds)
        self.now += seconds


def _client(handler, monkeypatch, **kwargs):
    monkeypatch.setattr("backend.app.cninfo.time.monotonic", Clock.monotonic)
    monkeypatch.setattr("backend.app.cninfo.time.sleep", Clock.sleep)
    return CNInfoClient(client=httpx.Client(transport=httpx.MockTransport(handler)), min_delay_seconds=0, **kwargs)


@pytest.mark.parametrize("status", [503, 502, 504, 408])
def test_transient_server_errors_are_retried(monkeypatch, status: int) -> None:
    calls: list[int] = []

    def handler(request: httpx.Request) -> httpx.Response:
        calls.append(status)
        return httpx.Response(200, json={"ok": True}) if len(calls) > 1 else httpx.Response(status)

    client = _client(handler, monkeypatch, max_retries=2)
    response = client._request("GET", "https://www.cninfo.com.cn/", source="离线夹具")
    assert response.status_code == 200
    assert len(calls) == 2
    assert client.retry_summary()["retried_requests"] == 1


def test_access_denied_stops_immediately(monkeypatch) -> None:
    calls: list[int] = []

    def handler(request: httpx.Request) -> httpx.Response:
        calls.append(1)
        return httpx.Response(403)

    client = _client(handler, monkeypatch, max_retries=3)
    with pytest.raises(CNInfoError) as error:
        client._request("GET", "https://www.cninfo.com.cn/", source="离线夹具")
    assert error.value.code == "CNINFO_ACCESS_DENIED"
    assert len(calls) == 1


def test_rate_limited_honours_retry_after(monkeypatch) -> None:
    calls: list[int] = []

    def handler(request: httpx.Request) -> httpx.Response:
        calls.append(1)
        return httpx.Response(429, headers={"Retry-After": "3"}) if len(calls) == 1 else httpx.Response(200, json={})

    client = _client(handler, monkeypatch, max_retries=2)
    assert client._request("GET", "https://www.cninfo.com.cn/", source="离线夹具").status_code == 200
    assert SLEEP_CALLS[0] == 3.0


def test_retry_deadline_stops_backoff(monkeypatch) -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(503)

    client = _client(handler, monkeypatch, max_retries=5, retry_deadline_seconds=4.0)
    assert client._request("GET", "https://www.cninfo.com.cn/", source="离线夹具").status_code == 503
    assert sum(1 for _ in SLEEP_CALLS) <= 5
    assert client.retry_summary()["stop_reason"] == "deadline_exceeded"
```

- [ ] **Step 2: 运行确认失败**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_cninfo_retry_policy.py -q`
Expected: FAIL —`TypeError: CNInfoClient.__init__() got an unexpected keyword argument 'retry_deadline_seconds'`

- [ ] **Step 3: 实现分类重试**

`cninfo.py` 常量区新增：

```python
# 只读查询与下载可对瞬断退避；403 属于访问限制，重复冲击没有意义。
RETRYABLE_STATUS_CODES = frozenset({408, 429, 502, 503, 504})
ACCESS_DENIED_STATUS_CODES = frozenset({403})
MAX_RETRY_SECONDS = 8.0
DEFAULT_RETRY_DEADLINE_SECONDS = 60.0
```

新增模块级确定性抖动与退避计算：

```python
def _retry_delay(response: httpx.Response | None, attempt: int, remaining: float) -> float | None:
    """429 优先读 Retry-After，其余用指数退避加确定性抖动；超预算返回 None。"""

    if remaining <= 0:
        return None
    delay: float | None = None
    if response is not None and response.status_code == 429:
        raw = str(response.headers.get("Retry-After") or "").strip()
        if raw.isdigit():
            delay = float(raw)
    if delay is None:
        # 抖动由来源与次数派生，保证同一批次可复现，不引入随机种子依赖。
        seed = hashlib.sha256(f"{attempt}".encode("utf-8")).hexdigest()[:4]
        delay = min(MAX_RETRY_SECONDS, 2.0 ** attempt) + (int(seed, 16) % 250) / 1000.0
    if delay > remaining:
        return None
    return delay
```

`CNInfoClient.__init__` 增加 `retry_deadline_seconds: float = DEFAULT_RETRY_DEADLINE_SECONDS`，保存 `self.retry_deadline_seconds`、`self._retry_journal: list[dict[str, Any]] = []`、`self._retry_stop_reason = "not_needed"`。

`_request` 主体替换为：

```python
    def _request(self, method: str, url: str, *, source: str, **kwargs: Any) -> httpx.Response:
        """统一执行有限重试：瞬断与 5xx 退避恢复，403 立即停止，429 尊重 Retry-After。"""

        if not _is_trusted_url(url):
            raise CNInfoError("CNINFO_URL_NOT_ALLOWED", f"拒绝访问非巨潮来源：{url}")
        # POST 仅在无副作用的公告查询上使用；模型请求不走这条链。
        deadline = time.monotonic() + self.retry_deadline_seconds
        last_error: Exception | None = None
        for attempt in range(self.max_retries + 1):
            try:
                self._wait_before_request()
                response = self.client.request(method, url, **kwargs)
                if response.status_code in ACCESS_DENIED_STATUS_CODES:
                    self._record_retry(attempt, response.status_code, 0.0, "access_denied")
                    self._retry_stop_reason = "access_denied"
                    raise CNInfoError(
                        "CNINFO_ACCESS_DENIED",
                        f"巨潮{source}访问被拒绝（HTTP {response.status_code}），已停止重试，需要人工检查来源与访问方式。",
                    )
                if response.status_code in RETRYABLE_STATUS_CODES:
                    remaining = deadline - time.monotonic()
                    delay = _retry_delay(response, attempt, remaining)
                    if delay is None:
                        self._record_retry(attempt, response.status_code, 0.0, "deadline_exceeded")
                        self._retry_stop_reason = "deadline_exceeded"
                        return response
                    if attempt >= self.max_retries:
                        self._record_retry(attempt, response.status_code, 0.0, "retries_exhausted")
                        self._retry_stop_reason = "retries_exhausted"
                        return response
                    self._record_retry(attempt, response.status_code, delay, "backoff")
                    self._retry_stop_reason = "backoff"
                    time.sleep(delay)
                    continue
                self._retry_stop_reason = "succeeded" if attempt else "not_needed"
                return response
            except CNInfoError:
                raise
            except (httpx.HTTPError, OSError) as error:
                last_error = error
                remaining = deadline - time.monotonic()
                delay = _retry_delay(None, attempt, remaining)
                if attempt >= self.max_retries or delay is None:
                    break
                self._record_retry(attempt, None, delay, "network_error")
                self._retry_stop_reason = "network_error"
                time.sleep(delay)
        detail = _network_error_detail(last_error, self.max_retries + 1)
        self._retry_stop_reason = "network_error"
        raise CNInfoError(
            "CNINFO_NETWORK_ERROR",
            f"巨潮{source}请求失败，已停止重试。{detail['suggestion']}",
            detail=detail,
        ) from last_error

    def _record_retry(self, attempt: int, status_code: int | None, delay: float, reason: str) -> None:
        """记录状态码、次数、实际等待与终止原因，供限流复盘与验收使用。"""

        self._retry_journal.append(
            {
                "attempt": attempt + 1,
                "status_code": status_code,
                "planned_wait_seconds": round(delay, 3),
                "reason": reason,
            }
        )

    def retry_summary(self) -> dict[str, Any]:
        """返回本客户端的重试台账摘要；不隐藏任何一次退避。"""

        return {
            "retried_requests": sum(1 for item in self._retry_journal if item["reason"] == "backoff"),
            "journal": list(self._retry_journal),
            "stop_reason": self._retry_stop_reason,
        }
```

- [ ] **Step 4: 运行重试与既有巨潮测试**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_cninfo_retry_policy.py backend/tests/test_cninfo_pipeline.py -q`
Expected: PASS（既有测试中若断言旧的"403 → CNINFO_RATE_LIMITED"，按新错误码 `CNINFO_ACCESS_DENIED` 更新并逐条确认原意）

---

## Task 4: PDF 文档身份硬校验（C03，P1）

**Files:**
- Modify: `backend/app/cninfo.py:499-564`
- Modify: `validate_pdf` 的调用方（实施时先 `grep -n "validate_pdf" backend/app/` 确认全部调用点）
- Create: `backend/tests/test_pdf_identity_validation.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_pdf_identity_validation.py`：

```python
"""企业身份、报告年度、年报类型是三个独立必要条件，不能互相补分。"""
from __future__ import annotations

import httpx
import pymupdf as fitz
import pytest

from backend.app.cninfo import CNInfoClient, CNInfoError


def _pdf(lines: list[str], pages: int = 12) -> bytes:
    doc = fitz.open()
    for index in range(pages):
        page = doc.new_page()
        text = lines[index] if index < len(lines) else lines[-1]
        page.insert_text((40, 60), text)
    pdf = doc.tobytes()
    doc.close()
    return pdf


def _validate(pdf: bytes, announcement: dict, company: dict) -> dict:
    with CNInfoClient(client=httpx.Client(transport=httpx.MockTransport(lambda request: httpx.Response(200)))) as client:
        return client.validate_pdf(pdf, announcement, company)


COMPANY = {"company_name": "Fixture Corporation", "company_alias": "", "ticker": "600302"}


def test_wrong_report_year_is_rejected_even_with_name_code_and_type() -> None:
    """旧缺陷：名称、代码、年报类型齐了就能放行错误年度。"""

    pdf = _pdf(["Fixture Corporation 600302 2022 annualreport"])
    with pytest.raises(CNInfoError) as error:
        _validate(
            pdf,
            {"report_year": 2024, "announcement_title": "2024 annualreport", "company_name": "Fixture Corporation"},
            COMPANY,
        )
    assert error.value.code == "PDF_REPORT_YEAR_UNCONFIRMED"


def test_year_only_in_comparison_column_is_not_auto_confirmed() -> None:
    """封面不含目标年度、只在比较列出现时，只能进入待核验。"""

    pdf = _pdf(["Fixture Corporation 600302 年度报告", "项目 2023年12月31日 2024年12月31日"])
    result = _validate(
        pdf,
        {"report_year": 2024, "announcement_title": "2024 年度报告", "company_name": "Fixture Corporation"},
        COMPANY,
    )
    assert result["validation_status"] == "identity_unresolved"
    assert result["content_checks"]["year_source"] == "comparison_column_only"


def test_cover_year_passes_and_records_evidence() -> None:
    pdf = _pdf(["Fixture Corporation 600302 2024年年度报告"])
    result = _validate(
        pdf,
        {"report_year": 2024, "announcement_title": "2024 年度报告", "company_name": "Fixture Corporation"},
        COMPANY,
    )
    assert result["validation_status"] == "passed"
    assert result["content_checks"]["year_source"] == "cover_text"


def test_missing_company_identity_is_rejected() -> None:
    pdf = _pdf(["Other Company 000001 2024年年度报告"])
    with pytest.raises(CNInfoError) as error:
        _validate(
            pdf,
            {"report_year": 2024, "announcement_title": "2024 年度报告", "company_name": "Other Company"},
            COMPANY,
        )
    assert error.value.code == "PDF_IDENTITY_MISMATCH"


def test_non_annual_document_is_rejected() -> None:
    pdf = _pdf(["Fixture Corporation 600302 2024年第一季度报告"])
    with pytest.raises(CNInfoError) as error:
        _validate(
            pdf,
            {"report_year": 2024, "announcement_title": "2024年第一季度报告", "company_name": "Fixture Corporation"},
            COMPANY,
        )
    assert error.value.code == "PDF_NOT_ANNUAL_REPORT"
```

- [ ] **Step 2: 运行确认失败**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_pdf_identity_validation.py -q`
Expected: FAIL —第一个用例返回 `passed` 而非抛 `PDF_REPORT_YEAR_UNCONFIRMED`

- [ ] **Step 3: 拆封面前 3 页与比较列证据**

`validate_pdf` 内把取样改为两段，并按三个独立条件判定：

```python
            metadata_text = "\n".join(str(value or "") for value in (document.metadata or {}).values())
            # 封面与报告期间只认前 3 页；正文比较列出现的年份不能当报告年度。
            cover_text = "\n".join(document[index].get_text("text") for index in range(min(3, page_count)))
            page_text = "\n".join(document[index].get_text("text") for index in range(min(12, page_count)))
            sample_text = f"{metadata_text}\n{cover_text}\n{page_text}"
```

```python
        compact_cover = re.sub(r"\s+", "", f"{metadata_text}\n{cover_text}").lower()
        report_year = str(announcement["report_year"])
        identity_hit = name_hit or code_hit
        year_cover_hit = f"{report_year}年" in compact_cover
        year_comparison_only_hit = (not year_cover_hit) and (report_year in compact)
        annual_hit = annual_content_hit or official_annual_title_hit
        if not identity_hit:
            raise CNInfoError(
                "PDF_IDENTITY_MISMATCH",
                "PDF 未能确认目标企业名称或证券代码。",
                detail={"name_hit": name_hit, "code_hit": code_hit, "year_hit": year_cover_hit, "annual_hit": annual_hit},
            )
        if not annual_hit:
            raise CNInfoError(
                "PDF_NOT_ANNUAL_REPORT",
                "PDF 内容与官方公告标题均未确认年度报告类型。",
                detail={"name_hit": name_hit, "code_hit": code_hit, "year_hit": year_cover_hit, "annual_hit": annual_hit},
            )
        year_source = "cover_text" if year_cover_hit else ("comparison_column_only" if year_comparison_only_hit else "absent")
```

`year_hit` 变量全部改用 `year_cover_hit`；`name_hit`、`code_hit` 基于 `compact`（12 页样本）保持不变，使身份仍可比对元数据。返回体：

```python
        return {
            "validation_status": "passed" if year_cover_hit else "identity_unresolved",
            ...
            "content_checks": {
                "name_hit": name_hit,
                "code_hit": code_hit,
                "year_hit": year_cover_hit,
                "year_source": year_source,
                "identity_hit": identity_hit,
                ...
            },
            "ocr_required": len(sample_text.strip()) < 200,
        }
```

删除 `score = sum(...)` 与 `if score < 3` 判定；错误文案不再声称"已同时确认三项"。

- [ ] **Step 4: 调用方处理 identity_unresolved**

在每个 `validate_pdf` 调用点，把非 `passed` 结果转成待人工核验状态并保留 `content_checks`，不得当成功继续：

```python
    validation = client.validate_pdf(content, announcement, company)
    if validation.get("validation_status") != "passed":
        # 年度仅在比较列或封面缺信息：进人工核验队列，不放行也不静默失败。
        raise CNInfoError(
            "PDF_IDENTITY_UNRESOLVED",
            "PDF 年度证据仅出现在比较列或封面缺失，需要人工确认文档身份。",
            detail={"content_checks": validation.get("content_checks"), "sha256": validation.get("sha256")},
        )
```

- [ ] **Step 5: 运行 PDF 与巨潮测试**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_pdf_identity_validation.py backend/tests/test_cninfo_pipeline.py -q`
Expected: PASS

---

## Task 5: 受控流式下载与分页截断显式化（4.1、4.2，P2）

**Files:**
- Create: `backend/app/secure_download.py`
- Modify: `backend/app/cninfo.py:361-400,477-497`
- Modify: `backend/app/source_cache.py:79-103`
- Create: `backend/tests/test_secure_download.py`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_secure_download.py`：

```python
"""下载限额必须在接收过程中生效，重定向每一跳都要复校。"""
from __future__ import annotations

from pathlib import Path

import httpx
import pytest

from backend.app.secure_download import SecureDownloadError, download_bounded

PDF_BYTES = b"%PDF-1.7\n" + b"x" * 4096


def _client(handler) -> httpx.Client:
    return httpx.Client(transport=httpx.MockTransport(handler))


def test_size_limit_stops_the_stream_early(tmp_path: Path) -> None:
    """旧缺陷是先收完整 body 再判体积；现在必须在块级别停止。"""

    blocks = 0

    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(200, content=iter([PDF_BYTES] * 64), headers={"Content-Type": "application/pdf"})

    with pytest.raises(SecureDownloadError) as error:
        download_bounded(
            "https://www.cninfo.com.cn/a.pdf",
            tmp_path / "a.pdf",
            client=_client(handler),
            max_bytes=8192,
            require_pdf=True,
            is_allowed_url=lambda url: True,
        )
    assert error.value.code == "TOO_LARGE"
    assert not (tmp_path / "a.pdf").exists()


def test_redirect_to_disallowed_host_is_rejected(tmp_path: Path) -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        if request.url.host == "www.cninfo.com.cn":
            return httpx.Response(302, headers={"Location": "https://evil.example/a.pdf"})
        return httpx.Response(200, content=PDF_BYTES)

    with pytest.raises(SecureDownloadError) as error:
        download_bounded(
            "https://www.cninfo.com.cn/a.pdf",
            tmp_path / "a.pdf",
            client=_client(handler),
            max_bytes=1024 * 1024,
            require_pdf=True,
            is_allowed_url=lambda url: "cninfo.com.cn" in str(url),
        )
    assert error.value.code == "REDIRECT_NOT_ALLOWED"


def test_fake_pdf_is_rejected_by_magic_bytes(tmp_path: Path) -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(200, content=b"<html>not a pdf</html>", headers={"Content-Type": "application/pdf"})

    with pytest.raises(SecureDownloadError) as error:
        download_bounded(
            "https://www.cninfo.com.cn/a.pdf",
            tmp_path / "a.pdf",
            client=_client(handler),
            max_bytes=1024 * 1024,
            require_pdf=True,
            is_allowed_url=lambda url: True,
        )
    assert error.value.code == "PDF_MAGIC_INVALID"


def test_expected_sha256_mismatch_is_rejected(tmp_path: Path) -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(200, content=PDF_BYTES)

    with pytest.raises(SecureDownloadError) as error:
        download_bounded(
            "https://www.cninfo.com.cn/a.pdf",
            tmp_path / "a.pdf",
            client=_client(handler),
            max_bytes=1024 * 1024,
            require_pdf=True,
            is_allowed_url=lambda url: True,
            expected_sha256="0" * 64,
        )
    assert error.value.code == "SHA256_MISMATCH"


def test_successful_download_is_atomic_and_reported(tmp_path: Path) -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(200, content=PDF_BYTES)

    result = download_bounded(
        "https://www.cninfo.com.cn/a.pdf",
        tmp_path / "a.pdf",
        client=_client(handler),
        max_bytes=1024 * 1024,
        require_pdf=True,
        is_allowed_url=lambda url: True,
    )
    assert (tmp_path / "a.pdf").read_bytes().startswith(b"%PDF-")
    assert result.byte_count == len(PDF_BYTES)
    assert len(result.sha256) == 64
    assert not list(tmp_path.glob(".*.part"))
```

- [ ] **Step 2: 运行确认失败**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_secure_download.py -q`
Expected: FAIL —`ModuleNotFoundError: No module named 'backend.app.secure_download'`

- [ ] **Step 3: 实现共享下载器**

创建 `backend/app/secure_download.py`：逐跳请求（`follow_redirects=False`）、每跳先复校目标 URL、最多 5 跳、块级限额、增量 SHA-256、`.part` 临时文件与成功后 `replace` 原子落盘、PDF 魔数校验、可选期望哈希。公开 `download_bounded(url, target, *, client, max_bytes, require_pdf, is_allowed_url, expected_sha256=None, max_hops=5) -> DownloadedFile` 与 `SecureDownloadError(code, message)`。限额超出的错误码必须是 `TOO_LARGE` 且**在读到超限的那一块立即中断**，不消费剩余响应体。

- [ ] **Step 4: 让两条既有下载路径复用**

`cninfo.download_pdf` 改为下载到 `tempfile` 目标、经共享下载器校验后读回 bytes，返回值与元数据形状保持 `(bytes, {"final_url","byte_count","sha256"})` 不变，并追加 `"streamed": True`。`source_cache._download_source` 同样改为调用共享下载器，保留 50MB 限额与哈希不符即抛错的行为。原 `_download_source` 内部实现删除，不留第二份流式代码。

- [ ] **Step 5: 分页截断显式化**

`search_annual_reports` 改为返回 `(announcements, query_status)` 的内部实现 `search_annual_reports_detailed`，公开 `search_annual_reports` 保留原返回类型并委托：

```python
        truncated = bool(body.get("hasMore")) and page == MAX_ANNOUNCEMENT_PAGES
        query_status = {
            "fetched_pages": page,
            "has_more": bool(body.get("hasMore")),
            "truncated": truncated,
            "report_year": int(report_year),
            "se_date": payload["seDate"],
            "max_pages": MAX_ANNOUNCEMENT_PAGES,
        }
        if truncated:
            query_status["completeness"] = "partial_page_limit_reached"
        else:
            query_status["completeness"] = "complete"
```

`select_annual_report` 增加可选参数 `query_status: dict | None = None`，截断时在 `selection_reason` 追加"候选列表已被分页上限截断，未完整核验最新版本"，并回传 `"candidate_completeness"`。pipeline 调用点写入 `announcement_query_status` 到案例记录，使验收能看出是否截断。

- [ ] **Step 6: 运行下载与 pipeline 测试**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_secure_download.py backend/tests/test_cninfo_pipeline.py -q`
Expected: PASS

---

## Task 6: 多源采集适配器边界补齐（C04，P2，不删文件）

**Files:**
- Modify: `backend/app/knowledge_ingest.py:20-95`
- Create: `backend/tests/test_knowledge_ingest_bounds.py`
- Modify: `PROJECT_STATUS.md`（状态措辞）

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_knowledge_ingest_bounds.py`，覆盖四项：非官方 `final_url` 必拒；`application/pdf` 头 + HTML 正文必拒（`pdf_magic_invalid`）；连续两次调用之间实际经过节流（注入时钟）；`MAX_DOWNLOAD_BYTES` 与说明一致（断言 `MAX_DOWNLOAD_BYTES == 120 * 1024 * 1024` 且 docstring 不含"200MB"）。

- [ ] **Step 2: 运行确认失败**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_knowledge_ingest_bounds.py -q`
Expected: FAIL（当前 `ok=True`、无节流、说明写 200MB）

- [ ] **Step 3: 改走共享下载器并落实节流**

`assess_official_document` 内部改用 `download_bounded(..., is_allowed_url=_is_official_url, require_pdf=expect_pdf, max_bytes=MAX_DOWNLOAD_BYTES)`，`tempfile` 落盘后校验；每次调用前按模块级 `_last_call_at` 与 `MIN_DELAY_SECONDS` 节流（注入时钟可测）；`content_type` 判定之后仍要求魔数与可解析性；docstring 的上限改为与常量一致的 120MiB。`BLOCKED_BODY_HINTS` 改为只对 HTML 正文判定，且 PDF 不因页面导航文字被误杀。

- [ ] **Step 4: 标注未接线状态**

`knowledge_ingest.py` 模块 docstring 追加一行事实：本模块当前未被 `backend/app` 任何路由调用，属"已实现未接入"的来源适配器，新增外部来源时必须连同测试一起接线。`PROJECT_STATUS.md` 的 G3 知识底座条目补同样措辞，避免被读成已可用的入库链。

- [ ] **Step 5: 运行测试**

Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_knowledge_ingest_bounds.py backend/tests/test_knowledge_sources.py -q`
Expected: PASS

---

## Task 7: 反例入库与全量复验

**Files:**
- Modify: `.gitignore:112` 附近
- Modify: `docs/TEST_INVENTORY.md`
- Modify: `docs/review-assets/20260909_logic_probes.py`（输出后追加"已固化为 pytest"提示）

- [ ] **Step 1: 放行审查产物**

`.gitignore` 在 `!/docs/TEST_INVENTORY.md` 之后追加：

```
# 审查报告与反例脚本是实施依据；被 /docs/* 吞掉会让干净克隆无法复验缺陷。
!/docs/2026-09-09_代码与运行逻辑审查及开源对照优化方案.md
!/docs/review-assets/
!/docs/superpowers/
```

Run: `git status --porcelain docs`
Expected: 报告、反例脚本与本计划出现在未跟踪列表（不 add，等队长决定）

- [ ] **Step 2: 全量测试与门禁**

```bash
backend\.venv\Scripts\python.exe -m pytest backend/tests -q
backend\.venv\Scripts\python.exe scripts/check_chinese_comments.py
node scripts/check_frontend_contract.mjs
backend\.venv\Scripts\python.exe docs/review-assets/20260909_logic_probes.py
```

Expected: 全量测试通过数不低于改动前基线；中文注释比例 ≥10%；反例脚本五项中 C01/C03 取到正确值、C04 返回 `ok=false`、C05 请求次数 >1、C02 命中数 0。

- [ ] **Step 3: 更新测试清单与报告**

`docs/TEST_INVENTORY.md` 登记新增五个测试文件；审查报告追加"2026-09-09 实施结果"一节，写明每项实跑命令、通过数与**仍未证明的边界**（真实年报错误率、开源解析器对照、Docling 引入决策均未做）。

---

## 实施结果与计划偏差（2026-09-09 当日回填）

计划按顺序执行完毕，六处与计划不同，均为实施时取得的证据所致：

1. **T1 本期列不是固定第 0 列。** 计划里"按位置取 offset 列"在倒序比较列上会把上年值标成本年值。`_resolve_column` 改为接受 `report_year`，用表头期间标签的年份对齐目标列；表头不含目标年度时返回 `unresolved_report_year_not_in_header`。`extract_cninfo_fields` 逐文档传入该文档的报告年度。
2. **T2 中文必须滑窗分词。** 直接用整段 CJK run 作 token 时，"应收账款的审计抽样程序" 与条目里的 "审计抽样" 永不匹配，导致真实清单 26 题全部返回空。新增 `_tokens_from_text`（整段 + 2–4 字滑窗，确定性、无外部分词库）。
3. **T2 追加跨企业过滤。** 检索测试集暴露 5 条"问五粮液／海油／比亚迪／锦州港却命中 600302 年报登记"的过召回。相关性挡不住它，改为在 `retrieve_knowledge` 内直接剔除 `_claim_scope == case_fact_prohibited` 的条目。
4. **T1 改了 `test_cninfo_pipeline._pdf_bytes` 夹具。** 失败原因是夹具表头本身没写期间列，新逻辑正确地报"列身份未确立"。选择给夹具补上真实年报的期间/附注列头，而不是把断言放宽成 `passed_technical_with_gaps`。
5. **T5 `download_pdf` 保留 bytes 返回合同。** 峰值内存已在接收过程按 1MiB 块受限；解析仍需整份字节，故最后一读不可避免，元数据新增 `streamed: True` 与 `redirect_hops`。
6. **T6 反例脚本改为复核脚本。** C04 从 urllib 换成共享下载器后，旧脚本的 `urlopen` patch 会失效并**真的向巨潮发请求**；脚本已改为注入 `httpx.MockTransport`，并把 C03 的抛错记成 `rejected:PDF_REPORT_YEAR_UNCONFIRMED`。

**检索测试集量化结果（同一 `questions.json`，26 题，k=5）：**

| 口径 | 应拒未拒 | 意外空 |
| --- | --- | --- |
| HEAD `155e3ad`（基线，本次重跑复现） | 11 条 | 0 条 |
| 本次修复后 | 6 条 | 0 条 |

基线文件：`artifacts/competition-improvement-20260908/retrieval-test/results_baseline_head_155e3ad.json`。原 9/8 生成的同名基线在本次实施中被中间态运行覆盖，已用 HEAD 原始模块重跑重建，并明确标注为 2026-09-09 重建。剩余 6 条应拒未拒都属"登记条目是类比/背景入口"而非文档正文，需要真人标注相关/不相关后才能继续收紧，本轮不靠猜阈值换取好看数字。

**未执行的动作：** 未 `git add`、未 commit、未 push、未部署、未触发任何付费模型调用（AGENTS.md §6）。

## 明确不做

- 不引入 Scrapy、RAGFlow、LightRAG；AKShare 与 Docling 只保留为后续离线对照候选。
- 不删除 `knowledge_ingest.py`（用户文件），只做边界补齐与状态标注。
- 不做真实年报抓取负载测试，不触发付费模型调用。
- 不改 `index.html`、样式与前端合同；本轮全部是后端数据链。
- 不动已冻结案例数据、T0、baseline 与人工评分台账。
