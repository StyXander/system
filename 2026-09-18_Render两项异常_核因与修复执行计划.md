# Render 两项异常：核因与修复执行计划

日期：2026-09-18。状态：阶段 A 已完成 Render 只读核验，阶段 B/C 待按本计划执行。本文件记录证据与执行方案，不代表代码已修复或线上已验收。

## 一、目标与边界

用户反馈：本地运行没有这两个问题，Render 网站出现异常。目标是解释本地与线上为什么不同，修复实际原因，并以同一案例、同一操作路线的真实验收证明修复有效。

本次是工程故障核验与修复计划。正式页面仍为根目录 `index.html` 及 `assets/`。实施限于本项目内的状态判断、来源准备、任务收尾、相关测试与运行说明，不改变审计规则、冻结数据、模型选择、人工评分和发布签字。

当前用户要求是编写计划；后续执行本地修改需获得执行指令。提交、推送、部署按项目 AGENTS.md 的“未经用户明确要求，不提交、推送、部署、发布或发送外部消息”执行；如果后续指令已明确包括部署，则完成验证后继续，不重复询问。

AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。

## 二、已知事实与尚未确认的原因

| 项目 | 已知事实 | 尚不能下的结论 |
| --- | --- | --- |
| 五粮液 | 截图同时显示“三 Agent 已通过硬校验、3/3 角色完成”和“本次未完成真实模型调用” | 不能据此判断 API Key 无效，也不能确定一定命中了缓存 |
| 标准股份 | 截图显示 2022 年来源下载 HTTP 403，来源准备失败；同一画面仍显示“正在分析” | 403 的具体触发条件、加载状态是否持续不结束，尚未确认 |
| 本地 | 用户反馈两个问题均不存在 | 尚未在本次任务独立复现；不能假定本地走过与线上完全相同的冷启动、缓存、公开任务路径 |
| 工作区 | 本地 `demo-app.js`、`main.py`、`schemas.py`、`index.html` 等文件已有未提交修改 | 本地工作区、GitHub 与 Render 是否一致，尚未确认；禁止覆盖这些改动 |
| 部署配置 | 本地 `render.yaml` 的构建命令使用 `public_demo_bootstrap --allow-missing-sources` | Render 当前部署是否采用这一文件及实际环境覆盖值，待核实 |
| 来源准备 | 本地 `public_demo_bootstrap.py` 允许官方来源 HTTP 403 时返回 degraded 并继续构建；运行入口会再尝试准备标准案例来源 | 网站健康检查成功不能证明标准案例来源或 RAG 就绪 |
| 状态分类 | 本地 `outcomeFromRun()` 排除缓存回放等分支后，其余部分情况进入 degraded；该展示分支统一写“未完成真实模型调用” | 不能把本地代码分析直接当成线上故障根因；先取线上版本和原始运行数据 |
| 线上核验 | 上一轮只读访问未取得实时状态，HTTP 检查超时 | 该超时不是来源 403 的复现，也不是 Render 整站不可用的证据 |

## 三、执行顺序与证据保存

### 2026-09-18 Render 控制台核验结果（只读）

- 服务为 `audittrace-demo`，Render 当前成功部署提交为 `a4fe9951b64ebf0785419b6dfabff5186d921937`，与公开接口 `/api/status` 的 deployment commit 一致。
- Environment 页面仍存在并处于脱敏状态的 `DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`DEEPSEEK_MODEL`、`SUPABASE_URL`、`SUPABASE_SERVICE_ROLE_KEY`、`SUPABASE_PRIVATE_BUCKET`、`SUPABASE_SIGNED_URL_SECONDS` 及任务/配额配置变量；未读取、未保存或输出任何密钥值。
- 最近一次成功部署构建日志明确记录：`public_demo_bootstrap=degraded`、`OFFICIAL_SOURCE_HTTP_403`、`source_count=0`、`rag=not_built`。这直接解释标准股份在 Render 缺源；不是 DeepSeek 或 Supabase 密钥丢失。
- 同一部署的启动日志记录 `provider=deepseek_direct`、`model=deepseek-v4-flash`、`external_model_enabled=true`、`api_key_present=True`，说明线上应用能读到 DeepSeek 配置。
- Render 的 Disk 页面提示 Free 方案不支持持久化磁盘。不能把本地年报文件或 RAG 索引假定为 Render 重启后仍存在；应按 C2 评估受控外部来源/构建输入。
- 当前公开 `/api/status` 报告 `task_backend=supabase`、`demo_task_configured=true`、`demo_quota_configured=true`、`availability=ready`；构建日志中 `persistence_not_supabase` 只对应公开目录构建步骤，不能据此判定任务持久化配置丢失。

以上证据仅确认配置与来源准备状态；尚未触发新的付费模型运行、修改 Render 环境、重部署或发布。

执行顺序：保护现有改动 → 版本与路径对齐 → 采集两案运行证据 → 分别确认根因 → 最小修改 → 本地回归 → 授权范围内上线 → Render 验收。

执行开始时创建 `outputs/render-two-issues-<实际时间>/`，建议保存：

- `baseline.md`：本地 HEAD、工作区改动清单、远程提交、Render 部署提交、浏览器资源版本。
- `environment-diff.json`：脱敏配置差异、案例清单与来源文件哈希、缓存和任务存储模式。
- `case-wuliangye/`：任务及运行响应、原始结果与缓存回放关系、截图、浏览器错误和判定说明。
- `case-standard/`：构建和运行日志摘录、来源访问结果、冷暖缓存结果、截图。
- `tests/`：回归命令、退出码、测试摘要与浏览器验收证据。
- `acceptance.md`：每项通过/失败/未验证、证据路径、剩余问题及回滚基线。

不得保存密钥、Cookie、Authorization、完整环境变量或带凭据的 URL。日志按 task_id、run_id、时间关联，保留用于解释问题的最少数据。

## 四、阶段 A：确认本地和 Render 到底差在哪里

### A1. 保护工作区并确认版本

1. 阅读项目 AGENTS.md、主方案书、运行说明、当前发布指针及相关子目录规则；确认实际入口和现有测试命令。
2. 记录 `git status --short`、`git rev-parse HEAD` 和相关文件差异。必要时在项目内保存本次修改前补丁；不得 reset、clean 或覆盖已有改动。
3. 只读取得 GitHub 目标分支提交和 Render 实际部署提交；取 `/api/status`、`/api/health`、`/api/demo/bootstrap` 的当前响应，字段以实际返回为准。
4. 比较本地与线上 `index.html`、`assets/official-v4/demo-app.js` 的内容或哈希、资源版本参数及响应缓存头。记录实际浏览器加载的脚本地址，排查旧页面、旧静态资源和服务端版本不一致。
5. 若版本不同，先在独立可丢弃目录复现线上版本或对照差异，禁止为了“同步”把所有本地未提交文件直接发布。

通过条件：明确本地正常对应的代码和资源版本，明确截图对应线上版本能否追溯；追溯不到时标为未知，用新一次受控复现替代。

### A2. 对齐运行条件

对比以下项目，只记录配置值是否存在及非敏感值：

| 类别 | 核对内容 |
| --- | --- |
| 操作路径 | 同一案例 ID、年度、T0；是否点击完整分析、备用演示或恢复历史任务 |
| 模型与任务 | 模型标识、公开模式、外部模型开关、任务持久化、执行模式、配额状态 |
| 来源与索引 | 年报文件是否存在、哈希、来源列表、RAG 是否建成及案例隔离 |
| 缓存 | 是否命中、有效期、键组成、来源运行、实例内与持久化缓存的差异 |
| 实例环境 | 构建命令、启动命令、重启后文件可用性；不预先假定文件一定丢失 |

本地正常必须进一步区分：实时新运行正常、历史结果正常，还是因为本地已有来源文件而没有执行下载。

## 五、阶段 B：五粮液状态矛盾的核因与修复

### B1. 先采样，后判断

从真实浏览器当前请求定位任务和运行接口，不猜测不存在的路由。保存同一次运行的原始结果与最终任务响应，检查：

- task_id、run_id、任务终态及阶段状态。
- `model_check.status`、`execution_mode`、`model_check.execution_mode`、`cache_hit`。
- `provider_call_count`、`run_completeness`、三个角色状态及脱敏调用留痕。
- 缓存来源运行标识、原运行时间、原调用记录与本次回放的区别。
- 来源校验、规则结果是否存在，以及后端序列化和 Supabase 读取前后字段是否丢失。

先读取已有任务，避免为确认状态重复付费调用。只有不能通过已有证据确认实时分支时，才在现有授权和额度内执行最少的新鲜运行；不能把缓存回放算作实时调用。

### B2. 根因决策表

| 证据 | 根因方向 | 修改动作 |
| --- | --- | --- |
| 线上脚本旧，本地同样响应能正确展示 | 部署或资源缓存差异 | 发布经验证的必要改动；按现有机制更新资源版本，验证实际加载字节 |
| 原运行成功，本次是可信缓存回放且本次调用数为 0 | 缓存状态被笼统解释为降级失败 | 增加明确的历史结果展示，不伪造本次调用次数 |
| 有真实成功调用留痕，但结果字段在存储或读取后缺失 | 数据合同或序列化问题 | 修复字段传递、回放适配和兼容读取；保留历史原始记录 |
| 模型成功标签存在，但没有可核验调用或缓存来源 | 状态证据不完整 | 显示“运行证据不完整/待核验”，不得自动补成成功 |
| 实际模型失败或仅部分角色完成 | 真实执行失败 | 保留真实失败原因，按鉴权、限流、超时、校验等具体错误处理 |

### B3. 期望状态合同

以下是展示要求，具体字段名须按当前项目合同设计，避免新增字段与现有枚举冲突。

| 实际情况 | 应显示内容 | 计数要求 |
| --- | --- | --- |
| 本次真实完整成功 | 本次完整分析已完成 | 使用实际调用数和实际角色状态 |
| 可信历史成功结果 | 已复用历史分析结果，本次未新增模型调用 | 本次为 0；原运行时间与来源可追溯 |
| 历史部分失败结果 | 已复用历史结果，并保留原失败/缺口说明 | 不把历史降级结果提升为成功 |
| 确定性备用 | 确定性结果已生成，未执行真实模型分析 | 不显示三 Agent 本次成功 |
| 模型失败 | 分析未完整完成，并给出可理解的失败原因 | 完成与跳过角色如实显示 |
| 字段缺失或冲突 | 运行证据不完整，需核查 | 不以缺省 0 直接断定模型从未执行 |

卡片、阶段条、顶部通知、详情、导出及刷新恢复应一致。禁止只替换一条警告文案，或者把所有 degraded 都改为 success。

优先检查文件：`assets/official-v4/demo-app.js`、`backend/app/schemas.py`、`backend/app/public_model.py`、`backend/app/supabase_adapter.py`，并沿实际调用链补充。当前 `sanitize_cached_trace()` 对回放计数的处理需要核验，但不能在没有证据时直接删除。

## 六、阶段 C：标准股份来源 403 与任务收尾

### C1. 确认来源失败链

1. 对齐 `STD_DEV_T0` 的实际来源登记、四份文件及调用入口；说明界面展示 2025/2024/2023 而依赖 2022 年文件的原因。若属于期初或比较基期依赖，保留；若属于误引用，依据冻结来源合同修复，不能直接删掉 2022 年绕过问题。
2. 在 Render 构建日志查找 `OFFICIAL_SOURCE_HTTP_403`、`public_demo_bootstrap=degraded`、`rag=not_built` 等实际输出；再关联运行日志，确认是否属于“构建允许缺源上线，运行再次下载仍失败”。
3. 在 Render 执行环境请求同一登记 URL，记录状态码、最终地址、内容类型和有限的脱敏响应摘要；与本地同一请求对比。
4. 核查链接有效性、是否返回拦截页、请求条件及出口差异。未经对比，不写成“确定是海外 IP 被封”。不通过绕过访问控制或反复轰炸来源站解决问题。
5. 在隔离测试目录模拟无来源缓存状态；不得删除本地工作区唯一年报文件。再验证有效缓存复用、损坏缓存拒绝、重启恢复。

### C2. 按证据选择来源修复

优先恢复登记官方来源的正常受控访问。若是请求实现与官方正常访问要求不一致，修复最小必要请求参数并验证；若链接失效，核对官方原件及哈希。若文件内容改变，进入来源版本和授权重新核验，不静默改登记哈希。

如果 Render 无法稳定获取，而已有合法取得且哈希匹配的原件，评估受控缓存方案：沿现有基础设施准备私有来源存储或可靠构建输入，服务端读取并验证 PDF、体积、SHA-256、路径与来源身份。公开来源入口仍返回官方原件地址，不新增公开全文镜像。

选择缓存方案前先核实现有存储权限、容量和实际可用能力。`render.yaml` 当前声明免费方案，不假设具备持久磁盘；不擅自升级付费套餐、引入新服务或将 PDF 提交公共 Git 仓库。所需资源暂不可用时，交付明确阻塞原因及可实施备选，不能把禁用案例当成业务修复完成。

加入每案例来源/RAG 就绪状态。构建可以保留“其余案例仍可用”的策略，但标准案例未准备好时必须在开始前明确显示原因；不能继续宣传该案例当前可完整运行。该状态只表示运行准备，不修改正式人工验收记录。

### C3. 修复失败后的终态

分别检查任务创建前失败和创建后失败：

- 创建前来源检查返回错误：解除 loading，显示来源准备失败，恢复符合当前条件的操作按钮，不轮询不存在的任务。
- 创建后准备失败：后端持久化 failed 终态与来源错误；前端收到终态后停止轮询并解除 loading。
- 规则、检索、Agent、验证、导出按依赖关系标记为未执行/跳过，不继续显示“等待开始”造成仍在工作的错觉。
- 用户刷新后恢复相同失败状态；临时网络错误不能直接冒充任务失败；实例中断与来源失败分别处理。
- 重试由用户明确触发；不因刷新、恢复或超时自动重复模型调用。不绕过已有防重复、配额和并发控制。

优先检查 `backend/app/source_cache.py`、`secure_download.py`、`public_demo_bootstrap.py`、`main.py`、实际任务执行/存储模块、`demo-app.js` 和 `render.yaml`。

## 七、实施原则与回归测试

每项代码修改前必须写出“可复现输入 → 实际行为 → 预期行为 → 根因证据”。如果根因仅是部署差异或环境配置，避免无必要地改业务代码。

页面仅修状态及错误恢复，沿用当前字体、布局、颜色、间距、断点与动效；成功、历史回放、备用、失败均有文字标识，不仅依靠颜色。实施前补齐项目要求的页面说明与读取材料，不做整体视觉重构。

使用现有测试体系。已发现 `scripts/verify_demo_outcome_judge.mjs`、`backend/tests/test_demo_run_tasks.py`、`test_demo_bootstrap.py`、`test_batch3_cache_manifest.py`、`test_v7_closure.py` 及浏览器验收脚本；先阅读参数和行为，防止批量脚本意外触发付费模型调用。

| 测试组 | 必须覆盖的行为 |
| --- | --- |
| 状态判定 | 实时成功、可信成功缓存、失败缓存、备用、部分角色失败、字段缺失、未知枚举、来源失败 |
| 数据往返 | 运行结果经序列化、任务存储和恢复后不丢关键信息；回放本次调用数仍为 0，不重复记账 |
| 来源准备 | 有效缓存复用、冷缓存成功、403、非 PDF、哈希错误、超大文件、非法重定向；失败不留下可误用文件 |
| 任务终态 | 创建前失败、执行中失败、终态刷新恢复、轮询网络短暂失败、实例中断、显式重试、防重复 |
| 功能回归 | 两个目标案例的读取、计算、RAG、Agent、来源详情和同源导出；第三个可用案例作对照 |

定向测试通过后运行项目规定的后端测试、前端合同/语法检查、中文注释检查及 `git diff --check`。构建、类型检查、Lint 按项目实际工具链执行；不存在则注明不适用。测试日志记录实际命令、退出码及失败原因，不引用历史通过数充当本次结果。

浏览器必须连接真实服务，覆盖 1440×1000、1024×768、768×1024、390×844，检查控制台、失败请求、溢出、焦点、键盘、触控、减少动态效果及错误提示。截图需查看，不能只保存。纯状态/布局测试复用已有结果，只有指定业务烟测触发新模型调用。

## 八、上线与 Render 独立验收

发布前交付最小补丁、根因报告、本地验证结果、配置变化及回滚步骤。只纳入本次已审阅的改动，保留其他工作区内容。若部署未获授权，停在“本地修复已验证，线上待部署”，不得报告 Render 已修复。

获得部署授权后：

1. 记录上一可用提交和相关配置；提交、推送并等待部署，核验 Render 实际提交和资源字节，不能以 push 成功代替上线成功。
2. 检查构建来源准备输出和每案例就绪情况，不以 `/api/health` 返回 200 作为完整验收。
3. 标准股份：来源校验和 RAG 就绪后，从真实页面完成一次受控分析；保存任务、运行、调用留痕与截图。若模型另有失败，单独记录，不能掩盖成来源已全部修复。
4. 五粮液：分别验证新鲜执行结果和缓存复用结果；前者需有实际调用证据，后者本次调用数为 0、来源可追溯、页面不再误报失败。
5. 两案刷新后状态和导出一致。验证已完成结果可恢复；实例中断恢复须在隔离环境或已授权维护窗口验证，不能突然重启有用户任务的生产实例。
6. 403 故障注入只在隔离环境进行；检查失败后页面停止加载、后续阶段未执行、按钮恢复和明确重试行为。生产只验证修复后正常路径及已有真实失败记录的展示。

若生产出现新的严重回归，按授权的发布流程回滚到记录的可用提交及配置；不删除历史任务与审计记录。涉及新增存储字段时优先使用向后兼容字段，预先确认旧版本仍能读取。

## 九、最终完成标准

- [ ] 两个问题分别有已确认根因，能解释为什么本地正常而 Render 异常。
- [ ] 本地与线上代码、资源、来源、缓存、任务存储差异已记录，不把假设写成结论。
- [ ] 五粮液实时成功和历史复用分别准确显示；调用次数与真实留痕一致。
- [ ] 标准股份已在 Render 取得并验证必需来源，完成对应业务流程；仅隐藏警告、屏蔽案例不算完成。
- [ ] 来源失败和任务失败均可正确收尾，刷新不造成卡死或自动付费重跑。
- [ ] 本地原有正常流程未回归；相关自动化及真实浏览器验收有证据。
- [ ] 发布提交与线上资源已核对；未获部署授权或无法访问线上时明确记为待完成。
- [ ] 不修改人工评分、签字和正式发布门禁，不将两项工程修复等同于整体比赛验收通过。

最终交付包括：根因与本地/线上差异说明、实际改动文件、测试摘要、Render 任务和运行编号、必要截图、部署提交、未验证项与回滚说明。

## 十、后续执行指令

可直接交给执行者：

> 按本计划逐阶段确认并修复。先保护已有未提交改动，查清 Render 与本地的版本和运行路径，再取得两案任务响应及来源准备证据。不要把缓存回放直接认定为模型失败，也不要把 403 直接认定为 IP 封禁。证据支持后做最小修改，完成本地测试和真实浏览器验收；只在明确授权包含部署时发布并做 Render 独立验收。不能访问文件、日志或线上环境时必须说明，不得编造检查结果。交付实际修复、证据和剩余阻塞，不以单纯改文案或禁用案例宣布完成。
