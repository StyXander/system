# 审迹智链：公司输入到分析修复交付说明

## 结论

这次页面显示“0 条字段 · not_extracted”不是比亚迪没有年报，而是系统把“资料预热”和“完整分析”错误地混成了同一个缓存结果。

原来的 50 多家公司预热任务大多使用 `rag_only`，它只负责：

1. 按股票代码或公司名确认巨潮企业；
2. 搜索、选择并下载官方年度报告；
3. 校验 PDF 内容、页数、哈希和披露时点；
4. 建立 RAG 原文索引。

它本来就不会提取财务字段。更严重的是，后续选择 `full_analysis` 时，缓存命中逻辑把“缓存里暂时没有字段”当成了最终字段结果，所以页面一直显示 `not_extracted`，没有重新扫描已经校验好的 PDF。

## 本轮实际修复

### 1. 完整分析命中 RAG 缓存时自动补提取

`backend/app/pipeline.py` 现在区分“来源/RAG 缓存”和“字段分析缓存”：

- `rag_only` 仍然只建立来源和 RAG，不猜测财务金额；
- `full_analysis` 命中只有 RAG 的旧缓存时，自动复用已校验 PDF 执行字段提取；
- 字段提取成功后写回案例、目录和 Supabase（启用时）；
- 提取失败不再伪装成 ready，而是返回明确的字段失败或资料缺口。

因此以后输入公司名或股票代码后，即使该公司以前只做过 RAG 预热，选择完整分析也会自动补齐字段。

### 2. 修复巨潮报表单位写法

巨潮 PDF 常把单位写在字段标题后，例如“营业收入（元）”，而不写成“单位：元”。
`backend/app/field_extraction.py` 现在两种写法都识别，并保留原始单位和页码。

这正是比亚迪 2024 年营业收入之前漏掉的直接原因之一。

### 3. 修复能源行业可选字段导致的 KeyError

能源/矿业规则声明了“应收账款坏账准备”和“应收账款净额”两个可选字段，但旧提取器没有登记词表，导致浙江能源、中石化、长江电力等案例在字段步骤直接 `KeyError`。

现在已补齐字段配置，并加入防御逻辑：即使未来行业规则和词表暂时不同步，也只形成明确资料缺口，不会让整个下载任务崩溃。

### 4. 修复金融行业有字段却显示无可用年度

银行、保险、券商不使用普通 R1 的“营业收入 + 应收账款”完整性口径。旧代码只按 R1 计算 `available_years`，所以金融行业明明已有专用字段，页面仍可能显示空年度。

现在会按各自专用规则计算连续年度，并保留普通 R1 年度作为独立元数据。

### 5. 补充中国平安行业映射

“中国平安”名称本身不包含“保险”二字，单靠关键词会被误判为未知行业。现在对已验证的证券代码 `601318` 使用受控的保险专用规则映射，不接受浏览器自行提交行业结论。

### 6. 增加本地批量回填工具

新增 `scripts/backfill_cninfo_fields.py`。它只读取已经下载并通过校验的本地 PDF，不会偷偷联网，也不会调用外部模型：

```powershell
backend\.venv\Scripts\python.exe -m scripts.backfill_cninfo_fields
```

单家公司示例：

```powershell
backend\.venv\Scripts\python.exe -m scripts.backfill_cninfo_fields --case-id CNINFO_002594_T0_20260327
```

## 当前数据验收结果

本地 52 个巨潮案例已完成一次字段回填：

| 项目 | 结果 |
|---|---:|
| 案例总数 | 52 |
| 有字段候选的案例 | 52 |
| 字段证据行 | 404 |
| 技术提取通过、待人工复核 | 44 |
| 技术提取通过但有资料缺口 | 8 |
| 仍为 `not_extracted` | 0 |
| 有连续可比年度的案例 | 35 |

“有资料缺口”不等于没有年报，而是某行业专用规则要求的字段在某一年度没有形成可回查候选。系统现在会进入 `DATA_GAP` 并列出缺哪些字段和应补哪些资料，不再显示成下载失败。

### 比亚迪实测

股票代码 `002594` 的三份官方年报均已存在并通过校验。完整分析实际得到 6 条字段候选，覆盖 2025、2024 两个可比年度：

- 2025 年营业收入：803,964,958,000 元；
- 2025 年应收账款：37,004,685,000 元；
- 2024 年营业收入：777,102,455,000 元；
- 2024 年应收账款：62,298,988,000 元；
- 2023 年营业收入：602,315,354,000 元；
- 2023 年应收账款：61,866,019,000 元。

这条数据还通过了真实 FastAPI 流程：公司确认、年报清单、文档选择、下载复用、来源校验、案例登记、RAG、字段提取、字段复核、分析运行共 11 个步骤全部通过，并生成 `RUN-V7-...` 运行记录。

另外验证了：

- `601318` 中国平安：保险专用规则完整跑通，生成候选结果；
- `600023` 浙江能源：能源专用规则完整跑通，生成候选结果；
- `600030` 中信证券：字段不足时完整跑通并明确返回 `DATA_GAP`，没有任务崩溃。

## 以后正确操作

1. 在“公司/股票代码”输入框输入名称或代码。
2. 选择 `full_analysis`（完整分析）。页面默认就是该模式。
3. 等待任务步骤完成；如果资料已经预热，系统会显示“复用已校验 PDF，补做结构化字段提取”。
4. 看到 `passed_technical_pending_human` 时，表示字段已提取，但正式采用前仍要回 PDF 页码人工复核。
5. 看到 `passed_technical_with_gaps` 或 `DATA_GAP` 时，表示年报已找到，但当前行业规则所需字段不齐；按页面列出的缺口补充资料即可。

`rag_only` 只适合先下载、校验和建立原文检索库，不会直接给出完整财务分析。现在即使先使用过 `rag_only`，之后切换到 `full_analysis` 也会自动补提取。

## 验证记录

- 后端全量：`177 passed, 1 warning`；warning 仅为 TestClient/httpx 弃用提示；
- 前端：`node --check assets/official-v4/app.js` 通过；前端合同检查通过；
- Python：`compileall backend/app scripts/backfill_cninfo_fields.py` 通过；
- 中文注释门槛：`10.79%`，高于项目要求；
- `git diff --check` 通过。

## 仍然保留的边界

- 自动提取只是候选，不替代专业人员确认；
- 外部模型调用仍受登录、模型同意、传输范围和凭据配置控制；
- 专用金融行业字段缺失时，系统应返回资料缺口，不应为了“有报告”而猜测普通 R1 字段；
- 若部署到新的 Render worker/web 环境，需要在该环境已有 PDF 缓存后运行一次回填命令，或直接让用户第一次选择 `full_analysis` 自动补提取。
