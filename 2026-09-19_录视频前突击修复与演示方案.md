# 录视频前突击修复与演示方案

> ## ⚠️ 本文件的实施方案已被取代，且 §四 P3 的根因判断已撤回
>
> **请改读** `2026-09-19_综合执行方案_多AI并行.md`。
> 本文件 **§一 缺陷一（数字闸门误杀）与 缺陷二（前端假话）的证据链仍然有效且已复核**，
> 但 **§四 P3「阻断字段仍被 R1 采用」为错误结论，已撤回**：
> `cases.py:1934-1944` 的 `calculation_ready_financial_rows()` 已按 `candidate_quality_issues` 过滤 blocked 行，
> 全库 53 次天文 `growth_gap` 全部落在 2026-08-10~08-13，2026-09 以后 0 次。
> 详见新方案 §0.1 与 W17。
>
> 时间前提也已变更：**不再是"下午录视频"，可用时间 ≥24 小时**。

日期：2026-09-19。状态：**缺陷一/二证据有效，实施方案已作废**。本文件只记录证据与建议，未修改任何代码、配置或冻结数据。
授权边界：用户已授权真实模型调用；实施需另行指令。

AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。

---

## 零、一句话结论

系统没坏，也**不是规则太少**。查到的是两个方向相反、但同源的缺陷——**都是"系统已经知道真相，却没按真相行动"**：

**缺陷一（让你以为产品没用）**：你今天下午连续三次"无法完成"，是一个**可证明的闸门误杀**。模型从年报第 89 页正确引用了 `41,813,685.32` 并绑定了 evidence_id，但数字闸门不读 RAG 原文（`numeric_gate.py:113-131` 只看结构化 `value`，而 `rag_evidence.value` 恒为 `None`），于是判"关键财务数字无来源"，把一次**成功的真实运行**降级；前端又没有处理该状态的分支，落到 `evidence_incomplete`，屏幕显示**"本次没有可核验的调用留痕"——一句关于自己系统的假话**（实际有 3 次调用、哈希齐全、3/3 角色完成）。

**缺陷二（会让答辩翻车）**：抽取器把年报里的**附注序号当成了金额**（紫金矿业上年应收账款 = 1.00 元，真实值 7,777,908,320 就在旁边）。系统**自己准确诊断了这一点**，写下"疑似附注号、序号或叙述数字，须人工回页确认"，并打了 `candidate_quality_status = "blocked_pending_human_confirmation"`。**但这个阻断标记在 `backend/app` 里没有任何消费者**（全量 grep：写入 2 处，读取 0 处），R1 照样拿 `value=1.0` 算出 `growth_gap = 93 亿百分点`，并标为"强候选"。这是 `field_extraction.py:436` 明文写的设计决定："公开预筛允许带着明确缺口继续运行"。**质量问题被报告了，没有被执行。**

15 个演示案例里 **6 个**中招；**五粮液 blocked 字段数 = 0，是唯一干净的**，这也是它能稳定给出 55.62pp 的原因。

修缺陷一约 75 分钟，修缺陷二约 40 分钟。两处都不新增阈值、不发明专业判断，只是**让系统按它自己已经得出的结论行动**。

---

## 一、根因证据链（全部来自真实运行文件，非推测）

### 1.1 你今天的三次失败

| 运行 | 时间 | model_check | 三角色 | run_completeness |
| --- | --- | --- | --- | --- |
| `RUN-V7-D8933A6BBDDB` | 09-19 13:09 | `model_success` | 3/3 completed | `incomplete_numeric_claims` |
| `RUN-V7-BB86D15EEE1E` | 09-19 13:10 | `model_success` | 3/3 completed | `incomplete_numeric_claims` |
| `RUN-V7-43AD8C03C3A7` | 09-19 13:13 | `model_success` | 3/3 completed | `incomplete_numeric_claims` |

三次都有真实调用留痕：`provider_call_count=3`、`execution_mode=external_live`、`cache_hit=false`、每个角色 `provider_call_performed=true`、`input_sha256`/`response_sha256` 齐全。

`context` 里 `prescreen_plan.missing_fields=[]`、`skipped_rules=[]`、`case_material_gaps=[]`。
**所以这不是"资料不足"，五粮液的资料是齐的。**

### 1.2 闸门卡在哪个数字

三次运行的 `context.numeric_claim_trace`：

```
unverified_count: 3~4
key_unverified_count: 1~2
key_unverified: ["41,813,685.32"]        ← 三次全中同一个
                  ["7,156,516.61"]        ← 第三次多一个
passed: False
```

模型的原话（`agent_steps[review].output.normal_explanations[1]`）：

> 待验证假设：应收账面余额 **41,813,685.32** 元与报表净额 37,745,419.51 元口径差异或影响增速可比性。
> evidence_ids: `RAG-CNINFO_000858_T0_20260430-2025-P0089-C00`, `CNINFO_000858_T0_20260430_ACCOUNTS_RECEIVABLE_2025`

判定逻辑在 `backend/app/main.py:4310-4312`：

```python
if numeric_gate.get("key_unverified_count") and model_check.status == "model_success":
    run_completeness = "incomplete_numeric_claims"
```

### 1.3 这是误杀，不是模型编数字（已实测验证）

我在该运行的 `evidence_bundle.rag_evidence` 里逐条搜索，结果：

```
id=RAG-CNINFO_000858_T0_20260430-2025-P0089-C00   value=None   含目标数字=True  ← 命中
其余 5 条 rag_evidence                                            含目标数字=False
```

`41,813,685.32` **真实存在于模型所引用的那个年报原文片段中**，模型还绑对了 evidence_id。

闸门看不见它的原因在 `backend/app/numeric_gate.py:113-131`：

```python
def _evidence_sources(evidence_bundle):
    for key, rows in evidence_bundle.items():
        for row in rows:
            value = row.get("value")
            if isinstance(value, (int, float)):      # ← rag_evidence 的 value 恒为 None
                sources.append(...)
```

`field_evidence` 有结构化 `value`（所以 `405.29亿`、`3774.54万` 都能追溯成功），
`rag_evidence` 的 `value=None`，数字在原文文本字段里 —— **闸门完全不读**。

> 专业含义：模型指出的"应收账面余额 vs 报表净额口径差异影响增速可比性"是**本次运行里最有会计含量的一句判断**。它因为闸门覆盖不全被毙掉了。

### 1.4 前端把这次降级说成了假话

`assets/official-v4/demo-app.js:1592-1611` `outcomeFromRun()`：
`completeness.startsWith("complete_")` 为假 → 落到 `return "degraded"`。

`demo-app.js:1617-1632` `degradedReason()`：
不是 cache_replay、不是 deterministic_backup、`modelStatus === "model_success"` → 落到最后一个分支
`{ kind: "evidence_incomplete" }`。

于是页面显示（`DEGRADED_COPY.evidence_incomplete`，`demo-app.js:1654-1659`）：

> **运行证据不完整，需核查**
> 模型状态登记为 model_success，但**本次没有可核验的调用留痕**

这句话对本次运行是**错的**：有 3 次调用、有哈希、有 3/3 角色留痕。
真实原因是"1 个财务数字未能追溯"。前端没有任何分支处理 `incomplete_numeric_claims`（在 `assets/` 全量搜索该字符串，0 命中）。

**这就是你觉得"系统没用、忙活这么久白干"的直接来源：系统做对了，但把自己的成功报成了失败。**

---

## 二、录哪个案例（以 2342 个真实运行文件为准，不信清单）

### 2.1 先纠错：`competition_demo_cases.json` 的历史登记不可信

清单里 `historical_external_model_run` 写长江电力是 `risk_candidate` / `complete_public_prescreen`。
实测该 run 文件 `RUN-V7-684E88DF0FBC.json`：`status=DATA_GAP`、`route=industry_review`、`run_completeness=complete_public_prescreen_with_gaps`。
**清单是历史记录，不是当前行为保证。选案例必须看运行文件。**

### 2.2 全量扫描结果（439 次 candidate 命中，其中 48 次真实 live）

| 案例 | candidate 命中 | 真实 live | growth_gap | 可否上台 |
| --- | --- | --- | --- | --- |
| **五粮液** `CNINFO_000858_T0_20260430` | **134** | **35** | **0.5562 稳定** | ✅ **首选** |
| 标准股份 `STD_DEV_T0` | 97 | **0** | 0.268~0.305 | ⚠️ 无 live 记录 |
| 保利发展 | 25 | 2 | 0.557 | 备选 |
| 中国神华 | 25 | 2 | 0.189 | 备选（弱） |
| 万科A | 23 | 3 | 0.493 ~ **2.38e9** | ❌ 脏数据 |
| 中国铁建 | 17 | 3 | 0.212 ~ **2.0e7** | ❌ 脏数据 |
| 长江电力 | 10 | 0 | **1.45e9** | ❌ 脏数据 |
| 紫金矿业 | 10 | 0 | **9.31e9** | ❌ 脏数据 |
| 立讯精密 | 10 | 0 | **4.84e7** | ❌ 脏数据 |

> 上表标 ❌ 的案例，根因不是"除零"，而是**抽取字段被系统自己标为 blocked 却仍被 R1 采用**。
> 实测字段值、系统自带的诊断原文、影响面与修复方案见 **§四 P3**。

### 2.3 五粮液的 35 次 live 运行分布

```
complete_public_prescreen              30 次   ← 干净完整，零缺口
incomplete_numeric_claims               3 次   ← 今天这三次，闸门误杀
complete_public_prescreen_with_gaps     2 次
route:      risk_candidate             35/35
conclusion: risk_candidate             34/35
```

**86% 的成功率，失败全部来自 §1.3 那个误杀，不是随机的。**

### 2.4 五粮液镜头前会显示的实际结论（取自真实运行）

```
应收账款增速    +1.07%
营业收入增速   -54.55%
增速差         55.62 个百分点      阈值 15pp / 强阈值 30pp → 强候选
应收绝对变动   398,857.56 元
本年营业收入   405.29 亿
本年应收账款   3,774.54 万（净额）
AI 反证假设    经销为主、先款后货的结算方式或可解释背离（绑定年报第10页）
AI 反证假设    账面余额与净额口径差异或影响增速可比性（绑定年报第89页）
待索取资料     账龄明细表、期后回款记录、信用政策说明、主要合同关键条款摘要
```

这是一条**有具体数字、有反证、有下一步程序**的结论，不是"资料不足"。

---

## 三、必须纠正的三个认知（这三条比改代码更重要）

### 3.1 "要求补充资料"不是失败，是你的交付物

你自己的方案书定的输出就是：**预审风险备忘录 + Top 5 待核查事项 + 待索取资料清单**。
审计计划阶段，注册会计师**本来就不能**只凭公开年报下结论。一个敢对着年报说"这家公司有问题"的工具，在会计教授评委面前会**当场失去全部可信度**。

所以别去削弱这个边界。要做的是**让差异化判断出现在资料清单前面**，而不是被资料清单盖住。五粮液有判断：55.62pp、超强阈值、A2 级。

### 3.2 "AI 的灵魂"已经存在，只是被压平了

现在的分工（`agents.py:353-391`、`main.py:2198-2494`）：**所有数字和 observation 是 Python 模板生成；LLM 只写受约束的自然语言层**——1 条 claim、≤2 条反证、≤3 条缺口、≤3 条资料，不得改动任何数字，每条必须绑 evidence_id。

你觉得"像 Python 脚本"，是因为**这个约束是对的，但 LLM 真正贡献的东西没被展示出来**。看今天这次运行 LLM 实际写了什么：

- "以经销为主、**先款后货**的结算方式或可解释应收与收入变动不同步" ← 这是**针对五粮液商业模式的公司特定反证**，脚本写不出来
- "应收**账面余额**与报表**净额**口径差异或影响增速可比性" ← 这是**真正的会计专业洞察**

**这两条就是你的"AI 灵魂"。** 它们现在混在一堆平铺文本里，第二条还被闸门毙了。

所以：**不要放松 LLM 约束去换"个性化"**。那等于亲手拆掉你唯一的护城河。要做的是把已有的反证假设**单独成块、显著展示、附上页码溯源链接**。

### 3.3 现在不要加规则

7 条规则（R1、R2 + 5 条行业规则）、约 13 个阈值，确实薄。但**今天下午加规则是自杀**：新阈值没有专业签字、没有测试覆盖、没有任何验证手段，而 `AGENTS.md §5` 明确禁止把未经真人冻结的东西当结论。

评委给分的逻辑是：**一个边界清楚、可追溯、会自我拒绝的窄系统 > 一个说不清阈值的宽系统。**
答辩时主动讲清范围（"R1/R2 + 5 条行业规则，聚焦销售与收款循环的增速错配族"），然后展示**深度**：证据绑定、数字闸门、三 Agent 对抗。

---

## 四、今天下午的执行方案（按优先级，可逐项砍）

### P0 · 前端停止说谎（约 25 分钟，必做）

**改点：** `assets/official-v4/demo-app.js`
- `degradedReason()`（1617-1632）：在 `evidence_incomplete` 之前增加分支——
  当 `run_completeness === "incomplete_numeric_claims"` 时返回 `{ kind: "numeric_gate_rejected", calls, unverified: run.context?.numeric_claim_trace?.key_unverified }`
- `DEGRADED_COPY`（1635-1660）：增加 `numeric_gate_rejected` 文案，说真话：

> **真实模型调用已完成 N 次，但 AI 草稿未通过数字可追溯闸门**
> 模型状态 model_success、3/3 角色完成、调用留痕齐备；草稿中有 X 个关键财务数字无法追溯到已登记来源，按闸门规则**不发布为完整结果**。未通过数字：41,813,685.32。确定性计算结果仍可查看。

**为什么必做：** 不改这里，视频里评委看到的就是"运行证据不完整、没有可核验的调用留痕"——一句关于自己系统的假话。
**验收：** 用 `RUN-V7-43AD8C03C3A7` 复现，页面显示新文案；用一次 `complete_public_prescreen` 运行验证仍显示 success，不误伤。

### P1 · 修闸门假阳性（约 50 分钟，强烈建议）

**改点：** `backend/app/numeric_gate.py` `_evidence_sources()`（113-131）
让 `rag_evidence` 行也贡献可追溯数字来源，从原文文本字段抽取数字，`source_ref` 用该行的 `evidence_id`，`source_type="rag_chunk"`。

**必须加的收紧条件（否则会真的放松控制）：**
只接受**当前 claim 的 `evidence_ids` 里实际绑定过的** chunk。即模型必须"引对页 + 绑对 ID"才能用该页数字，凭空写数字仍然被拒。
实现上需要把 claim→evidence_ids 的绑定关系传进闸门（`main.py:4297` 调用处已能拿到 `rule_result_dicts`）。

**为什么这是修正而非放松：** 闸门的书面边界是"关键财务数字**无来源**时禁止完整成功"。该数字**有来源**（第 89 页，已登记检索片段，模型已绑定 ID）。当前是**假阳性**，修它让控制更准，不是更松。

**需要你的专业裁决：** 这动的是专业控制项。按 `AGENTS.md §5`，闸门口径变更须由队长确认。
**验收：**
1. 新增单测：数字存在于已绑定 rag chunk → `traced`；数字存在于**未绑定** chunk → 仍 `unverified`；数字任何 chunk 都没有 → 仍 `unverified`。
2. 跑 `backend/tests/test_innovation_controls.py` 全绿。
3. 五粮液真实 live 跑一次，`run_completeness == complete_public_prescreen`，`numeric_claim_trace.passed == true`。

### P2 · 核查优先级分级 A1/A2/B1/B2/C/G（约 60 分钟，加分项）

**你要的分级可以做，但必须改名。**
"像证券评估那样 A1/A2/B1/B2/C1/C2"——**证券评估式评级 = 投资/信用评级，你自己的范围明确禁止**（不得出具审计意见或给投资建议）。
改成 **「核查优先级」**（审计计划阶段决定进一步审计程序的性质、时间安排和范围，这是准则内的正当语言）。

**只用已经算好的字段，零新增数学、零新增专业判断：**

| 轴 | 来源 | 取值 |
| --- | --- | --- |
| 幅度 | `risk_card.screening_strength`（`main.py:2327,2355`） | strong（≥30pp）/ standard（15–30pp）/ none |
| 持续性 | `metrics.sustained_periods`（`main.py:2297-2299`） | 2 = 连续两期均 ≥15pp / 1 = 仅当期 |

| 级别 | 条件 | 含义 |
| --- | --- | --- |
| **A1** | strong + sustained=2 | 强背离·连续两期 → 优先核查 |
| **A2** | strong + sustained=1 | 强背离·单期 → 优先核查 |
| **B1** | standard + sustained=2 | 中度背离·连续两期 → 重点核查 |
| **B2** | standard + sustained=1 | 中度背离·单期 → 重点核查 |
| **C** | `RULE_NOT_TRIGGERED` | 数据完整、未触发候选（**这是正面结论，不是"没结果"**） |
| **G** | `DATA_GAP` / `DATA_NOT_COMPARABLE` | 资料不足，**暂不评级**，出具索取清单 |

**五粮液 = strong（55.62pp）+ sustained_periods=1 → A2。**

**重要性不要折进级别。** 五粮液的 `materiality_assessment` 是"未评价金额重要性"，若把它当必要条件，最高级永远拿不到。建议作为**独立标注**并排显示（达到/低于/未评价），并留一条待查项：为什么 405 亿营收的案例重要性未评价（`main.py:2300-2311`）——这可能本身是个缺陷，但**今天下午不修，只如实显示"未评价"**。

**改点：**
1. `backend/app/main.py` R1 结果构造处（2351-2374）加纯函数算级别，写进 `risk_card`
2. `backend/app/schemas.py` 若升为一等字段需改 `RuleResult`（214-226）——**注意这会让已有的 `signoff_stale_requires_reapproval` 继续 stale，不会新增损害，但要知情**
3. `assets/official-v4/demo-app.js` 风险卡渲染加级别徽标（沿用现有字体/颜色/间距，不做视觉重构）
4. `backend/app/delivery.py`（369-374）待核查事项列表加级别列
5. 单测覆盖 A1/A2/B1/B2/C/G 六条路径 + 阈值边界（恰好 15pp、恰好 30pp）

**需要你的专业裁决：** 六档阈值口径由队长签字确认。

### P3 · 阻断字段仍被 R1 采用（根因已查实，约 40 分钟）

> **本节已更正。** 初版把它归因于"R1 的 `comparable` 守卫没拦住上年基数趋零"，那是错的。
> 实测字段值后确认：根因在**抽取质量阻断标记未被下游执行**，`comparable` 守卫只是症状被暴露的位置。

#### P3.1 现象

`growth_gap` 出现 **1.45e9（长江电力）、9.31e9（紫金矿业）、4.84e7（立讯精密）、2.38e9（万科A）、2.0e7（中国铁建）**。

#### P3.2 实测字段值（`backend/cache_seed.materialized.json`）

| 案例 | 本年应收 | **上年应收** | **前两年应收** |
| --- | --- | --- | --- |
| 长江电力 | 7,251,847,539.23 | **5.00 元** | 8,510,343,962.88 |
| 紫金矿业 | 9,308,395,310.0 | **1.00 元** | **4.00 元** |
| 立讯精密 | 48,438,796.0 | **1.00 元** | **1.00 元** |

紫金矿业 3490 亿营收，上年应收账款 = 1 元。这不是财务事实。

#### P3.3 系统自己已经诊断出来了

紫金矿业 `ACCOUNTS_RECEIVABLE_2023` 字段的原始登记：

```
value: 4.0
extraction_method:      "pdf_text_heuristic_candidate"
source_review_status:   "auto_extracted_pending_human_page_confirmation"
human_review:           {"status": "pending", "decision": null}
candidate_quality_status: "blocked_pending_human_confirmation"     ← 已标记阻断
candidate_quality_issues:
  "自动提取金额的原始值仅为 1元，疑似附注号、序号或叙述数字，须人工回页确认。"
  "同字段连续年度金额相差 9308395310.0 倍，可能存在错列、单位或表内子项误取，须人工回页确认。"
raw_excerpt: "553,119,452 | 729,421,153 | 应收账款 | 4 | 7,777,908,320 | 7,916,964,606"
```

抽取器把**附注序号「4」**当成了金额，真正的 `7,777,908,320` 就在它右边。
2024 年那条同理：`raw_excerpt` 是 `"应收账款 | （1） 应收账款分类列示"`，取到了「（1）」里的 1。

**检测层完全正确**：诊断精确、中文说明清楚、`blocked` 标记打对了。

#### P3.4 真正的根因：阻断标记无人执行

`candidate_quality_status` 在整个 `backend/app` 里：

```
写入：backend/app/cases.py:1885-1886      （有 issues → blocked_pending_human_confirmation）
      backend/app/cases.py:1930
读取：无。全量 grep 零命中消费者。
```

`candidate_quality_issues` 确实被传进 `prescreen_plan` 并在 `main.py:1624,2579,3546` 等处**展示**，
但它**不阻断计算**。实测证据：紫金矿业 `RUN-V7-2164293EAD03` 在上述字段已标 `blocked` 的情况下，
R1 仍输出 `status="candidate"`、`growth_gap=9308395308.85`。

而且这是**书面写明的设计决定**，不是疏漏。`backend/app/field_extraction.py:436` 的注释：

> 「公开预筛允许带着明确缺口继续运行；人工确认保留为正式采用/导出前的推荐动作。」

同文件 425-429 行的 `found_required` 已经正确排除了有质量问题的字段，
案例状态也正确降级为 `passed_technical_with_gaps`（437 行）——
**但 R1 拿到的仍是那个 `value=1.0`，照算，并把结果标成"强候选"。**

> 一句话：质量问题被*报告*了，没有被*执行*。

#### P3.5 影响面（实测）

```
50 个目录案例中含 blocked 字段者：30 个（60%）
15 个演示案例中含 blocked 应收字段者：6 个
    CNINFO_002475  立讯精密    blocked=3   AR 2023/2024/2025
    CNINFO_600028  中国石化    blocked=3   AR 2023/2024/2025
    CNINFO_600309  万华化学    blocked=3   AR 2023/2024/2025
    CNINFO_600900  长江电力    blocked=6   AR + allowance 各 3 年
    CNINFO_601857  中国石油    blocked=6   AR + AR_net 各 3 年
    CNINFO_601899  紫金矿业    blocked=5   AR 3 年 + allowance 2 年

五粮液 CNINFO_000858：blocked 字段数 = 0
    financial_fields_status = passed_technical_pending_human
    three_year_r1_ready     = True
```

**五粮液不只是运行历史最好，它是这批案例里抽取唯一干净的。** 这才是它能稳定给出 55.62pp 的原因。

#### P3.6 修复方案

**改点：** `backend/app/main.py` `_r1_result()`（2198-2375）与 `_r2_result()`（2378-2494）入口。
在读取字段后、计算增速前加一道守卫：

> 若本次计算所需的任一输入字段 `candidate_quality_status == "blocked_pending_human_confirmation"`，
> **不得返回 `status="candidate"`**，改为返回一个新状态 `DATA_BLOCKED_PENDING_HUMAN`，
> 并把该字段已登记的 `candidate_quality_issues` 原文透传到 `data_gaps` 与 `requested_materials`。

**为什么这是最小且正当的修改：**
- 不新增任何阈值，不发明任何专业判断——**只是执行系统自己已经做出的判断**
- 加强而非削弱 `AGENTS.md §5` 的人在回路边界
- 直接拆除"评委点开长江电力看到 14.5 亿百分点强候选"这颗雷
- `DATA_BLOCKED_PENDING_HUMAN` 会自然落入 `partial_prescreen`（`main.py:4042-4049` 已按 `DATA_GAP` 判定，需并列加入新状态），路由到 `evidence_gap_review`，出具资料清单——**这条链路已经存在，不需要新建**

**必须同时改的下游：**
1. `backend/app/schemas.py` 的 `RuleStatus` 枚举加入新状态
2. `main.py:3124-3142` `_select_ai_analysis_route()` 把新状态并入 gap 分支
3. `main.py:4042-4049` `partial_prescreen` 判定条件
4. `assets/official-v4/demo-app.js` 增加对应文案：**"抽取字段待人工回页确认，暂不评级"**（不要复用"资料不足"，两者事实不同）
5. `backend/app/delivery.py` 备忘录中如实区分

**必须诚实告知的代价：**
- 6 个演示案例会从（假的）`candidate` 变成 `DATA_BLOCKED_PENDING_HUMAN`，**可用演示面从 15 缩到 9**
- 你最烦的"资料不足"类结果**会变多**，不是变少
- 但这 6 个案例现在给的是**建立在系统自己已判定为垃圾的数据上的自信结论**——那比"资料不足"危险得多
- 会改变历史结论口径，439 次历史 candidate 命中中依赖 blocked 字段的部分需要重新验收

**验收：**
1. 单测：blocked 字段 → 不出 candidate；同案例人工确认后（`human_review.decision=accepted`）→ 恢复可出 candidate
2. 五粮液回归：blocked=0，必须仍是 `candidate` + gap 0.5562，**不得被误伤**
3. 长江电力/紫金矿业/立讯精密实测：不再出现 1e7 量级 `growth_gap`
4. 跑 `backend/tests/` 全量

#### P3.7 录制前的替代方案（若时间不够）

不改后端，仅在前端案例目录对 6 个受影响案例加禁用态 + 说明"抽取字段待人工回页确认"。
**这是遮蔽不是修复**，`AGENTS.md` 与 09-18 计划都明确"仅隐藏警告、屏蔽案例不算完成"，
只能作为录制当天的临时止血，之后必须回到 P3.6。

---

## 五、红线（今天下午绝对不做）

- ❌ 不加新规则、不改 15pp/30pp 冻结阈值
- ❌ 不放松 LLM 的 claim 数量与 evidence_id 绑定约束
- ❌ 不把 `incomplete_*` 直接改成 `success`（`AGENTS.md` 与 09-18 计划均禁止）
- ❌ 不把缓存回放讲成本次实时调用
- ❌ 不改 B0—B3 定义、不动人工评分与签字门禁
- ❌ 不做整体视觉重构，沿用现有设计系统

---

## 六、录制脚本建议（三幕，约 4 分钟）

**第一幕 · 提出质疑（30 秒）**
"把五粮液 2025 年报丢给 DeepSeek，它会给你一段流畅的话。我们无法验证其中任何一个数字。"

**第二幕 · 跑一次真实运行（2 分钟）**
选五粮液 → 完整分析 → 展示：
1. 确定性计算：收入 -54.55%、应收 +1.07%、**增速差 55.62pp，超强阈值 30pp → 核查优先级 A2**
2. RAG 溯源：每条结论点开是年报第几页第几段
3. 三 Agent 对抗：challenge / counter / review 各自 output_tokens 与 response_sha256
4. **AI 反证假设单独一块**：先款后货结算方式（第10页）、账面余额与净额口径差异（第89页）
5. 待索取资料清单：账龄明细表、期后回款记录、信用政策说明、合同关键条款

**第三幕 · 展示自我拒绝（1 分钟，这是全片高潮）**
如果 P1 没做完，或想主动展示控制强度：
"这次运行里，模型写了 41,813,685.32 这个数。系统**拒绝把它作为完整结果发布**，因为这个数字当时无法追溯到已登记来源。
通用大模型永远不会拒绝自己。我们把'不能证明的话不许说'写成了硬闸门——**这是我们和把年报丢给 DeepSeek 的根本区别。**"

> 若 P1 已完成，这一幕改为讲修好的溯源：同一个数字现在能追到第 89 页，闸门放行；而任何凭空数字仍然被拒。

**收尾逐字念：** "AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。"

---

## 七、需要你（队长）现在决定的四件事

> 更新：你在第一轮已选定 **P3 走根因修复**（而非遮蔽案例），实施范围待你看完方案再定。
> P3 的根因已从"`comparable` 守卫"更正为"阻断标记无人执行"，见 §四 P3.4，工作量由 15 分钟上修为约 40 分钟。

1. **是否授权实施 P0 + P1？**（约 75 分钟，是让视频能跑通的最小集合）
2. **P1 动的是专业控制项**——数字闸门口径从"仅结构化字段值"扩到"已绑定 evidence_id 的年报原文片段数字"。**你是否以队长身份确认这一口径？**
3. **P3 根因修复的代价你接受吗？** 6 个演示案例会从（假的）`candidate` 变成 `DATA_BLOCKED_PENDING_HUMAN`，可用演示面 **15 → 9**，你最烦的"资料不足"类结果**会变多**。
   换来的保证是：评委无论点哪个案例，都不会看到建立在系统自己已判定为垃圾的数据上的自信结论。
   **若不接受，只剩 P3.7 的遮蔽方案，而 `AGENTS.md` 明文"仅屏蔽案例不算完成"。**
4. **P2 分级**是否要做？六档阈值口径（幅度 × 持续性）你是否认可并签字？名称用「核查优先级」而非「风险评级」，你是否同意？

时间不够就砍顺序：**P0 → P3 → P1 → P2**。
P0 + P3 加起来约 65 分钟，能保证视频里既不出现假话、也不出现天文数字；P1 决定五粮液能否跑出完整成功态；P2 是加分项。

---

## 八、附：本次核验所用命令与文件

```
git rev-parse --short HEAD                      → e17e007
curl 127.0.0.1:8000/api/status                  → 服务在跑，模型 ready
backend/runtime/runs/*.json                     → 2342 个运行文件全量扫描
backend/runtime/runs/RUN-V7-43AD8C03C3A7.json   → 今天 13:13 失败样本
backend/runtime/runs/RUN-V7-684E88DF0FBC.json   → 长江电力清单不符证据
backend/app/numeric_gate.py:113-131,204-234     → 闸门实现
backend/app/main.py:4286-4312                   → 闸门调用与降级判定
assets/official-v4/demo-app.js:1592-1660        → 前端状态映射与文案
backend/competition_demo_cases.json             → 冻结清单（历史登记，已证不可信）
```

未核验项：五粮液 `materiality_assessment="未评价金额重要性"` 的成因（`main.py:2300-2311`）；
长江电力等脏数据的确切触发条件；P1 修改后对 439 次历史 candidate 命中的回归影响面。
