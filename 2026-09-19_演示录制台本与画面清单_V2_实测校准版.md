# 演示录制台本与画面清单 V2（实测校准版）

日期：2026-09-19。适用：审迹智链竞赛演示视频录制。
本文件**取代** `docs/2026-09-08_初赛五分钟视频录制台本与画面清单.md` 中的案例选择与口播事实部分；
那份的镜头节奏与自检清单结构仍可沿用。

所有数字、页码、处置与运行编号均取自 2026-09-19 当前 HEAD 的真实 `external_live` 运行，
逐条可回查 `outputs/w21-w23-live-verification-20260919/`。

AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。

---

## 零、录制前必须做的两件事（不做会当场出问题）

### 0.1 必须重启 `:8000` 本地服务

**当前 `:8000` 实例启动于 2026-09-19 12:56:59，而契约字段挂载代码 20:47–20:52 才落地。**
启动器默认**故意不带 `--reload`**（脚本注释：`Keep normal usage on one stable process so background tasks are not reloaded`），
所以那个进程冻结在旧代码上。

**对着它录制的后果**：屏幕上**不会出现**「审计关注优先级」「证据闭合状态」「金额重要性」三徽标，
也不会出现「综合审计关注」六区卡——因为它们全靠 `planning_priority` / `evidence_state` 等新字段渲染。

操作：关掉那个服务窗口，重新双击 `启动审迹智链.bat`。
自检（录制前必做，0 成本）：

```bash
# 期望 full_analysis_ready=true，且 engine_version=0.7.1
curl -s http://127.0.0.1:8000/api/status
```

然后在页面上跑一次五粮液，确认首屏出现「综合审计关注」卡与三枚状态徽标，再开始录。

### 0.2 必须现场真实重跑，禁止用回放

录制时**不要**用刷新恢复或历史结果代替现场运行。理由：
前端会如实显示「已验证历史结果回放 · 本次新增 0 次模型调用」，录进视频等于自曝没真跑；
`AGENTS.md §5` 也明确不得把缓存回放算作实时调用。

对照基线用已冻结的 Golden Demo Run（见 §一），**用来比对结果是否一致，不是用来代替运行**。

---

## 一、主演示案例：五粮液 2025（已冻结基线）

```
run_id        RUN-V7-901E91444479      task_id  DEMO-RUN-9ABA77BB1D68
git           remediation-2026-09-19 @ 13f25549f9b0
模式          external_live · cache_hit=False · 3 次 provider 调用
完整性        complete_public_prescreen
三轴          P2 优先核查 · E2 部分闭合 · retain 建议保留
确定性事实    营业收入增速 −54.55%   应收账款增速 +1.07%
              增速差 55.62 个百分点（阈值 15pp / 强阈值 30pp）→ strong
              持续期间 1 期          绝对变动 398,857.56 元
              金额重要性 未评价
闸门          passed=True  validation_mode=claim_scoped
              已追溯的年报原文来源 RAG-…-2025-P0089-C00（第 89 页）
反证          2 条（均 unverified_hypothesis）   待索取资料 3 项
原件          backend/runtime/runs/RUN-V7-901E91444479.json
              SHA-256 04F5E3E146042685…  224,226 字节
浏览器        4 视口实测：控制台错误 0 · 失败请求 0 · 横向溢出 0
```

**稳定性**：同一案例连续 3 次 fresh live，`growth_gap` 三次逐位相同（`0.55619794099056`），
三轴取值三次一致 `P2 / E2 / retain`，`run_completeness` 三次均为 `complete_public_prescreen`。
其中 2 次的 `analysis_conclusion` 是 `additional_procedure_required`、1 次是 `risk_candidate`——
**自然语言层允许不同，程序事实层必须一致**，这正是本系统的设计口径。

---

## 二、口播红线（照错就会和屏幕矛盾）

| # | 必须说 | 绝不能说 | 依据 |
| --- | --- | --- | --- |
| 1 | 五粮液的复核处置是 **`retain` 建议保留** | ~~defer~~、~~暂缓判断~~ | 3/3 次 live 全 retain |
| 2 | 角色名 **质疑 Agent / 反证 Agent / 复核 Agent** | ~~Arbiter~~、~~质检仲裁~~ | 界面无此角色 |
| 3 | `defer` 属于**标准股份、中国海油、长江电力** | 不要说"只有标准股份会 defer" | 三条 live 全 defer |
| 4 | **备用链下的五粮液也会 `defer`** | 不要承诺"任何时候都是 retain" | `RUN-V7-1FF5B1149044` 实测 |
| 5 | 长江电力是 **`DATA_GAP` → G 暂不分级** | ~~增速差 14.5 亿百分点~~ | live 实测 `growth_gap=null` |
| 6 | 「未形成程序候选**不等于企业无风险**」 | ~~无风险~~、~~风险评级~~、~~A1/B2/C1~~ | 中国海油运行里模型自己写的这句 |
| 7 | 「**审计关注优先级**」「**证据闭合状态**」「程序筛查信号分级」 | ~~企业风险等级~~ | 已签字 D1/D2 命名红线 |
| 8 | 反证两条都是「**待验证解释**」 | 不要把它们讲成已成立的事实 | `support_status=unverified_hypothesis` |
| 9 | 反确认表述：**「本系统将反向检索与反证记录设计成强制工作流，而不是依赖一次对话中模型是否主动想到」** | ~~通用大模型绝不会这样做~~ | 绝对化表述不可证 |

**收尾必须逐字念**（页面、报告、口播三处一致）：
> AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。

---

## 三、五幕台本（约 4 分 30 秒）

### 第一幕 · 提出质疑（30 秒）

画面：首页 → 案例选择区。

> "把五粮液 2025 年报丢给通用大模型，它会给你一段很流畅的话。
> 但你无法验证其中任何一个数字，也无法把它归档成审计底稿。
> 我们要解决的不是'让模型多说'，而是'让模型说的每句话都能被追问出处'。"

### 第二幕 · 真实运行（90 秒）

画面：选 **五粮液 → 启动完整分析 → 等真实调用**。
**镜头要停在过程上**：六阶段进度、三次 provider 调用、token 计数跳动。
这段等待本身就是证据——聊天机器人没有这个阶段台账。

出结果后依次点开：

1. **运行来源徽标**：「本次真实模型运行 · 3 次调用」
   > "先确认这是真跑，不是回放。这句话由本次运行留痕驱动，不由页面推断。"
2. **① 程序信号**：营收 −54.55%、应收 +1.07%、增速差 **55.62 个百分点**、强度 strong
   > "这三个数是确定性规则算出来的，标注写得很清楚：确定性计算，非模型生成。
   > 增速差超过我们 30 个百分点的强阈值。"
3. **点任一数字回链** → 年报 PDF 具体页码
   > "每一个数字都能一键回到年报第几页。"

### 第三幕 · AI 反证与自我克制（90 秒，全片核心）

画面：**② AI 反证与正常解释**区。

> "注意这里。反证 Agent 没有顺着'增速差大就是风险'往下说，它提出了两条**针对五粮液这家公司**的解释：
> 第一，白酒经销普遍是先款后货，收入下滑未必带来应收同步下降——这条绑定年报第 10 页；
> 第二，应收账面余额 41,813,685.32 元与报表净额 37,745,419.51 元存在口径差异，可能部分解释这个差——这条绑定第 89 页。"

然后**必须强调**：

> "但系统给这两条都打了'待验证解释'的标签，没有把它们当成结论。
> 复核 Agent 的判断是：程序事实和增速差有直接证据支持，但应收只有净额口径、账龄和期后回款缺失，
> **正常解释均为待验证假设，所以保留候选、不撤销、也不升级为风险认定**。"

画面切 **③ 复核 Agent 判断**：`复核处置 建议保留`。

> "这就是 retain——不是'没问题'，也不是'有问题'，而是'证据还不足以改变判断，所以维持候选，并说清要补什么'。
> 在审计计划阶段，这是唯一站得住的处置。"

### 第四幕 · 自我拒绝机制（45 秒，差异化最锋利的一段）

画面：⑥ 三徽标 + 展开数字轨迹，或用降级态运行展示闸门。

> "刚才那个 41,813,685.32，今天早些时候其实**毁掉过整次运行**。
> 系统的数字闸门当时判它'无法追溯到已登记来源'，于是即使三次真实模型调用全部成功、
> 三个角色全部完成，这次运行也不允许被发布成完整结果。
> 后来我们定位到是闸门读不到年报原文里的数字——修的是闸门的覆盖面，不是放松它的标准。
> 现在它能追溯到模型自己绑定的第 89 页片段，判定方式叫 `claim_scoped`：
> **每个主张只能用自己引用的那份证据，模型凭空写的数字照样被拒。**"

> "通用大模型不会拒绝自己。我们把'不能证明的话不许说'做成了硬闸门，
> 而且闸门失败时页面上写的是真话——哪个数字没过、为什么没过，都列出来。"

### 第五幕 · 层级区分与交付物（45 秒）

画面：切 **标准股份**（或直接展示已录好的对照结果）。

> "同一条链跑标准股份：R1 增速差 −13.20 个百分点，未触发；R2 因为经营现金流跨期变号，
> 系统判'口径不可比'，**拒绝计算伪同比**。结果是「暂不分级（资料/口径受限）」+「建议暂缓」。"

画面：切 **长江电力**。

> "长江电力更说明问题。它的年报自动抽取里，有字段的金额被抽成了 1 元、4 元、5 元——
> 那是年报里的附注序号。系统自己诊断出了这一点，把这些字段挡在规则计算之外，
> 所以它落到「资料缺口」而不是给出一个建立在垃圾数据上的自信结论。"

收尾：展示 **④ 什么证据能改变当前判断** 与 **⑤ 下一步审计程序 AP-01 / AP-03 / AP-06**。

> "最后交付的是三样东西：预审风险备忘录、待核查事项、进场前待索取资料清单。
> 每一样都写明了系统替你完成了什么、哪些判断仍然归人。"

**逐字念统一声明。**

---

## 四、画面清单（录制勾选）

| # | 画面 | 必须出现的内容 | 已实测 |
| --- | --- | --- | --- |
| S01 | 首页与案例选择 | 15 案、免责声明可见 | ✅ |
| S02 | 五粮液运行中 | 六阶段进度、真实调用 | ✅ 3/3 次成功 |
| S03 | 运行来源徽标 | 「本次真实模型运行 · 3 次调用」 | ✅ |
| S04 | ① 程序信号 | −54.55% / +1.07% / 55.62pp / strong / 1 期 | ✅ |
| S05 | 数字回链 | Evidence ID + 来源类型 + PDF 年度 + 页码 + 查看原文 | ✅ 第10/89/18页 |
| S06 | ② AI 反证 | 2 条，均带「待验证解释」标记 | ✅ |
| S07 | ③ 复核判断 | 建议保留 + reason 原文 | ✅ |
| S08 | ④ 什么能改变判断 | 待取得资料 + 未解决缺口 | ✅ |
| S09 | ⑤ 下一步程序 | AP-01/03/06，区分"系统已完成"与"人工保留" | ✅ |
| S10 | ⑥ 三徽标 | P2 优先核查 · E2 部分闭合 · 未评价金额重要性 + 两条边界句 | ✅ 4 视口 |
| S11 | 标准股份对照 | G 暂不分级 + 建议暂缓 + R2 口径不可比 | ✅ live |
| S12 | 长江电力对照 | DATA_GAP + 抽取诊断原文 | ✅ live |
| S13 | 导出 Word | 四行状态 + 级别列 + 统一声明 | ✅ 5/5 一致 |
| S14 | 收尾声明 | 逐字统一声明 | ✅ |

---

## 五、已知瑕疵（如实记录，不掩盖）

1. **④ 区存在近似重复条目**：「应收账款账龄明细表」与「账龄明细表」并列出现。
   来自复核输出与规则卡两个来源，去重按字面匹配未做语义归一。**属呈现层瑕疵，不影响结论。**
2. **`PROC-R1-*` 程序结果类证据无 PDF 页码**，其回链只能显示「原文入口未提供」。
   已如实呈现，未伪装成可回查。
3. **`cache_replay` 态未在真实浏览器中实测**。本机需 Supabase 缓存行 + 真人 `export_approved` 才可达。
   **该项不得计入验收通过**，四视口 × 五状态目前只覆盖到四状态。
4. **`:8000` 旧实例仍在运行**（12:56:59 启动）。不重启就录制，三徽标与六区卡不会出现。见 §0.1。
5. **`no_trigger_confirmed` 结构性不可达**（2342 次运行 0 命中）。
   根因是 R1 正常计算时也恒发 4 条常设资料缺口，路由选择器因此永不进入 `negative_confirmation`。
   队长裁决 R4 选 (a)：本轮只在呈现层兜底，**根治留到赛后**（删除该析取项会翻转 858 次运行的路线）。
   **因此不得在答辩中声称"系统能确认企业无风险"或"未触发已确认可达"。**

---

## 六、录制后自检（沿用 09-08 版清单，追加三项）

- [ ] 口播未出现 §二 表格右列任何一条禁用表述
- [ ] 三次运行画面均显示「本次真实模型运行」，无一次落到回放或备用链
- [ ] ⑥ 三徽标在视频分辨率下可读，且两条边界句完整入画
- [ ] 统一声明逐字出现且可读
- [ ] 若展示了标准股份/长江电力，口播说的是 G 暂不分级与资料缺口，**不是**"系统失败"
