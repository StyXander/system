# 2026-09-19 新修改计划完成度独立复核

复核对象：综合执行方案、口径签字与开工基线、W19—W25 执行计划，以及用户提供的 Qwen 执行结论。

代码基线：`remediation-2026-09-19`，HEAD `381a2bd`。本次只做复核和生成本报告，不修改业务源码，不合并、提交、推送、部署，不新增付费模型调用，不代签人工门禁。

**结论：核心功能与主要真实运行证据基本到位，但“计划全部执行完毕”不成立。尚有合回 main、浏览器缓存态、事实源与预算回填等收尾事项。** 不能把本轮工程进展等同于正式竞赛验收。

## 一、已独立核实的成果

| 项目 | 本次核实结果 | 证据及边界 |
|---|---|---|
| W01—W08 后端、契约与判定模块 | 对应源码、fixture、测试已落盘 | 新增数字闸门、优先级、证据闭合及 Word 合同测试；全量回归结果见下 |
| W09—W18 前端与诊断 | 主要实现及已有验收记录到位 | 本次重跑前端合同 75 项通过；另跳过 1 组本机产物向量。检查了历史验收措辞和抽取诊断读取路径；抽看一张手机端真实截图，未重新执行全浏览器矩阵 |
| W11 可达性审计 | 审计及本轮呈现方案已交付 | `docs/audit_no_trigger_reachability.md`；路由根治按裁决留待赛后，不能当成本轮漏做 |
| W19 三端一致性 | 实现和合同测试有依据；原始逐项比对留档不足 | Qwen 所述五条 live 的 Word 比对没有在本轮指定证据目录找到逐项结果文件；不能仅凭 PROJECT_STATUS 的自述独立重证 5/5 |
| W21 三次 fresh live | 已核实 | 直接读取三份 `backend/runtime/runs` 原件，均为 external_live、cache_hit=false、3 次调用、三角色 completed 且有响应哈希；闸门 passed、key_unverified=[]；P2/E2/retain 一致，growth_gap 均为 0.55619794099056 |
| W22 案例矩阵 | 四家公司记录真实；表述和替代依据需收尾 | 实际有三种 route、三种优先级、四组不同的优先级/证据/处置组合；不能称“四条不同路线” |
| W23 Golden Run | 工程冻结已核实 | 原件 224226 字节，SHA-256 与冻结记录完全一致；13f2554 至当前 HEAD 的 backend/app、assets、index.html 无变化，可复用其代码基线 |
| W24 浏览器验收 | 部分完成 | 新记录为三家公司 live 结果 × 四视口；旧轨 C 另有降级、备用等状态记录。cache_replay 仍未实测，不能宣布完整状态矩阵通过 |
| W25 录制台本 | 文件已交付 | 五幕结构、S01—S14、边界及已知问题在场；文档存在不等于已录制，也不自动等于人工批准对外表述 |

W21 原件编号：`RUN-V7-A923212497D0`、`RUN-V7-CD12FD56306C`、`RUN-V7-901E91444479`。

Golden 原件哈希：`04F5E3E146042685C69754FD15BF61F9084034DBB023BFD2C948494C42BF0AC0`。

## 二、必须更正或补齐的事项

### 1. 计划承诺的合回 main 未完成

当前分支为 `remediation-2026-09-19`，HEAD 为 `381a2bd`；main 仍在 `e17e007`，落后四个提交。执行计划“一、队长四项指示”及“合回 main”明确要求快进合并和合并后冒烟，当前状态不满足。

本次用户要求检查，故未擅自执行合并。收尾应包含合并、免费冒烟及更新基线，而不只是口头写“全部完成”。

### 2. W24 缓存回放状态仍未验收

`w24_live_acceptance.json` 的三个 state 实为五粮液 P2、标准股份 G、中国海油 S，均属真实模型结果。12 组记录能证明这些结果在四视口下的显示，不能替代 cache_replay 的独立状态验证。

后续执行计划已允许缓存态维持“未验、不计通过”，因此可以作为明确豁免的剩余项，但总评必须写“有保留完成”，不能写原方案全量验收通过。不得伪造真人 export_approved 来凑通过。

### 3. 付费台账漏掉旧实例诊断运行

原始文件另有 `RUN-V7-9F95459E6259`，文件时间为 21:41:05，external_live、provider_call_count=3、complete_public_prescreen，但无 planning_priority。它与 Qwen 描述的“先在旧 8000 运行、随后发现契约字段为空”相符。

因此，21:41—21:47 这段至少是 **7 次真实运行 / 21 次 provider 调用**；W21/W22 成功验收子集是 **6 次 / 18 次**。应分别登记，不能把后者作为整轮总消耗。

执行计划预算表仍为“待填”。其中“≤12 次、硬上限 15 次”没有统一写明完整链次数还是底层 provider 次数；任务行看起来按完整链计算。仅凭 21 次 provider 调用不能断言超支，应补明确切单位和全部诊断消耗。

### 4. W22 缺 P4 有批准的替代方案，不能误判

签字与开工基线 §7.5 明确允许：优先从历史 P4 对应案例选取；当前 HEAD 无稳定 P4 时改用 S，并区分“维持常规程序”与“暂缓判断”。所以不能仅因本轮没有 P4 就判定违约。

实际 route 为：五粮液 risk_candidate；标准股份 evidence_gap_review；中国海油与长江电力均为 industry_review。应改称“四家公司、三种技术路线、四组不同结果”。建议把 S 替代 P4 的依据和最终案例选择回填至矩阵；本次未找到新的逐案例真人批准记录。

### 5. W20 文档与事实源尚未统一

- PROJECT_STATUS.md 已追加当天结果，但 PROJECT_STATUS.json 未登记 09-19 改动，且相对 main 无变化。
- W19—W25 执行计划仍标“经批准后进入执行”，预算结果待填，未反映执行终态。
- PROJECT_STATUS.md 当天段落仍写“未提交”，但相关成果已有提交。
- 本次开始时仍有一份既有的未跟踪身份恢复文件，属于计划明确排除的文件。保留正确，但“工作树干净”字面不成立；准确说法是“相关已跟踪改动已提交，另有已排除的未跟踪文件”。本次未读取该文件内容。
- 注释率 10.56% 满足 ≥10% 的硬要求，但未达到 P0 写出的 ≥11.5% 目标；应如实区分达标与达到目标。

### 6. 录制环境与运行结果要分开判断

本次核实 8000 的 Python 进程仍启动于 12:56:59；8010 启动于 21:05:23。8000 属于契约挂载前的旧服务，录制新运行前仍应重启并自检。

但“旧实例下六区卡和徽标一定全部不出现”过于绝对：本次只读 GET 8000 上已保存的 Golden task，返回结果实际包含六个新字段。已有结果恢复与旧代码新建运行是两条不同路径。录制验收应以重启后的新建运行字段及页面为准。

## 三、本次独立验证

- 全量后端回归：586 passed、1 skipped、1 warning，148.97 秒，退出码 0。跳过项为未随源码分发的内部签字原件测试；不等于签字门禁已完成。
- 前端合同：75 项通过；1 组本机产物向量跳过，不能省略该边界。
- 中文注释：2813/26641 = 10.56%，通过 ≥10% 门槛。
- `git diff --check`：通过。
- 已跟踪文件秘密扫描：no_secret_hits；这不等于个人信息审查通过。
- Golden 文件哈希：通过；六份验收运行原件均存在。
- 浏览器证据：核读保存的 JSON，三个 live 结果各四视口，console_errors 与 failed_requests 均为空；本次仅抽看五粮液手机截图，没有声称重做全部浏览器验收。

首轮全量测试人为设置了 `AUDITTRACE_DEMO_USE_EXTERNAL_MODEL=false`，导致匿名模型调用限流测试走到备用路径，结果为 585 passed、1 failed、1 skipped。源码明确在该开关开启时才走相关限流。清除该额外设置、保持 API Key 为空后，失败单项通过，再重新执行全量；首轮失败不作为业务缺陷。

Qwen 对间歇测试的归因也应保持边界：“停掉 8011 后通过”是相关性证据，不能单独证明精确失效机制。源码中测试另有 pytest runtime namespace，相关用例也替换部分额度账本。当前材料不足以把共享台账干扰写成已经证明的唯一根因。

## 四、附带发现与明确排除

当前 Word 导出模板在 delivery.py 中仍为正文 Microsoft YaHei 9pt、主标题 17pt，未符合用户当前要求的“正文宋体小四、标题黑体三号”。这与本次新增字段是否正确是两个验收维度。本次只报告，未修改模板或重新交付 Word。

R1 重新签字、B0—B3 真人评分、候选字段回页确认、最终专业与发布批准继续开放，不应由 AI 自动补齐。W11 路由根治已明确延期，不混入本轮完成率。也没有证据支持把今日代码进展直接外推为 Render 已更新。

建议收尾顺序：先对齐分支并免费冒烟，再补机器可读状态、预算和 W19 比对留档；缓存态保留未验或按真实授权条件补验；录制前重启服务，现场创建 fresh live 并对照 Golden。完成这些动作后，再更新“完成”结论。

AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。
