# 口径签字记录 与 开工基线

日期：2026-09-19。
配套文件：`2026-09-19_综合执行方案_多AI并行.md`（W00-W25）。本记录是该方案 §2 契约的**签字生效版本**；两者冲突时以本记录为准。

AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。

---

## 一、本记录的性质与边界（先读这一段）

**本记录是什么**：项目队长对 2026-09-19 这一轮工程实施所需的三项专业口径与一项协作方式的决定。

**本记录不是什么**（逐条明示，防止被误当成正式门禁通过）：

- ❌ **不是 R1 规则签字**。`backend/release_records/r1_signoff_20260825_r2.json` 的
  `signoff_status = signoff_stale_requires_reapproval` 状态**不变**，仍需队长按 `AGENTS.md` 流程重新确认。
- ❌ **不是发布批准**。`release_gate.status = blocked`、`competition_release_ready = false` 状态不变。
- ❌ **不是 B0—B3 的任何一项**。B0 未执行；B1 `RUN-V7-0BDE5060FBED` 已执行；
  B2 `EVAL-B2-2F8BE053630D` 校验失败；历史 B3 `RUN-V7-00ED00962F34` 复核失败；人工评分全部空白。
  本轮工程改造**不得**被重新命名为 B0—B3 中的任何一档。
- ❌ **不是人工评分**，不填入 `backend/release_records/human_scores/`。
- ❌ **不写入 `backend/release_records/`**。本文件放在项目根目录，与本轮方案文档同级。

签字方式：队长在 2026-09-19 的协作会话中，对下列每一项从具体口径文本中**逐项点选确认**。
AI 只负责把口径拟成逐字可批的形式并记录队长的选择，**不代签、不代批、不推断未明示的同意**（`AGENTS.md §3`）。

---

## 二、已签字口径

### D1 · 审计关注优先级（六档）——已签字

签字人：项目队长（用户本人）　签字时间：2026-09-19　选项：修正命名版

| 级别 | 标签（逐字） | 判定条件 |
| --- | --- | --- |
| **G** | 暂不分级（资料/口径受限） | 任一被选用规则返回 `DATA_GAP` 或 `DATA_NOT_COMPARABLE` |
| **P1** | 立即扩大核查 | `screening_status=candidate` ∧ `screening_strength=strong` ∧ `sustained_periods≥2` ∧ `ai_recommendation=retain` |
| **P2** | 优先核查 | `candidate` ∧ `strong` ∧ 不满足 P1 |
| **P3** | 常规跟进 | `candidate` ∧ `standard` |
| **S** | 暂缓判断 | 非 candidate ∧ `ai_recommendation=defer` |
| **P4** | 维持常规程序 | `RULE_NOT_TRIGGERED` ∧ 数据完整 ∧ 无 blocked 行 |

**判定顺序（固定，不得调整）**：`G → P1 → P2 → P3 → S → P4`

**铁律**：
1. **数据缺口只能导致不出级（G），绝不能使优先级升级。**
2. `materiality_assessment` **不折入**级别，作为独立字段并排展示（达到 / 低于 / 未评价）。
3. **不引入任何新阈值**。仅可组合已冻结字段：`screening_status`、`screening_strength`、`sustained_periods`、`ai_recommendation`、`analysis_conclusion`、数据完整性状态。Opus 原案中的 `>0.08` **明确否决**。
4. `disposition`（retain / downgrade / defer）是**独立轴**，与 grade 并排显示，二者互不覆盖。
5. 级别只能由确定性纯函数产出，**不得由模型直接给出**。

**命名红线**：
- ✅ 允许：「审计关注优先级」「程序筛查信号分级」
- ❌ 禁止：「风险评级」「信用等级」「无风险」「企业风险等级」，以及证券评级代码 A1/A2/B1/B2/C1/C2
- ❌ 禁止 0-100 分、雷达图

**必须随级别一起显示的边界句（逐字）**：
> 本级别为审计计划阶段的程序筛查信号分级，不是审计认定，不构成审计结论或审计意见。

**实测归属（供验收比对，非预设结论）**：五粮液 → **P2**；标准股份 → **G**。

---

### D2 · 证据闭合状态（三档）——已签字

签字人：项目队长（用户本人）　签字时间：2026-09-19　选项：三档 E1/E2/E3

| 状态 | 标签（逐字） | 判定条件 |
| --- | --- | --- |
| **E1** | 已闭合 | `numeric_gate.passed=true` ∧ 所有 claim 的 `support_status=supported` ∧ 无未解决 `data_gaps` |
| **E2** | 部分闭合 | `numeric_gate.passed=true` ∧ 有直接证据，但存在 `unverified_hypothesis` 或待取得资料 |
| **E3** | 未闭合 | `numeric_gate.passed=false` ∨ 关键 claim 无 evidence 绑定 ∨ `evidence_fitness` 违规 |

**铁律**：
1. 输入**只能**来自现有证据控制项：Numeric Gate、`evidence_fitness`、coverage matrix、`claim.support_status`、未解决的 `data_gaps` / `requested_materials`。
2. **禁止让模型直接打 E1/E2/E3。**
3. 必须输出三个字段：`evidence_state`、`evidence_state_reasons`（逐条记录"哪个控制项返回了什么，因此判为此档"）、`evidence_state_boundary`。
4. 与 D1 的优先级**彼此独立**，不得合并成一个复合分数。

**实测归属（供验收比对）**：五粮液 → **E2**；标准股份 → **E3**。

---

### D4 · 数字闸门可追溯来源范围（专业控制项）——已签字

签字人：项目队长（用户本人）　签字时间：2026-09-19　选项：扩至已绑定 RAG 原文

**口径变更**：可追溯来源从「仅结构化 field/metric value」扩展为
「结构化 field/metric value **＋ 当前 claim 已实际绑定 `evidence_id` 的 RAG chunk 原文数字**」。

**这是修正假阳性，不是放松控制。** 实证依据：
`RUN-V7-43AD8C03C3A7` / `RUN-V7-BB86D15EEE1E` / `RUN-V7-D8933A6BBDDB`（2026-09-19 13:09/13:10/13:13）
三次运行的 `key_unverified` 均为 `41,813,685.32`；经实测该数字**确实存在于**模型所绑定的
`RAG-CNINFO_000858_T0_20260430-2025-P0089-C00`（2025 年报第 89 页）片段原文中，且 claim 的
`evidence_ids` 已包含该 ID。当前 `numeric_gate.py:113-131` 的 `_evidence_sources()`
只接受 `isinstance(row.get("value"), (int,float))`，而 `rag_evidence` 行的 `value` 恒为 `None`，故漏判。

**必须同时满足的收紧条件（缺一不可）**：
1. 采用 **claim-aware** 校验：`claim.text → claim.evidence_ids → 对应 evidence → number`。每段文本只允许用**该 claim 自己绑定的** evidence 构造来源池。
2. 数字存在于**未被该 claim 绑定**的 chunk 中 → 仍然 `unverified`。
3. 模型凭空写出的数字 → 仍然拒绝。
4. **严禁把所有 RAG 文本数字加入全局白名单。**
5. **保留 fail-closed 原则**：`key_unverified_count > 0` 时仍判 `incomplete_numeric_claims`，不得改为 success。
6. 结构化 field/metric value 的旧路径**不得被破坏**（W02 的 T4 用例即为回归保护）。

**验收硬门（W02 四类单测，缺一不可）**：
| | 场景 | 期望 |
| --- | --- | --- |
| T1 | 数字存在于 claim 已绑定的 RAG chunk | `traced`，`source_type="rag_chunk"` |
| T2 | 数字存在于某 RAG chunk，但该 claim 未绑定它 | `unverified_numeric_claim` |
| T3 | 数字在任何 evidence 中都不存在 | `unverified_numeric_claim`，`key_unverified_count>0`，`passed=false` |
| T4 | 数字来自结构化 field / metric value | 仍 `traced` |

**回滚条件**：出现任一"凭空数字被 traced"实例 → 立即回滚本项。

---

### D-ISO · 并行隔离方式——已签字

签字人：项目队长（用户本人）　签字时间：2026-09-19　选项：单一集成分支

**决定**：三轨在**同一工作副本、同一集成分支** `remediation-2026-09-19` 上工作，靠**严格文件所有权**避免冲突，不使用多分支或多 worktree。

**实测依据**：
```
git 跟踪文件：261 个          .git：62 MB
backend/runtime/：2.3 GB      ← 已被 .gitignore 排除，不进版本库
   cases/  1.3 GB（年报 PDF）
   rag/    340 MB（FAISS 索引）
   runs/   177 MB（2342 个运行记录）
```
worktree 只会得到 261 个跟踪文件，缺全部 2.3 GB runtime（PDF、索引、运行历史），
轨 A 的测试与轨 C 的验证会立即失败；复制或软链 runtime 又会让三轨同时写同一个 `runs/`，
反而制造冲突。多分支在同一工作副本中，任一 AI 执行 `git checkout` 即扰动其余两轨的未提交改动。

**Git 硬规则（对三个 AI 一律适用）**：
- 🚫 不得执行 `git checkout` / `git switch` / `git stash` / `git stash pop` / `git reset` / `git clean` / `git rebase`
- 🚫 不得执行 `git commit`、`git push`、部署（`AGENTS.md §6`）。提交由队长在检查点统一执行
- 🚫 不得改写 `backend/runtime/runs/` 下任何已存在的 JSON（历史证据，保全哈希）
- 🚫 不得改写 `backend/competition_demo_cases.json`、`backend/release_records/` 下任何文件（已冻结，受 `sha256_expected` 校验）
- ✅ 只允许写入下表所列本轨独占文件

---

## 三、文件所有权表（唯一权威，越界即停）

| 轨 | 独占可写文件 | 工作项 |
| --- | --- | --- |
| **轨 A** 后端控制链 | `backend/app/numeric_gate.py`<br>`backend/app/main.py`<br>`backend/app/schemas.py`<br>`backend/app/delivery.py`<br>`backend/tests/test_numeric_gate_claim_aware.py`（新增）<br>`docs/CONTRACT_2026-09-19.md`（新增） | W01 W02 W03 W04 W05<br>＋代轨 B 实现 `main.py` 挂载点<br>＋代轨 C 实现 W16 的后端透出（若需要） |
| **轨 B** 判定模块 | `backend/app/planning_priority.py`（新增）<br>`backend/app/evidence_closure.py`（新增）<br>`backend/tests/test_planning_priority.py`（新增）<br>`backend/tests/test_evidence_closure.py`（新增） | W06 W07 W08 |
| **轨 C** 前端与核验 | `assets/official-v4/demo-app.js`<br>`assets/official-v4/styles.css`<br>`index.html`<br>`scripts/verify_demo_outcome_judge.mjs`<br>`docs/audit_no_trigger_reachability.md`（新增）<br>`outputs/`（本轨本次产出目录） | W09 W10 W11 W12 W13 W14 W15<br>W16（前端部分）W17 W18 |
| **串行** 集成与验收 | 合并点 | W19 W20（串行）<br>W21 W22 W23 W24 W25（可并行） |

**轨 B 特别约束**：不得修改任何已存在文件，包括 `main.py`。挂载由轨 A 按契约实现。
若发现契约签名不可实现，**停下来报告，不要自行改 `main.py`**。

**轨 C 特别约束**：不得写入 `backend/` 下任何文件（含测试）。需要后端改动时写进报告的"未确认事项"，由轨 A 处理。

---

## 四、回滚基线

```
分支          remediation-2026-09-19（自 main 切出）
基线 commit   e17e00744d0e95633c9c962ec10a02ecba13a89b
回滚目标分支  main（同一 commit）
```

**开工时工作副本的 6 个未跟踪文件（须完整保留，不得 clean）**：
```
2026-09-18_Render两项异常_核因与修复执行计划.md
2026-09-19_三份AI建议评审与24小时改进计划.md
2026-09-19_录视频前突击修复与演示方案.md
2026-09-19_综合执行方案_多AI并行.md
2026-09-19_口径签字与开工基线.md            ← 本文件
docs/review-assets/2026-09-15_realname_identity_restore.json
opus修改建议 .md
```

**全局回滚触发条件**（任一即回滚到 `e17e007`）：
1. 任一历史 run JSON 读取失败
2. 出现"凭空数字被 traced"或"真实失败被显示为成功"任一实例
3. `backend/tests/` 全量出现无法当场解释的失败
4. 中文注释与独立说明性 docstring 占非空行比例跌破 10%（`scripts/check_chinese_comments.py` 为准）
5. 前后端字段语义分歧无法在集成期对齐

---

## 五、尚未签字的事项（不阻塞三轨开工，但阻塞对应工作项完成）

| # | 事项 | 阻塞的工作项 | 何时需要 |
| --- | --- | --- | --- |
| D3 | 确认 grade 与 disposition 为两条独立轴、并排显示、互不覆盖 | W13 ⑥区 | 轨 C 做 W13 前。**注：D1 铁律 4 已含此意，可视为已签，此处仅待正式确认** |
| D5 | `additional_procedure_required` 作为独立结论显示，不再改写为 `data_gap` | W10 | 轨 C 做 W10 前 |
| D6 | `no_trigger_confirmed` 结论二选一：(a) 设计上不由 AI 宣称无风险，前端仍表达「当前规则未形成需提升关注的程序信号，维持常规审计程序」；(b) 应当可达，批准最小修复 | W11 | W11 审计报告产出后 |
| D7 | `schemas.py` 变更导致 R1 signoff 继续 stale，是否接受并在赛后重新签字 | W04 | 轨 A 做 W04 前 |
| D8 | 主演示案例选定（`AGENTS.md §3/§5`：案例选定属人工决定） | W22 | 阶段 3 |
| D9 | 付费模型调用授权次数（建议 W21 三次 + W22 ≤六次 + W23 一次 ≈ 10 次） | W21 W22 W23 | 阶段 3 |
| D10 | 是否为 6 个 `DATA_GAP` 案例透传"抽取字段待人工回页确认"的具体诊断原文 | W16 | 轨 C 做 W16 前 |

> D5 与 D10 的建议默认值均为「是」；D3 已隐含在 D1 中。若队长无异议，可在开工后一次性追认。

---

## 六、三个 AI 的启动顺序

三轨**可同时启动**，无需等待 `docs/CONTRACT_2026-09-19.md` 正式化——
本记录 §二 与方案 §2 已构成可实现的契约。轨 A 的 W00 交付物只是把 §2 抄成正式文档。

建议启动顺序（若不能同时开三个）：
1. **轨 C 先启**：W17 / W18 / W11 是纯只读，零风险，且 W17 的结论决定是否需要额外的后端工作
2. **轨 B 次之**：两个新文件零冲突，契约已定，可完全独立推进
3. **轨 A 最后**：改动面最大、风险最高，且需要在前两轨的接口确定后挂载

三轨均不得自行发起付费模型调用；需要真实 run 时报告队长，由队长在阶段 3 统一授权（见 D9）。

---

## 七、追加裁定（2026-09-19 轨 A / 轨 B 交付后）

本节记录实施过程中出现的口径澄清。**§二 已签字的判定条件与判定顺序均未变更**；本节只更正一处我方标注错误、追认若干保守收紧、并指定若干归属。

### 7.1 更正：标准股份的 E 档标注错误（我方错误，非轨 B 错误）

§二 D2 末尾原写「实测归属（供验收比对）：标准股份 → **E3**」。**该标注错误，现更正为 E2。**

原因：那一行是我在任何人运行函数之前**凭推测写的预期值**，却放在"实测归属"标题下，措辞不当。
轨 B 用真实封存运行 `RUN-V7-BF8D84368D35` 实测：`numeric_claim_trace.passed=true`、3 条 claim 全部带
`evidence_ids`、`evidence_fitness_violations=[]`、覆盖矩阵 5 行含当前企业直接证据，
仅因 1 条 `unverified_hypothesis` 与 4 项未解决 `data_gaps`/`requested_materials` 而落 **E2**。

**裁定：E2 成立，逐字套用已签字 D2 即得此结果。不得为凑 E3 而新增触发条件。**
轨 B 提议的「被选用规则含 `DATA_NOT_COMPARABLE` ⇒ E3」**明确否决**，理由：
`DATA_NOT_COMPARABLE` 属于**口径受限**，已由 `grade = G` 承载；再让它触发 E3 就是把同一事实
在两个轴上重复计罚，且与 D1 铁律 4「grade 与 disposition/其他轴彼此独立」相矛盾。
这正是本方案 §九 否决 Opus `C2 = C1 + 严重数据缺口` 的同一类错误，不能自己再犯一次。

> 更正后的实测归属：**五粮液 → P2 / E2**（W01 落地后，已由 `RUN-V7-45BFA3297C39` 证实）；
> **标准股份 → G / E2**。两轴给出不同信息：G 说"口径不可比，无法定级"，E2 说"已有证据部分闭合"。
> 这正是双轴设计的价值，不是矛盾。

### 7.2 追认：D2 的两处保守收紧（轨 B 主动收紧，未放松任何条件）

| 收紧点 | 轨 B 的读法 | 裁定 |
| --- | --- | --- |
| 「关键 claim 无 evidence 绑定」中的"关键 claim" | 读作 `support_status=supported` 却无 `evidence_ids`；`unverified_hypothesis` 无证据不计入 | **追认**。已声明为待验证假设的内容本就不主张证据支持，把它算作 E3 触发条件会让 E3 失去区分度 |
| E1 的额外前置 | 除签字表三条件外，另要求 claims 非空、有直接证据、`requested_materials` 为空、`evidence_fitness` 控制项确已提供；任一缺失最高只到 E2/E3 | **追认**。方向是不夸大闭合性。副作用是 E1 会很少出现——这是诚实的结果，不是缺陷 |

### 7.3 追认：`evidence_state_boundary` 措辞（逐字）

D2 只规定"必须输出该字段"，未给原文。轨 B 按 D1 同族撰写，**逐字批准如下**：

> 证据闭合状态只描述本次运行的数字闸门、证据适配度、认定覆盖与主张绑定等控制项是否齐备，不是审计认定，不构成审计结论或审计意见。

### 7.4 裁定：D1 六档不穷尽时返回 `grade = null`

轨 B 实测残余量（2342 条历史运行普查）：

| 无法定级的输入 | 条数 | 成因 |
| --- | --- | --- |
| `candidate` 但 `screening_strength` 缺失 | 154 | R2 从不产出 `screening_strength`（仅 R1 在 `main.py` 写该字段） |
| `RULE_NOT_TRIGGERED` 且数据不完整 | 643 | 不满足 P4 的"数据完整"，也不满足 G 的规则状态条件 |
| `SOURCE_INCOMPLETE` | 1 | — |
| `screening_status` 为空（旧 schema） | 96 | — |

**裁定：采用轨 B 的方案 (a)** —— 返回 `grade = null` + 解释性 `label` + 完整 `factors`/`reasons`，
前端显示「未分级」并把 reasons 一并显示。**不得自创第七档。**

**方案 (b)（扩大 G 的触发条件）本轮否决**：那属于修改已签字的 D1 判定条件，需重新签字；
且 894 条全部是历史运行，当前演示案例（五粮液 P2、标准股份 G）均已可定级，本轮无收益。
记为赛后事项。

> 轨 A 挂载时须保证：`grade=null` 不得被序列化成空字符串或 `"None"` 字符串，
> 前端须能区分「未分级」与「G 暂不分级」——两者事实不同，前者是判据不足，后者是口径受限。

### 7.5 裁定：`S` 优先于 `P4` 是正确的，判定顺序不变

轨 B 报告：`RULE_NOT_TRIGGERED` + 数据完整的运行中，168 条复核建议为 `defer` 而先命中 S，
真正落 P4 的只有 36 条（`not_generated` 30 + `retain` 6）。

**裁定：顺序 `G→P1→P2→P3→S→P4` 保持不变。** 理由：复核 Agent 说"暂缓判断、先补证据"时，
不得对外表达成"维持常规程序即可"——后者会让审计师少做程序。S 遮蔽 P4 是**保守方向**，正确。

**对 W22 的影响**：Demo Matrix 第 ② 类「未触发 / 常规程序型」若要求严格落 P4，候选池仅 36 条历史运行。
**指示**：优先从这 36 条对应的案例里选；若当前 HEAD 无稳定 P4 案例，
则第 ② 类改用 S 案例，并在讲解中明确区分「维持常规程序」与「暂缓判断」两种不同处置——
这本身就是层级区分的有力证据，不是缺陷。

### 7.6 裁定：G 档标签以本签字记录为准

两份文档不一致：本记录 §二 为「暂不分级（资料/口径受限）」，方案 W00.1 为「资料或口径受限，暂不分级」。
**裁定：逐字采用「暂不分级（资料/口径受限）」。** 方案文档已同步更正。轨 C 渲染必须逐字一致。

### 7.7 裁定：`blocked_candidate_count` 改为直接传入，取消代理判据

轨 B 因签名内无该入参，用「被选用规则未落到 `DATA_GAP`/`DATA_NOT_COMPARABLE`/`SOURCE_INCOMPLETE`」
近似 P4 的"无 blocked 行"，并在 factors 标 `blocked_row_proxy`。
轨 A 已实测该计数**确在运行响应内**（`context.prescreen_plan.blocked_candidate_count`，三案实测 3/3/5）。

**裁定：取消代理判据。**
- 轨 B：`compute_planning_priority()` 增加**可选**关键字参数 `blocked_candidate_count: int | None = None`。
  传入时用它判定 P4 的"无 blocked 行"；为 `None` 时保留现有代理行为并继续标 `blocked_row_proxy`。
  签名保持向后兼容，其余五档判定不变。
- 轨 A：挂载时传入该计数。

### 7.8 裁定：`reason_for_status` 来源需实证等价

轨 B 取 `rule_results[].ai_draft.reason_for_status` 首条非空原文，而 W00.1 要求「复核 Agent 原文」。
**裁定：暂接受，但须实证。** 轨 A 在 W19 集成前，用真实 run 逐条比对
`rule_results[].ai_draft.reason_for_status` 与 `agent_steps[role=review].output.reason_for_status`：
- 若恒等 → 记录为已验证等价，结案
- 若可分歧 → 改为显式传入复核 Agent 原文，轨 B 增加可选入参

### 7.9 归属与协作规则补充

1. **`backend/tests/fixtures/run_contract_mock.json` 归属轨 A**（§三 文件所有权表漏列，现补）。
   轨 A 已用 403 条真实 run 的 `model_validate` 做了兼容性实测，可据此产出 fixture，也可继续以实测替代并在报告中注明。
2. **跨轨接口变更必须经队长与契约文件，禁止 AI 之间直接通知后各自改动。**
   本轮已发生一次：轨 B 通知轨 A"已收敛到契约键"，但轨 A 在 16:01 读盘发现
   `evidence_closure.py:309-310` 仍返回 `"evidence_state": state`。**以磁盘为准的判断是正确的**，
   轨 A 保留归一函数的决定予以确认。W19 集成时逐字核对键名后再决定是否删除归一分支。
3. **`execution_badge` 的唯一实现点是 `schemas.execution_badge_for()`**（轨 A 已建于 `schemas.py:291`）。
   **轨 C 不得在前端重新推导徽标三态**，必须直接消费后端产出的 `execution_badge.mode`。
   轨 A 已在 `main.py:2879`（external_cached）、`main.py:6050`（强制备用链续跑）、
   `delivery.py:184`（缓存回放）三处同步改写，前端只需读。
4. **W16 后端零改动即满足**（轨 A 实测）：`context.prescreen_plan.candidate_quality_issues`、
   `context.prescreen_summary.candidate_quality_issues`、`blocked_candidate_count` 均已在运行响应内。
   轨 C 直接读取，不需要轨 A 配合。
5. **`docs/` 被 gitignore**（`.gitignore:113` 为 `/docs/`）。`docs/CONTRACT_2026-09-19.md` 与
   `docs/audit_no_trigger_reachability.md` 不会出现在 `git status`，检查点提交须 `git add -f`。
6. **跑任何后端测试前必须置空 `DEEPSEEK_API_KEY`**（`main.py:274` 在导入时 `load_dotenv(.env)`，
   真 key 会进入环境并可能触发付费调用）。两轨均已全程带 `DEEPSEEK_API_KEY=` 执行。
   **轨 C 与集成期同样适用。**

### 7.10 待队长决定（不阻塞 W19 之前的工作）

| # | 事项 | 建议 |
| --- | --- | --- |
| D7 | `schemas.py` 已实际变更，R1 signoff 继续 `signoff_stale_requires_reapproval` | 接受现状，赛后重新签字；本轮不得声称 signoff 有效 |
| D11 | 草稿级叙述（`draft_title`/`draft_observation`/`reason_for_status`）无 evidence 绑定，轨 A 按最严口径只允许结构化来源 | **建议维持最严口径**。但须先回答轨 A 的完整性追问（见回复 A 第 1 条） |
| D12 | W05 的「级别列」按运行级 `planning_priority.grade` 实现（claim 无 per-claim 级别），`support_status` 另列 | **建议接受**。签字口径中不存在 per-claim 级别 |
| D13 | 闸门容差 `max(0.005, |值|×0.005)` 未动（对 4181 万级金额等于 ±20.9 万），chunk 来源同享该容差 | **建议不动**。改它即引入新阈值，违反 D1/D4 铁律 |
