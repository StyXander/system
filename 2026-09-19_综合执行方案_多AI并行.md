# 审迹智链 · 综合执行方案（多 AI 并行版）

日期：2026-09-19。状态：**执行计划，未实施任何代码改动**。

> ## ✅ 契约已签字生效，请改以签字记录为准
>
> **`2026-09-19_口径签字与开工基线.md`** 是本方案 §2 契约的**签字生效版本**。
> **两者冲突时以签字记录为准。** 已签字项：D1（审计关注优先级六档）、D2（证据闭合三档）、
> D4（数字闸门可追溯来源范围）、D-ISO（并行隔离方式）。
>
> 与本文件 §2 的**两处差异**，实施时按签字记录：
> 1. **W00.3 的中文标签已更名**（原采纳 GPT 命名，顺序含糊）：
>    `P1 优先核查 / P2 重点核查 / P3 一般核查 / P4 常规核查`
>    → **`P1 立即扩大核查 / P2 优先核查 / P3 常规跟进 / P4 维持常规程序`**。
>    级别代号、判定条件、判定顺序 `G→P1→P2→P3→S→P4` 均不变。
> 2. **W00 的产出物已就位**：契约不再需要单独冻结，签字记录 §二 即可直接实现。
>    `docs/CONTRACT_2026-09-19.md` 降为轨 A 的**应做**项（把签字记录 §二 抄成正式契约文档）。
>
> **并行方式已按实测更正为单一集成分支**（见签字记录 D-ISO），本文件 §1 中
> "三个独立 git 分支 / worktree" 的说法**作废**：仓库 `backend/runtime/` 有 2.3 GB 且被 gitignore，
> worktree 拿不到 PDF、RAG 索引与运行历史，轨 A 测试与轨 C 验证会立即失败。
>
> **当前分支**：`remediation-2026-09-19`　**回滚基线**：`e17e00744d0e95633c9c962ec10a02ecba13a89b`

本文件**取代**以下两份的实施方案部分（其证据链仍有效）：
- `2026-09-19_录视频前突击修复与演示方案.md`
- `2026-09-19_三份AI建议评审与24小时改进计划.md`

综合来源：我方两轮独立核验 + GPT 三轮建议 + Gemini 建议 + `opus修改建议 .md`。
时间预算：≥24 小时。执行方式：**3 个 AI 并行**，按文件所有权分轨。

AI生成内容，仅供审计计划阶段进一步核查，不构成审计结论或审计意见。

---

## 第 0 章 · 开工前必须知道的两处更正

### 0.1 撤回：F3「blocked 字段仍进入规则计算」不成立

我方前两份文件断言"`candidate_quality_status` 在 `backend/app` 内写入 2 处、读取 0 处，故阻断标记无人执行"。
**该断言错误，现予撤回。** GPT 提出的反驳经实测成立：

```
backend/app/cases.py:1934-1944
def calculation_ready_financial_rows(rows):
    """只返回未被拒绝且通过候选质量闸门的字段行。"""
    for row in annotate_financial_field_rows_quality(rows):
        if str(row.get("source_review_status") or "") == "human_rejected": continue
        if row["candidate_quality_issues"]: continue        ← 阻断在此执行
        ready.append(row)

调用点：cases.py:1973, cases.py:2064, main.py:1559, main.py:2524（导入 main.py:131）
```

阻断走的是 **`candidate_quality_issues`**，不是 `candidate_quality_status`。我方 grep 的是后者，据"搜不到"推断"未执行"——**这正是「grep 无命中 ≠ 功能缺失」的典型错误**。

实测佐证：

```
全库 growth_gap 绝对值 > 1000 的运行：53 次
  时间跨度：2026-08-10 10:12 ~ 2026-08-13 12:04
  2026-09-01 以后：0 次

6 个受影响案例 2026-09-08 的运行（长江电力/中国石化/万华化学/立讯精密 等）：
  rule_results[].status = ['DATA_GAP']
  metrics.growth_gap    = [None]
  run_completeness      = incomplete_calculation_only
```

**结论：天文数字 `growth_gap` 的缺陷在当前 HEAD 已不复现。** 原 F3「新增 `DATA_BLOCKED_PENDING_HUMAN` 状态」**不得实施**，改为只读复现验证（见 W17）。

**残余问题（降级为「应做」）**：这 6 个案例现在落到 `DATA_GAP` + `incomplete_calculation_only`，页面只给出泛化的"资料不足"，**没有说明真实原因是"自动抽取字段待人工回页确认"**。系统内部已有精确诊断原文（"自动提取金额的原始值仅为 1元，疑似附注号、序号或叙述数字，须人工回页确认"），但未透传到前端。这是**文案透传问题，不是状态机问题**，工作量 30 分钟级。

### 0.2 编号冲突已修正

前两份文件把"缺陷"和"关注等级"都编成 F1-F4，实施必串任务。本方案改为：

| 用途 | 编号 |
| --- | --- |
| 工作项 | **W00 ~ W27** |
| 缺陷（历史称谓，仅用于追溯） | D1 闸门误杀 / D2 前端假话 / D3 阻断（已撤回）/ D4 结论坍缩 |
| 审计关注优先级 | **P1 / P2 / P3 / P4 / S / G** |
| 证据闭合状态 | **E1 / E2 / E3** |

---

## 第 1 章 · 为什么不能按"前 1/3、中 1/3、后 1/3"切

你设想的三个 AI 各做 1/3 是**串行切分**，对本任务会产生两类致命冲突：

1. **文件争用**：`main.py` 被闸门改造、优先级挂载、证据状态挂载、`delivery.py` 同步四处同时触碰；`demo-app.js` 被状态分支、结论坍缩、关注卡、证据回链、来源徽标五处同时触碰。三个 AI 并行编辑同一文件必然互相覆盖。
2. **接口未定**：前端要渲染 `planning_priority.grade`，后端要产出它。若两侧各自开工，字段名/枚举值/嵌套结构会对不上，集成期返工。

**正确切法：契约先行 → 按文件所有权并行 → 集成串行 → 验收并行。**

```
阶段 0  契约冻结          1 个 AI，串行，约 40 分钟     ← 不可并行，是并行的前提
   ↓
阶段 1  三轨并行          3 个 AI，各自独占文件，约 5-7 小时
        轨 A 后端控制链    numeric_gate.py / main.py / schemas.py / delivery.py
        轨 B 判定模块      planning_priority.py（新）/ evidence_closure.py（新）
        轨 C 前端与核验    demo-app.js / styles.css / index.html + 全部只读核验
   ↓
阶段 2  集成              1 个 AI，串行，约 1.5 小时
   ↓
阶段 3  验收与录制基线    3 个 AI 可并行，约 4 小时
```

**文件所有权铁律**：每个文件只有一个轨可写。轨 B 需要挂载进 `main.py` 的部分，由**轨 A 按冻结契约代为实现**，轨 B 只交付纯函数 + 单测。轨 C 用契约里的 mock run JSON 开发，不等轨 A/B 完工。

---

## 第 2 章 · 阶段 0：契约冻结（串行，必做，阻塞后续全部）

### W00 · 冻结字段契约与模块接口

| 项 | 内容 |
| --- | --- |
| 修改目的 | 让三轨在无相互等待的前提下产出可集成的代码 |
| 当前源码证据 | `schemas.py:214-262`（`RuleResult` / `RunResponse`）、`demo-app.js:108-148`（`AUDIT_OVERVIEW_META` / `RECOMMENDATION_LABELS` / `ROUTE_CONCLUSION_FALLBACKS`）、`demo-app.js:794-848`（`deriveAuditOverview`）、`main.py:4286-4312`（闸门调用点） |
| 涉及文件 | **新增** `docs/CONTRACT_2026-09-19.md`、**新增** `backend/tests/fixtures/run_contract_mock.json` |
| 精确修改方案 | 见下方契约正文，逐字冻结 |
| 新增/修改字段 | 见下方 |
| 兼容性风险 | 低。新增字段全部可选，旧 run JSON 缺失时前端必须走"未提供"分支而非报错 |
| 单元测试 | fixture 能被 `RunResponse` 反序列化通过 |
| 集成测试 | 不适用（本项即集成前提） |
| 人工验收 | 队长逐字确认枚举值与中文标签 |
| 回滚条件 | 无需回滚（新增文档） |
| 完成标准 | 三轨负责人各自书面确认"按此契约可实现" |
| 标记 | **必做** · 改 schema=是 · 改专业口径=**是** · 需队长签字=**是** · 历史 run 失效=否 · 影响 release/signoff=**是**（signoff 已 stale，不新增损害但需知情） |

#### W00.1 后端产出字段（挂 `RunResponse`，全部 optional）

```jsonc
"planning_priority": {
  "grade": "P1|P2|P3|P4|S|G",
  "label": "优先核查|重点核查|一般核查|常规核查|暂缓判断|资料或口径受限，暂不分级",
  "factors": [ { "factor": "screening_strength", "value": "strong", "contribution": "≥30pp 强阈值" } ],
  "boundary": "本级别为审计计划阶段的程序筛查信号分级，不是审计认定，不构成审计结论或审计意见。"
},
"evidence_state": {
  "state": "E1|E2|E3",
  "label": "已闭合|部分闭合|未闭合",
  "reasons": ["..."],
  "boundary": "..."
},
"disposition": {                       // 与 grade 独立并排，不合并
  "ai_recommendation": "retain|downgrade|defer|not_applicable",
  "label": "建议保留|建议降级|建议暂缓|本角色不作建议",
  "reason_for_status": "复核 Agent 原文"
},
"materiality_display": {               // 独立展示，不折入 grade
  "assessment": "达到或超过计划重要性|低于计划重要性|未评价金额重要性",
  "multiple": null
},
"execution_badge": {
  "mode": "external_live|cache_replay|deterministic_backup",
  "label": "本次真实模型运行|已验证历史结果回放|确定性备用链",
  "provider_call_count": 3
},
"numeric_gate_summary": {
  "passed": false,
  "key_unverified": ["41,813,685.32"],
  "unverified_count": 3,
  "trace_count": 13
}
```

#### W00.2 模块接口（轨 B 交付纯函数，轨 A 负责在 `main.py` 挂载）

```python
# backend/app/planning_priority.py（新增）
def compute_planning_priority(*, rule_results, ai_recommendation,
                              analysis_conclusion, run_completeness,
                              screening_status) -> dict: ...
# 纯函数。禁止 I/O、禁止模型调用、禁止读取全局配置以外的状态。

# backend/app/evidence_closure.py（新增）
def compute_evidence_state(*, numeric_gate, evidence_fitness,
                           coverage_matrix, claims, data_gaps,
                           requested_materials) -> dict: ...
# 纯函数。禁止由模型直接给出 E1/E2/E3。
```

#### W00.3 分级判定口径（**需队长签字**）

优先级**唯一轴**是信号强度 × 持续性 × 复核处置。**数据缺口只能导致不出级，绝不能升级。**

| 级别 | 条件（全部取自已冻结字段，不引入任何新阈值） |
| --- | --- |
| **G** 暂不分级（资料/口径受限） | 任一被选用规则返回 `DATA_GAP` 或 `DATA_NOT_COMPARABLE` |
| **P1** 立即扩大核查 | `screening_status=candidate` ∧ `screening_strength=strong` ∧ `sustained_periods≥2` ∧ `ai_recommendation=retain` |
| **P2** 优先核查 | `candidate` ∧ `strong` ∧ 不满足 P1 |
| **P3** 常规跟进 | `candidate` ∧ `standard` |
| **S** 暂缓判断 | 非 candidate ∧ `ai_recommendation=defer` |
| **P4** 维持常规程序 | `RULE_NOT_TRIGGERED` ∧ 数据完整 ∧ 无 blocked 行 |

> **本表标签已按签字更正**（原采纳 GPT 命名，顺序含糊）。六档标签、`grade=null` 的处理、
> S 优先于 P4 的裁定、G 档标签的唯一权威版本，见 `2026-09-19_口径签字与开工基线.md` §七 追加裁定。

判定顺序：**G → P1 → P2 → P3 → S → P4**（G 最先，因为无法评级比暂缓更根本）。

`disposition` 是**独立轴**，与 grade 并排显示，二者不互相覆盖。实测归属：

| 案例 | grade | disposition |
| --- | --- | --- |
| 五粮液 | **P2**（candidate + strong + sustained=1 + retain） | **retain 建议保留** |
| 标准股份 | **G**（R2 = `DATA_NOT_COMPARABLE`） | **defer 建议暂缓** |

> 这正是 GPT 坚持"两个产品层状态彼此独立"的价值：标准股份既不是"没问题"也不是"高风险"，
> 它是**「口径受限，暂不分级」+「建议暂缓」**——两句话合起来才是完整、诚实、专业的判断。

#### W00.4 证据闭合口径（**需队长签字**）

| 状态 | 条件 |
| --- | --- |
| **E1 已闭合** | `numeric_gate.passed=true` ∧ 所有 claim 的 `support_status=supported` ∧ 无未解决 `data_gaps` |
| **E2 部分闭合** | `numeric_gate.passed=true` ∧ 有直接证据，但存在 `unverified_hypothesis` 或待取得资料 |
| **E3 未闭合** | `numeric_gate.passed=false` ∨ 关键 claim 无 evidence 绑定 ∨ `evidence_fitness` 违规 |

五粮液实测归属 **E2**（闸门修复后 passed；但 2 条 `normal_explanations` 均为 `unverified_hypothesis`，4 项 `data_gaps` 未解决）。

#### W00.5 命名红线

- ✅ 「审计关注优先级」「证据闭合状态」「程序筛查信号分级」
- ❌ 「风险评级」「信用等级」「无风险」「企业风险等级」
- ❌ 证券评级代码 A1/A2/B1/B2/C1/C2
- ❌ 0-100 分、雷达图

---

## 第 3 章 · 阶段 1 轨 A：后端控制链（独占 `numeric_gate.py` / `main.py` / `schemas.py` / `delivery.py`）

### W01 · Numeric Gate 升级为 claim-aware trace 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 消除 D1 假阳性：模型正确引用已绑定年报原文片段中的数字，却因闸门只读结构化 `value` 而被判"无来源" |
| 当前源码证据 | `numeric_gate.py:113-131` `_evidence_sources()` 仅接受 `isinstance(row.get("value"), (int,float))`；实测 `rag_evidence` 6 行的 `value` **全为 `None`**，数字在原文文本字段内。`numeric_gate.py:204-234` `validate_numeric_claims()`。调用点 `main.py:4297-4308`。实证：`RUN-V7-43AD8C03C3A7` / `BB86D15EEE1E` / `D8933A6BBDDB` 三次均因 `41,813,685.32` 失败，而我方已实测该数字确实存在于其绑定的 `RAG-CNINFO_000858_T0_20260430-2025-P0089-C00` 片段原文中 |
| 涉及文件/函数 | `backend/app/numeric_gate.py`：`_evidence_sources()`、`build_numeric_claim_trace()`、`validate_numeric_claims()`；`backend/app/main.py:4270-4312` |
| 精确修改方案 | ①`validate_numeric_claims()` / `build_numeric_claim_trace()` 新增入参 `claim_evidence_bindings: list[dict]`，形如 `[{"text": claim原文, "evidence_ids": [...]}]`；②新增 `_rag_chunk_sources(evidence_bundle, allowed_evidence_ids)`：从 `rag_evidence` 行的原文文本字段抽取数字，`source_type="rag_chunk"`、`source_ref=evidence_id`，**仅收录 `allowed_evidence_ids` 集合内的 chunk**；③逐 claim 分段校验：每段文本只允许用该 claim 自己绑定的 evidence 构造来源池，metric/结构化 field 来源仍全局可用；④`main.py:4270-4285` 组装 `numeric_text_parts` 时同步组装 bindings，不再把全部文本拼成一整段 |
| 新增/修改字段 | trace 行新增 `source_type="rag_chunk"`、`bound_by_claim: bool`；`numeric_gate_summary` 见 W00.1 |
| 兼容性风险 | **中**。分段校验比整段校验更严（来源池更小），可能让原本蒙对的数字变为 unverified。**严禁把所有 RAG 文本数字加入全局白名单**——那等于拆掉闸门 |
| 单元测试 | 见 W02 |
| 集成测试 | 五粮液 fresh live 一次，`numeric_claim_trace.passed=true` 且 `run_completeness=complete_public_prescreen` |
| 人工验收 | 打开 `RUN-V7-43AD8C03C3A7` 的输入重放，确认 `41,813,685.32` 变为 `traced` 且 `source` 指向 P0089 chunk |
| 回滚条件 | 若出现"凭空数字被 traced"任一实例 → 立即回滚本项 |
| 完成标准 | 4 类单测全绿 + 五粮液 live 通过 + 保留 fail-closed |
| 标记 | **必做** · 改 schema=是（trace 内字段）· 改专业口径=**是**（闸门可追溯来源范围）· 需队长签字=**是** · 历史 run 失效=**是**（历史 `incomplete_numeric_claims` 结论需重新解释，但**不得改写已封存 JSON**）· 影响 release/signoff=是 |

### W02 · Numeric Gate 四类单元测试 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 证明 W01 既修好假阳性、又没放松真阳性 |
| 当前源码证据 | `backend/tests/test_innovation_controls.py:15,71-` 已有 `validate_numeric_claims` 测试入口 |
| 涉及文件 | `backend/tests/test_numeric_gate_claim_aware.py`（新增） |
| 精确修改方案 | 四个用例，缺一不可：<br>**T1** 数字存在于 claim 已绑定的 RAG chunk → `traced`，`source_type="rag_chunk"`<br>**T2** 数字存在于某 RAG chunk，但该 claim **未**绑定它 → `unverified_numeric_claim`<br>**T3** 数字在任何 evidence 中都不存在 → `unverified_numeric_claim`，且 `key_unverified_count>0` → `passed=false`<br>**T4** 数字来自结构化 field/metric value → 仍 `traced`（回归保护，确保旧路径未破） |
| 新增/修改字段 | 无 |
| 兼容性风险 | 无 |
| 单元测试 | 本项即单测 |
| 集成测试 | 与 `test_innovation_controls.py` 一并跑 |
| 人工验收 | 四条断言逐条阅读，确认 T2 与 T3 的差别真的被覆盖 |
| 回滚条件 | 不适用 |
| 完成标准 | 4/4 通过，且 T4 在未改动 `_metric_sources` 前提下也通过 |
| 标记 | **必做** · schema=否 · 口径=否 · 签字=否 · 历史 run=否 · release=否 |

### W03 · `main.py` 挂载点与降级判定 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 把 W01 的 bindings、轨 B 的两个纯函数、W00 的字段全部接进运行响应 |
| 当前源码证据 | `main.py:4270-4285`（文本拼装）、`4286-4296`（`numeric_additional_sources`）、`4297-4308`（闸门调用）、`4310-4312`（`incomplete_numeric_claims` 判定）、`4313-4320`（`final_ai_draft`）、`3754-4131`（`run_completeness` 阶梯）、`4042-4049`（`partial_prescreen`） |
| 涉及文件/函数 | `backend/app/main.py`（**轨 A 独占**）；导入 `planning_priority`、`evidence_closure` |
| 精确修改方案 | ①`4270-4285` 改为按 claim 分段收集 `(text, evidence_ids)`；②`4297-4308` 传入 `claim_evidence_bindings`；③`4310-4312` 保留原判定**不变**（不得把 incomplete 改成 success）；④在 `RunResponse` 组装处调用两个纯函数，写入 `planning_priority` / `evidence_state` / `disposition` / `materiality_display` / `execution_badge` / `numeric_gate_summary`；⑤`execution_badge.mode` 取自 `execution_mode` 与 `model_check.cache_hit`，**判定逻辑必须与前端 `degradedReason()` 同源，不得各写一套** |
| 新增/修改字段 | W00.1 全部 |
| 兼容性风险 | **中**。旧 run JSON 无新字段；读取端必须容忍缺失。**不得为补字段而重算或改写已封存历史 run** |
| 单元测试 | 新字段在 `complete_public_prescreen` / `incomplete_numeric_claims` / `cache_replay` / `deterministic_backup` 四种 run 下均正确产出 |
| 集成测试 | `backend/tests/test_demo_run_tasks.py`、`test_v7_closure.py` |
| 人工验收 | 对同一 run 比对后端 JSON 与前端显示，逐字段一致 |
| 回滚条件 | 任一历史 run 读取报错 → 回滚 |
| 完成标准 | 四种 completeness 下字段齐全且语义正确 |
| 标记 | **必做** · schema=**是** · 口径=否（仅承载 W00 已签口径）· 签字=否（W00 已签）· 历史 run=否 · release=是 |

### W04 · `schemas.py` 字段定义 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 让新字段进入 Pydantic 合同，而不是塞进无类型 dict |
| 当前源码证据 | `schemas.py:15`（`RuleId = Literal["R1","R2"]`）、`73-81`（阈值）、`160-163`（`AgentClaim`）、`173-179`（`analysis_conclusion` 5 档枚举）、`180,190`（`status` / `ai_recommendation`）、`214-226`（`RuleResult`，其中 `risk_card: dict[str,Any]` 无类型）、`229-262`（`RunResponse`） |
| 涉及文件 | `backend/app/schemas.py` |
| 精确修改方案 | 新增 `PlanningPriority` / `EvidenceState` / `Disposition` / `MaterialityDisplay` / `ExecutionBadge` / `NumericGateSummary` 六个模型，全部字段 `Optional` 且带默认 `None`；挂到 `RunResponse`。**不改 `RuleId`，不加 R3/R4/R5，不改任何阈值默认值** |
| 新增/修改字段 | 见 W00.1 |
| 兼容性风险 | **中高**。`schemas.py` 变更会使 `signoff_status` 保持 `signoff_stale_requires_reapproval`（当前已是此状态，`reason: "R1 源文件 backend/app/schemas.py 已变化，需要项目队长重新确认"`） |
| 单元测试 | 反序列化 W00 的 mock fixture；旧 run JSON（无新字段）仍能反序列化 |
| 集成测试 | 全量 `backend/tests/` |
| 人工验收 | 队长确认六个模型的字段名与枚举逐字符合 W00 |
| 回滚条件 | 任一旧 run 无法读取 → 回滚 |
| 完成标准 | 新旧 run JSON 双向兼容 |
| 标记 | **必做** · schema=**是** · 口径=否 · 签字=**是**（schema 变更触发 R1 重新签字）· 历史 run=否 · release=**是** |

### W05 · `delivery.py` 备忘录同步 【应做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | Word 报告与页面结论一致，避免"网页一个说法、导出另一个说法" |
| 当前源码证据 | `delivery.py:246-524` `build_report()`；`352-361`（逐 rule 写 `card["title"]` / `card["observation"]` 与全部 metric）；`369-374`（Top 5 待核查事项平铺，无排序无级别）；`395`（`未形成通过硬校验的AI草稿`） |
| 涉及文件 | `backend/app/delivery.py` |
| 精确修改方案 | ①在备忘录抬头区加「审计关注优先级」「证据闭合状态」「处置」「重要性」四行并排；②`369-374` 的待核查事项列表增加级别列与 evidence_id/页码列；③`incomplete_numeric_claims` 时如实写明"真实模型调用已完成 N 次，但 X 个关键财务数字未通过可追溯闸门，故不发布为完整结果"，并列出未通过数字；④**逐字保留** AI 声明，不得改写为较弱版本 |
| 新增/修改字段 | 无（消费 W00 字段） |
| 兼容性风险 | 低 |
| 单元测试 | delivery/export 测试：四种 completeness 各导出一份，断言关键行存在 |
| 集成测试 | 与页面同一 run 导出，人工逐段比对 |
| 人工验收 | 打开 .docx 肉眼核对四行并排区 + 级别列 + 声明逐字 |
| 回滚条件 | 导出报错或声明被改写 → 回滚 |
| 完成标准 | 导出与页面结论零分歧 |
| 标记 | **应做** · schema=否 · 口径=否 · 签字=否 · 历史 run=否 · release=否 |

---

## 第 4 章 · 阶段 1 轨 B：判定模块（独占两个新文件，零冲突）

### W06 · `planning_priority.py` 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 把已计算但未展示的层级区分显性化，且**不引入任何新阈值** |
| 当前源码证据 | 输入全部已存在：`screening_strength`（`main.py:2327,2355`）、`sustained_periods`（`main.py:2297-2299`）、`materiality_assessment`/`materiality_multiple`（`main.py:2300-2311,2340-2341`）、`screening_status`、`ai_recommendation`（`schemas.py:190`）、`analysis_conclusion`（`schemas.py:173-179`）、规则 `status` 含 `DATA_GAP`/`DATA_NOT_COMPARABLE`（`main.py:2233-2274,2399-2419,2446-2473`） |
| 涉及文件 | **新增** `backend/app/planning_priority.py` |
| 精确修改方案 | 实现 W00.2 签名与 W00.3 判定表。纯函数，无 I/O、无模型调用。`factors` 逐条记录"哪个字段取了什么值、因此贡献什么"，使级别完全可追溯。**判定顺序必须为 G → P1 → P2 → P3 → S → P4** |
| 新增/修改字段 | 产出 W00.1 的 `planning_priority` |
| 兼容性风险 | 低（新文件） |
| 单元测试 | 见 W08 |
| 集成测试 | 由轨 A 在 W03 挂载后联调 |
| 人工验收 | 队长核对六档口径与判定顺序 |
| 回滚条件 | 不适用（新文件，未被引用即无影响） |
| 完成标准 | 六档全覆盖 + 边界值正确 + 缺口不升级 |
| 标记 | **必做** · schema=否（产出结构由 W04 定义）· 口径=**是** · 签字=**是**（W00 已含）· 历史 run=否 · release=否 |

### W07 · `evidence_closure.py` 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 给评委一句话可判的"证据到底闭没闭合"，且**不由模型打分** |
| 当前源码证据 | 输入全部已存在：`numeric_gate`（`numeric_gate.py:226-234` 返回 `passed`/`key_unverified`）、`evidence_fitness.py`（`annotate_evidence_bundle` / `enforce_claim_boundaries` / `fitness_map_for_evidence`，`main.py:185` 导入）、`coverage_matrix.py`（`build_assertion_evidence_procedure_matrix`，`main.py:184`）、`AgentClaim.support_status`（`schemas.py:160-163`，取值 `supported`/`unverified_hypothesis`）、`anti_confirmation.py`（`build_anti_confirmation_record`，`main.py:183`） |
| 涉及文件 | **新增** `backend/app/evidence_closure.py` |
| 精确修改方案 | 实现 W00.2 签名与 W00.4 判定表。纯函数。必须输出 `evidence_state` / `evidence_state_reasons`（逐条列出为什么是这个状态）/ `evidence_state_boundary`。**禁止让模型直接产出 E1/E2/E3** |
| 新增/修改字段 | 产出 W00.1 的 `evidence_state` |
| 兼容性风险 | 低 |
| 单元测试 | 见 W08 |
| 集成测试 | 与 W06 一并联调 |
| 人工验收 | 用五粮液真实 run 核对应为 E2，并逐条读 `reasons` |
| 回滚条件 | 不适用 |
| 完成标准 | 三档全覆盖 + reasons 可追溯到具体控制项 |
| 标记 | **必做** · schema=否 · 口径=**是** · 签字=**是**（W00 已含）· 历史 run=否 · release=否 |

### W08 · 两模块单元测试 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 锁定分级与证据状态逻辑，防止后续改动悄悄改口径 |
| 涉及文件 | **新增** `backend/tests/test_planning_priority.py`、`backend/tests/test_evidence_closure.py` |
| 精确修改方案 | **优先级用例**：P1（strong+sustained2+retain）、P2（strong+sustained1+retain）、P2（strong+downgrade）、P3（standard）、P4（未触发+数据完整）、S（defer+非candidate）、G（`DATA_GAP`）、G（`DATA_NOT_COMPARABLE`）；**关键反向用例**：数据缺口严重时**不得**升到 P1，必须落 G；**边界用例**：`growth_gap` 恰为 0.15、恰为 0.30 时的归档（沿用现有 `>=` 语义，`main.py:2326-2328`）。<br>**证据状态用例**：E1（闸门 passed + 全 supported + 无 gaps）、E2（passed + 有 `unverified_hypothesis`）、E2（passed + 有待取得资料）、E3（闸门 failed）、E3（关键 claim 无 evidence 绑定）、E3（`evidence_fitness` 违规） |
| 兼容性风险 | 无 |
| 人工验收 | 逐条读断言，确认"缺口不升级"这一条真的被测到 |
| 完成标准 | 全部通过，且新增用例覆盖 W00.3/W00.4 每一行 |
| 标记 | **必做** · 全部标记=否 |

---

## 第 5 章 · 阶段 1 轨 C：前端与只读核验（独占 `demo-app.js` / `styles.css` / `index.html`）

> 轨 C 前半段用 W00 的 mock fixture 开发，**不等轨 A/B 完工**。

### W09 · 修复 D2：前端对 `incomplete_numeric_claims` 说假话 【必做，最高优先】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 消除页面上关于系统自身状态的虚假陈述 |
| 当前源码证据 | `assets/` 全量搜索 `incomplete_numeric_claims` → **0 命中**。`demo-app.js:1592-1611` `outcomeFromRun()`：`completeness.startsWith("complete_")` 为假 → `degraded`；`1617-1632` `degradedReason()`：非 cache_replay、非 deterministic_backup、`modelStatus==="model_success"` → 落兜底 `{kind:"evidence_incomplete"}`；`1654-1659` 文案：**"模型状态登记为 model_success，但本次没有可核验的调用留痕"**。实证反例：`RUN-V7-43AD8C03C3A7` 有 `provider_call_count=3`、`input_sha256`/`response_sha256` 齐全、3/3 角色 `provider_call_performed=true` |
| 涉及文件/函数 | `demo-app.js`：`degradedReason()`、`DEGRADED_COPY` |
| 精确修改方案 | ①`degradedReason()` 在 `evidence_incomplete` 兜底**之前**插入分支：`run_completeness === "incomplete_numeric_claims"` → `{kind:"numeric_gate_rejected", calls, unverified: run.numeric_gate_summary?.key_unverified ?? run.context?.numeric_claim_trace?.key_unverified}`；②`DEGRADED_COPY` 新增 `numeric_gate_rejected` 四项文案（stage/pill/gateTitle/gateDetail），逐字采用：<br>**gateTitle**：「真实模型分析已完成，但未通过数字可追溯闸门」<br>**gateDetail**：「本次已完成 N 次真实模型调用、3/3 角色结构化输出通过 evidence_id 与禁用词校验；但 AI 草稿中有 X 个关键财务数字无法追溯到已登记来源，按闸门规则不发布为完整结果。未通过数字：…。确定性计算结果仍可查看。」<br>③**不得**把此状态显示为 success；④必须同时显示 `provider_call_count` 与 3/3 角色状态 |
| 新增/修改字段 | 消费 `numeric_gate_summary`，缺失时回退读 `context.numeric_claim_trace` |
| 兼容性风险 | 低。新增分支在兜底之前，不影响既有四类 |
| 单元测试 | `scripts/verify_demo_outcome_judge.mjs` 增用例：`incomplete_numeric_claims` + `model_success` → `numeric_gate_rejected`，**不得**落 `evidence_incomplete` |
| 集成测试 | 用 `RUN-V7-43AD8C03C3A7` 真实 JSON 驱动页面渲染 |
| 人工验收 | 页面文案与该 run 的真实留痕逐项对照，确认无一句假话 |
| 回滚条件 | 若任一真实失败被显示成成功 → 立即回滚 |
| 完成标准 | 该状态下页面所有陈述均可由 run JSON 逐条证实 |
| 标记 | **必做** · schema=否 · 口径=否 · 签字=否 · 历史 run=否 · release=否 |

### W10 · 修复 D4：停止把 `additional_procedure_required` 坍缩成 `data_gap` 【必做，性价比最高】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 这是"怎么最后又是资料不足"观感的**直接成因** |
| 当前源码证据 | `demo-app.js:831-833`：<br>`if (conclusion === "additional_procedure_required") { conclusion = dataGaps.length \|\| requestedMaterials.length ? "data_gap" : "risk_candidate"; }`<br>`AUDIT_OVERVIEW_META`（`108-133`）**无** `additional_procedure_required` 条目，只有 `risk_candidate`/`no_trigger_confirmed`/`data_gap`/`industry_boundary` 四条。<br>全库 2342 次运行实测：`additional_procedure_required` **107 次**、`no_trigger_confirmed` **0 次**。系统按设计总会产出 `requested_materials`，故 `dataGaps.length \|\| requestedMaterials.length` 几乎恒真 |
| 涉及文件/函数 | `demo-app.js`：`AUDIT_OVERVIEW_META`、`deriveAuditOverview()` |
| 精确修改方案 | ①**删除** `831-833` 的改写逻辑；②`AUDIT_OVERVIEW_META` 新增 `additional_procedure_required` 条目：`label: "需执行额外程序后判断"`、`tone` 取现有语义色（不复用 `gap`）、`state: "waiting"`、`procedureIds` 按实际依赖选取（不得照抄 `data_gap` 的 AP-02/04/06）；③资料索取清单渲染为该结论**下的下一步行动**，不得反向覆盖结论类别；④检查 `815` 行的 `explicitConclusion` 过滤条件已含 `additional_procedure_required`，确认新条目生效后该分支能正常命中 |
| 新增/修改字段 | 前端 META 新增一键 |
| 兼容性风险 | **中**。107 次历史运行的页面结论会从"资料不足"变为"需执行额外程序后判断"——这是**纠正**，但需在验收时确认不产生新的矛盾表述 |
| 单元测试 | 前端合同测试：构造 `additional_procedure_required` + 非空 `requested_materials` 的 run，断言 label 为新条目而非 `data_gap` |
| 集成测试 | 找一个真实 `additional_procedure_required` run 驱动页面 |
| 人工验收 | 对照 run JSON 与页面，确认结论类别与资料清单是**两层**而非互相覆盖 |
| 回滚条件 | 若出现结论与处置自相矛盾 → 回滚 |
| 完成标准 | `additional_procedure_required` 作为独立结论正确显示 |
| 标记 | **必做** · schema=否 · 口径=**是**（结论类别呈现口径）· 签字=**是** · 历史 run=否（不改写 JSON，仅改渲染）· release=否 |

### W11 · `no_trigger_confirmed` 可达性审计 【应做，只读优先】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 查清系统为何**从未能对任何公司说出"这家没问题"**（2342 次运行 0 命中） |
| 当前源码证据 | `schemas.py:173-179` 枚举含 `no_trigger_confirmed`；`demo-app.js:115-120` 有其 META 条目（label「暂未发现需提升关注的程序信号，维持常规核查」、`procedureIds: AP-01/02/04`）；`demo-app.js:142-147` `ROUTE_CONCLUSION_FALLBACKS.negative_confirmation = "no_trigger_confirmed"`；`main.py:3142` `return "negative_confirmation"`（选择器最后兜底）；`agents.py:145-154` 各 route 允许的 conclusion 集合 |
| 涉及文件 | **只读审计**，产出 `docs/audit_no_trigger_reachability.md` |
| 精确修改方案 | 逐步回答：①`main.py:3124-3142` 的 `negative_confirmation` 分支在什么条件下才可达？②`agents.py:145-154` 是否允许该 route 产出 `no_trigger_confirmed`？③`deriveAuditOverview` 的 `routeFallbackAllowed`（`817-819`）是否把它挡掉？④`838` 的 `valid` 判定是否要求 `completeRuleChain`，而 `RULE_NOT_TRIGGERED` 的运行常伴 `incomplete_calculation_only` 从而 `valid=false`？**不得为了让它出现而硬改结果** |
| 结论二选一 | (a) 若设计上不应由 AI 宣称"无风险"→ 前端仍须能表达「当前规则未形成需提升关注的程序信号，维持常规审计程序」，**绝不能写"无风险"**；(b) 若应当可达 → 提出最小修复方案，报队长批准后再改 |
| 兼容性风险 | 审计阶段为零 |
| 人工验收 | 队长阅读审计结论并选择 (a) 或 (b) |
| 完成标准 | 有明确因果链，指出是设计如此 / prompt 如此 / route 如此 / 前端聚合如此 |
| 标记 | **应做** · schema=否 · 口径=**是** · 签字=**是** · 历史 run=否 · release=否 |

### W12 · 运行来源徽标（execution badge） 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | live / cache_replay / deterministic_backup 三者在页面、报告、录屏中**永不混淆** |
| 当前源码证据 | `demo-app.js:1604-1605`（`cacheReplay` / `deterministicFallback` 判定）、`1617-1632`（`degradedReason`）、`1635-1660`（`DEGRADED_COPY` 已有三类文案）；`sourceBadgeForRun(run)` 已存在（`demo-app.js:844` 被引用） |
| 涉及文件 | `demo-app.js`、`styles.css` |
| 精确修改方案 | ①复用/扩展 `sourceBadgeForRun()`，产出 W00.1 的 `execution_badge` 三态；②徽标置于总览卡与阶段条**同屏可见**位置，明显但不喧宾夺主；③文案：`external_live`→「本次真实模型运行 · N 次调用」、`cache_replay`→「已验证历史结果回放 · 本次新增 0 次调用」、`deterministic_backup`→「确定性备用链 · 未调用外部模型」；④**不得仅靠颜色区分**（`AGENTS.md §7` 非颜色编码要求）；⑤导出报告同步 |
| 兼容性风险 | 低 |
| 单元测试 | 三态各一条断言 |
| 人工验收 | 四视口下徽标均可见、不遮挡、不溢出 |
| 完成标准 | 任一截图都能一眼分辨本次是否真实调用模型 |
| 标记 | **必做** · schema=否 · 口径=否 · 签字=否 · 历史 run=否 · release=否 |

### W13 · 综合审计关注卡（首屏） 【必做，产品差异化核心】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 把已存在但被压平在二三屏的判断，变成评委运行完成后**第一眼看到的东西**；不新增任何 AI 判断 |
| 当前源码证据 | 全部数据已存在：`risk_card.observation` 与 `metrics`（`main.py:2329-2374`）、Counter 的 `normal_explanations` + `support_status`（`schemas.py:160-163`）、Review 的 `ai_recommendation`/`reason_for_status`（实测 `RUN-V7-43AD8C03C3A7`）、`requested_materials`/`data_gaps`、`AUDIT_OVERVIEW_META.procedureIds`（`demo-app.js:113,119,125,131`）、`AGENT_ROLE_META`（`135-139`） |
| 涉及文件 | `index.html`、`demo-app.js`、`styles.css` |
| 精确修改方案 | 卡片六区，顺序固定：<br>**① 程序信号** — 触发规则、revenue growth、AR growth、growth gap、`screening_strength`、`sustained_periods`（全部来自确定性计算，标注"确定性计算，非模型生成"）<br>**② AI 反证与正常解释** — 直接读 Counter 的 `normal_explanations`，**每条必须显示 `support_status`**；`unverified_hypothesis` 醒目标为「待验证解释」，**不得显示为事实**<br>**③ Review 判断** — `ai_recommendation`（用 `RECOMMENDATION_LABELS` 真实文案）、`analysis_conclusion`、`reason_for_status` 原文<br>**④ 什么证据能改变当前判断** — 由 `requested_materials + data_gaps` 组织；**不新增模型调用、不生成凭空建议**<br>**⑤ 下一步审计程序** — AP-01~AP-06 中对应项**紧邻判断显示**，不再埋在二三屏<br>**⑥ 三个并排徽标** — 审计关注优先级（P*/S/G）· 证据闭合状态（E1/E2/E3）· 金额重要性（达到/低于/未评价）<br>视觉：沿用现有 token 分层与既有组件形态，**不做视觉重构**，不引入紫色渐变/发光球体/玻璃拟态/悬浮圆角卡片 |
| 新增/修改字段 | 消费 W00 全部字段 |
| 兼容性风险 | 中。首屏信息密度上升，移动端易溢出 |
| 单元测试 | 前端合同测试：给定 mock run，断言六区均渲染且 `unverified_hypothesis` 带「待验证解释」标记 |
| 集成测试 | 五粮液 + 标准股份两个真实 run 各渲染一次 |
| 人工验收 | 四视口逐一目视；确认 ② 区不会把假设读成事实 |
| 回滚条件 | 移动端无法在不横向滚动前提下读完 → 调整而非放弃 |
| 完成标准 | 首屏即可回答"信号是什么/AI 怎么反驳/系统怎么判/什么能改变判断/下一步做什么" |
| 标记 | **必做** · schema=否 · 口径=否（呈现层）· 签字=否 · 历史 run=否 · release=否 |

### W14 · 逐条 AI insight 证据回链 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 把"可回查"从个别案例的巧合变成**产品级硬要求** |
| 当前源码证据 | `AgentClaim` 含 `evidence_ids`（`schemas.py:160-163`）；`agents.py:542,636` 强制 claim 绑定白名单 evidence_id；`evidence_bundle` 含 `field_evidence`/`rag_evidence`，后者带 `pdf_page`/`source_file`/`locator`（实测 `RAG-CNINFO_000858_T0_20260430-2025-P0089-C00`）；`enforce_claim_boundaries`（`evidence_fitness.py`）已把越界 claim 标为 `unverified_hypothesis` |
| 涉及文件 | `demo-app.js`、`styles.css` |
| 精确修改方案 | 对前端展示的**每一条** Challenge claim / Counter explanation / Review claim / 最终 observation：凡带 `evidence_ids`，必须显示 ≥1 个清晰的【Evidence ID · 来源类型 · PDF 年度 · PDF 页码 · "查看原文"入口】。**无 `evidence_id` 的内容必须显式标注证据状态，不得伪装成已核实事实** |
| 兼容性风险 | 低 |
| 单元测试 | 构造一条无 evidence_id 的 claim，断言页面显示"证据状态未提供"而非静默渲染 |
| 人工验收 | 随机抽 5 条 insight，逐条点"查看原文"确认真的能定位到页 |
| 完成标准 | 页面上不存在"看起来像事实但无法回查"的句子 |
| 标记 | **必做** · schema=否 · 口径=否 · 签字=否 · 历史 run=否 · release=否 |

### W15 · 护城河模块前移 【应做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 把真正的差异化从深层详情提到一级展开区 |
| 当前源码证据 | `coverage_matrix.py` `build_assertion_evidence_procedure_matrix`（`main.py:184`）、`evidence_fitness.py` `fitness_map_for_evidence`（`main.py:185`）、`anti_confirmation.py` `build_anti_confirmation_record`（`main.py:183`）；`demo-app.js:149` `COLLAPSIBLE_SECTIONS` |
| 涉及文件 | `demo-app.js`、`index.html`、`styles.css` |
| 精确修改方案 | 三者从深层提到首屏/一级展开。**anti-confirmation 必须突出**，但表述逐字采用：<br>✅「本系统将反向检索与反证记录设计成强制工作流，而不是依赖一次对话中模型是否主动想到。」<br>❌ 禁止「通用大模型绝不会这样做」这类绝对表述 |
| 兼容性风险 | 低 |
| 人工验收 | 四视口目视 + 确认无绝对化宣传语 |
| 完成标准 | 三个模块在一级展开内可见 |
| 标记 | **应做** · 全部标记=否 |

### W16 · 阻断原因文案透传（原 F3 残余） 【应做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 6 个受影响案例现在正确落到 `DATA_GAP`，但页面只说泛化"资料不足"，未说明真实原因 |
| 当前源码证据 | `cases.py:1934-1944` 已过滤 blocked 行；系统内部已有精确诊断原文（实测 seed：「自动提取金额的原始值仅为 1元，疑似附注号、序号或叙述数字，须人工回页确认」「同字段连续年度金额相差 9308395310.0 倍，可能存在错列、单位或表内子项误取，须人工回页确认」）；`candidate_quality_issues` 已进入 `prescreen_plan`（`main.py:1624,2579,3546`）与 `field_extraction.py:419-424` |
| 涉及文件 | `demo-app.js`（若后端未透出则加 `main.py`，**需与轨 A 协调，由轨 A 执行**） |
| 精确修改方案 | 当规则返回 `DATA_GAP` 且 `prescreen_plan.candidate_quality_issues` 非空时，页面显示「自动抽取字段待人工回页确认，暂不评级」并**列出诊断原文**；**不得复用泛化的"资料不足"文案**（两者事实不同） |
| 兼容性风险 | 低 |
| 人工验收 | 长江电力/紫金矿业/立讯精密 三案页面均显示具体诊断而非泛化文案 |
| 完成标准 | 用户能看懂"为什么这个案例算不出来" |
| 标记 | **应做** · schema=否 · 口径=否 · 签字=否 · 历史 run=否 · release=否 |

### W17 · 【只读】blocked 字段门禁当前 HEAD 复现验证 【必做，先于任何状态机改动】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 确认 D3 是否已在当前 HEAD 修复，**避免花数小时修一个已修好的问题** |
| 当前源码证据 | 已初步确认：`cases.py:1934-1944` 过滤 `candidate_quality_issues`；调用点 `main.py:1559,2524`、`cases.py:1973,2064`；全库 53 次天文 `growth_gap` 全部落在 2026-08-10~08-13，09 月 0 次；6 案例 09-08 运行均为 `DATA_GAP`/`gap=None` |
| 涉及文件 | **零代码改动**。产出 `outputs/blocked-field-verification-<时间>/report.md` |
| 精确修改方案 | 在当前 HEAD 分别重跑紫金矿业 / 长江电力 / 立讯精密（**确定性计算即可，无需付费模型调用**），验证：①R1/R2 实际输入不含 blocked row；②不再出现 1e7/1e9 量级 `growth_gap`；③`prescreen_plan.blocked_candidate_count` 与实际过滤数一致（若该字段不存在则记录为"字段缺失"，不得假称一致） |
| 分支决策 | **只有当前 HEAD 仍能复现 blocked 字段参与计算，才设计新状态或后端守卫。** 若不复现（预期如此）→ 关闭 D3，仅保留 W16 文案透传 |
| 兼容性风险 | 零 |
| 人工验收 | 三案运行 JSON 逐项核对 |
| 完成标准 | 书面结论：D3 在当前 HEAD 复现 / 不复现，附证据路径 |
| 标记 | **必做** · 全部标记=否 |

### W18 · 【只读】`historical_external_model_run` 语义修正 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 防止再次用历史记录冒充当前行为保证 |
| 当前源码证据 | 实测反例：`competition_demo_cases.json` 称长江电力 `risk_candidate`/`complete_public_prescreen`，而 `RUN-V7-684E88DF0FBC.json` 实为 `status=DATA_GAP`/`route=industry_review`/`complete_public_prescreen_with_gaps`。该文件 `notes` 已自述「`demo_focus` 只说明讲解重点，不预写任何运行结论」。加载点 `demo_bootstrap.py:19`、`main.py:4595` |
| 涉及文件 | 界面/文档措辞（`index.html`、`demo-app.js` 若展示该字段、README） |
| 精确修改方案 | 统一改称 **「历史验收运行」**，并附一句边界：「仅作为传输、Schema 与留痕技术证据，不保证当前 HEAD 行为」。**选主演示案例必须以当前 HEAD 的 fresh run 为准** |
| 兼容性风险 | 零。**不得改写 `competition_demo_cases.json` 本身**（已冻结，`sha256_expected` 受 `release_records` 校验） |
| 人工验收 | 页面与文档中不再出现暗示"保证当前行为"的表述 |
| 完成标准 | 术语统一 |
| 标记 | **必做** · schema=否 · 口径=否 · 签字=否 · 历史 run=否 · release=否（**严禁改动冻结清单**） |

---

## 第 6 章 · 阶段 2：集成（串行，1 个 AI）

### W19 · 三轨合并与字段联调 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 确认三轨产出在真实 run 上端到端一致 |
| 精确修改方案 | ①合并三轨改动，逐文件确认无越权修改（轨 B 不得改 `main.py`，轨 C 不得改后端）；②用同一次五粮液 fresh live run，逐字段比对「后端 JSON → 前端渲染 → 导出 .docx」三处一致；③检查新字段缺失时（旧 run JSON）三端均优雅降级 |
| 兼容性风险 | **高**（合并点） |
| 回滚条件 | 任一历史 run 读取失败、或前后端字段语义分歧无法当场对齐 → 回滚到合并前 commit |
| 完成标准 | 三端零分歧 |
| 标记 | **必做** · schema=否 · 口径=否 · 签字=否 · 历史 run=否 · release=否 |

### W20 · 分层测试执行 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 先针对性、后全量，避免批量脚本意外触发付费模型调用 |
| 当前源码证据 | 已知存在：`scripts/verify_demo_outcome_judge.mjs`、`backend/tests/test_demo_run_tasks.py`、`test_demo_bootstrap.py`、`test_batch3_cache_manifest.py`、`test_v7_closure.py`、`test_innovation_controls.py`、`scripts/check_chinese_comments.py` |
| 精确修改方案 | **先跑针对性集合，逐项记录命令与退出码**：<br>1. Numeric Gate targeted（W02）<br>2. 前端 status/contract（`verify_demo_outcome_judge.mjs` + W09/W10 新增）<br>3. innovation controls（`test_innovation_controls.py`）<br>4. competition demo contract<br>5. case field quality / extraction<br>6. delivery / export（W05）<br>7. `planning_priority` / `evidence_closure`（W08）<br>**再跑全量**：<br>8. `backend/tests/` 全量<br>9. `git diff --check`<br>10. `scripts/check_chinese_comments.py`（`backend/app` 中文注释与独立说明性 docstring 占非空行 **≥10%**）<br>**执行前必须先读脚本参数，确认不会批量触发付费模型调用** |
| 完成标准 | 全部通过；失败项逐条给出真实原因，**不得引用历史通过数充当本次结果** |
| 标记 | **必做** · 全部标记=否 |

---

## 第 7 章 · 阶段 3：验收与录制基线（可 3 AI 并行）

### W21 · 五粮液连续 3 次 fresh external_live 稳定性 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 证明 W01 修好后五粮液**稳定**通过，而非偶然 |
| 精确修改方案 | 连续执行 **3 次新的 external_live 完整分析，不得使用 cache replay**。每次记录：`run_id`、`execution_mode`、`cache_hit`、`provider_call_count`、challenge/counter/review 三角色状态、`ai_recommendation`、`analysis_conclusion`、`numeric_claim_trace.passed`、`run_completeness`、`duration_ms`、`input_tokens`/`output_tokens` |
| 验收判据 | **不要求三次自然语言输出完全一致**；要求核心程序事实稳定：`growth_gap ≈ 0.5562`、`screening_strength=strong`、证据绑定成功、`numeric_claim_trace.passed=true`、`run_completeness=complete_public_prescreen`、`ai_recommendation=retain` |
| 参考基线 | 历史 35 次 live 中 30 次为 `complete_public_prescreen`；单次 `duration_ms` 实测 80777（`RUN-V7-684E88DF0FBC`） |
| 失败处置 | 若 3 次中仍有 `incomplete_numeric_claims`，**记录未通过的具体数字并回到 W01**，不得放宽闸门 |
| 标记 | **必做** · 全部标记=否 · **需队长授权付费调用（3 次）** |

### W22 · 重建 Demo Case Matrix（≥4 类真实路线） 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 不再只靠一个案例；用当前 HEAD 的 fresh run 证明系统对不同公司给出**不同**判断 |
| 精确修改方案 | 基于当前 HEAD **新鲜运行**选出至少四类：<br>①`risk_candidate` / retain 型 —— 首选五粮液<br>②未触发 / 常规程序型（P4）<br>③`industry_boundary` 型 —— 候选中国海油（Level A，`industry_family=energy`）<br>④`data_gap` / `DATA_NOT_COMPARABLE` / evidence-gap 型 —— 候选标准股份（R2 实测 `DATA_NOT_COMPARABLE`）<br>⑤若 `additional_procedure_required` 有稳定案例，作为第五类优先保留<br>每案记录：`case_id`、最新 fresh `run_id`、route、screening status、AI recommendation、analysis conclusion、run completeness、是否含 blocked 行、为什么适合展示 |
| 硬约束 | **不得依据旧 manifest 猜当前结果**。6 个含 blocked 行的案例（立讯精密/中国石化/万华化学/长江电力/中国石油/紫金矿业）当前均落 `DATA_GAP`，只能作为第 ④ 类或 W16 文案演示，**不得作为主风险案例** |
| 双案例主轴 | 五粮液（P2 + retain + strong + 55.62pp）对照 标准股份（G + defer + R1 未触发 -13.20pp + R2 不可比）；**最终以最新运行重新确认** |
| 标记 | **必做** · 口径=否 · 签字=**是**（案例选定属人工决定，`AGENTS.md §3/§5`）· **需队长授权付费调用** |

### W23 · 冻结 Golden Demo Run 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 建立录制前的可复核验收基线 |
| 精确修改方案 | 全部修复完成后重跑五粮液 fresh external live，从通过全部门禁的运行中选定一条为 **Golden Demo Run**，冻结记录：`run_id`、commit SHA、model ID、prompt version、三 Agent 状态、provider call 哈希、Numeric Gate 状态、evidence 页码、关键 Counter explanations、Review recommendation、AP 程序、timestamp |
| 硬约束 | **它不是 cache replay 的借口**，而是录制前的验收基线。录制时必须真实重跑，Golden Run 仅用于比对结果是否一致 |
| 标记 | **必做** · 签字=**是** · **需队长授权付费调用** |

### W24 · 浏览器验收：4 视口 × 4 运行状态 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | `AGENTS.md §7` 硬性要求；且前两份方案只覆盖视口、未覆盖状态 |
| 精确修改方案 | **视口**：1440×1000、1024×768、768×1024、390×844<br>**状态**（每个视口都要过）：①`success`（完整 live）②`incomplete_numeric_claims` / degraded（用 `RUN-V7-43AD8C03C3A7` 驱动）③`cache_replay` ④`deterministic_backup` / model failure<br>**逐状态检查**：文案与真实技术状态一致、控制台零错误、无 failed request、evidence drawer 可打开、Agent drawer 数据一致、移动端不溢出、焦点与键盘可达、触控目标尺寸、`prefers-reduced-motion`<br>**截图必须由实际运行页面生成并人工查看**，不得用静态 HTML 双击或设计稿替代 |
| 硬约束 | **逐状态实测，单态绿扫/汇总数不算整体通过** |
| 标记 | **必做** · 全部标记=否 |

### W25 · 录制脚本终稿 【必做】

| 项 | 内容 |
| --- | --- |
| 修改目的 | 确保口播与屏幕逐字一致，不复现 GPT/Gemini 的事实错误 |
| 精确修改方案 | 采用评审文件 §六 的五幕结构，并按本方案更正三处：<br>①五粮液处置说 **`retain` 建议保留**，**绝不说 defer**（实测 `review.status = retain × 35`，defer 出现 0 次）<br>②角色名说 **质疑 Agent / 反证 Agent / 复核 Agent**，**绝不说 Arbiter**（系统无此角色）<br>③`defer` 只在讲**标准股份**时使用（实测三次 live 均 `ai_recommendation=defer`）<br>**必须真实运行，不用回放**；收尾逐字念 AI 声明 |
| 标记 | **必做** · 签字=**是**（对外表述由人决定） |

---

## 第 8 章 · 三轨任务分配表

| 轨 | 独占文件 | 工作项 | 预估 |
| --- | --- | --- | --- |
| **阶段 0（串行）** | `docs/CONTRACT_2026-09-19.md`、`backend/tests/fixtures/run_contract_mock.json` | W00 | 40min |
| **轨 A · 后端控制链** | `numeric_gate.py`、`main.py`、`schemas.py`、`delivery.py`、`backend/tests/test_numeric_gate_*.py` | W01 W02 W03 W04 W05<br>+ 代轨 B 实现 W06/W07 在 `main.py` 的挂载<br>+ 代轨 C 实现 W16 若需后端透出 | 5-6h |
| **轨 B · 判定模块** | `planning_priority.py`（新）、`evidence_closure.py`（新）、`backend/tests/test_planning_priority.py`、`test_evidence_closure.py` | W06 W07 W08 | 3h |
| **轨 C · 前端与核验** | `demo-app.js`、`styles.css`、`index.html`、`scripts/verify_demo_outcome_judge.mjs`、`outputs/`、`docs/audit_*.md` | W09 W10 W11 W12 W13 W14 W15 W16(前端部分) W17 W18 | 6-7h |
| **阶段 2（串行）** | 合并点 | W19 W20 | 1.5h |
| **阶段 3（可并行）** | 运行与验收 | W21 W22 W23 W24 W25 | 4h |

**并行可行性说明**
- 轨 B 只写两个新文件 + 两个新测试文件，与 A/C **零文件交集**，契约冻结后可立即独立开工
- 轨 C 的 W09/W10/W12/W14/W15/W16/W17/W18 **不依赖** A/B 完工（W17/W18 是纯只读，W09/W10 修的是既有缺陷）；仅 W13 的 ⑥ 区徽标需要 mock fixture，W00 已提供
- 轨 A 的 W01/W02 不依赖 B/C；W03/W04 需要 W00 契约，不需要 B 的实现完成（按接口编码）
- **唯一串行瓶颈是 W00 与 W19**，合计约 2 小时

**给每个 AI 的开工指令模板**
> 你是轨 X。只允许修改本轨独占文件清单内的文件，越界即停并报告。先读 `docs/CONTRACT_2026-09-19.md` 与《2026-09-19_综合执行方案_多AI并行.md》第 X 章，再读 `AGENTS.md`。**不得直接照文档里的旧行号执行，必须先读当前 HEAD 重新确认代码路径。** 每个工作项完成后按"修改文件 / 核验依据 / 未确认事项 / 下一步"四段报告。不提交、不推送、不部署。

---

## 第 9 章 · 明确不做（红线）

- ❌ 新增 R3/R4/R5（`RuleId` 是 `Literal["R1","R2"]`，牵连 schema/路由/前端/测试全链）
- ❌ 修改 15pp / 30pp 已冻结阈值
- ❌ 引入 0.08 等任何未签字新阈值（Opus 原案中的 `A2 = 增速差>0.08` **不得实施**）
- ❌ 0-100 风险分、雷达图、大规模视觉重构
- ❌ 放松 evidence_id 绑定、放松 Numeric Gate、放松 Agent schema
- ❌ 把 `incomplete_*` 或任何降级状态改名为 `success`
- ❌ 用 cache replay 冒充现场真实调用（**Gemini 该条建议已否决**）
- ❌ 实施原 F3 的 `DATA_BLOCKED_PENDING_HUMAN` 新状态（**已被 W17 前置验证否决，除非 W17 复现**）
- ❌ 改写已封存的 `competition_demo_cases.json` 或历史 run JSON
- ❌ 让模型直接产出 P 级或 E 级
- ❌ 数据缺口参与优先级**升级**（Opus `C2 = C1 + 严重数据缺口`、Gemini 四级并列"资料受限"，**两处均否决**）
- ❌ 出现"无风险""企业风险评级""证券评级代码"等表述
- ❌ 使用 GPT 稿中"Reviewer 给五粮液 defer"、Gemini 稿中"Arbiter"及其虚构 Agent 台词

---

## 第 10 章 · 需队长签字/决定的清单

| # | 事项 | 涉及工作项 | 性质 |
| --- | --- | --- | --- |
| D1 | W00.3 六档「审计关注优先级」口径与判定顺序（G→P1→P2→P3→S→P4） | W00 W06 | 专业口径 |
| D2 | W00.4 三档「证据闭合状态」口径 | W00 W07 | 专业口径 |
| D3 | 确认 grade 与 disposition 为**两条独立轴**、并排显示、互不覆盖 | W00 W13 | 专业口径 |
| D4 | W01 数字闸门可追溯来源范围扩至"claim 已绑定 evidence_id 的年报原文片段数字" | W01 | **专业控制项** |
| D5 | W10 `additional_procedure_required` 作为独立结论显示，不再改写为 `data_gap` | W10 | 专业口径 |
| D6 | W11 结论二选一：(a) 设计上不由 AI 宣称无风险，前端仍表达"维持常规审计程序"；(b) 应当可达，批准最小修复 | W11 | 专业口径 |
| D7 | `schemas.py` 变更导致 R1 signoff 继续 stale，是否接受并在赛后重新签字 | W04 | 发布门禁 |
| D8 | W22 主演示案例选定（案例选定属人工决定） | W22 | 人工决定 |
| D9 | W21/W22/W23 付费模型调用授权次数（建议：3 + ≤6 + 1 ≈ 10 次） | W21-W23 | 预算 |
| D10 | W16 是否为 6 个 `DATA_GAP` 案例透传"抽取字段待人工回页确认"的具体诊断原文 | W16 | 呈现口径 |

---

## 第 11 章 · 全局回滚与完成标准

**回滚基线**：开工前记录 `git rev-parse HEAD`（当前 `e17e007`）与 `git status --short`；每轨开工前各自保存补丁。**不得 reset / clean / 覆盖已有未提交改动**（当前工作区有 2 个未跟踪文件需保留）。

**全局回滚触发条件**（任一即回滚到合并前）：
1. 任一历史 run JSON 读取失败
2. 出现"凭空数字被 traced"或"真实失败被显示为成功"任一实例
3. `backend/tests/` 全量出现无法当场解释的失败
4. 中文注释比例跌破 10%
5. 前后端字段语义分歧无法在集成期对齐

**全局完成标准**：
- [ ] D1-D10 全部有队长书面决定
- [ ] W01-W25 中标记「必做」的项全部完成并有证据
- [ ] 五粮液连续 3 次 fresh external_live 全部 `complete_public_prescreen` + `numeric_claim_trace.passed=true` + `retain`
- [ ] 紫金矿业/长江电力/立讯精密 在当前 HEAD 无 1e7 量级 `growth_gap`（W17 书面结论）
- [ ] 页面上不存在任何一句无法由 run JSON 证实的陈述（尤其 D2 的假话已消除）
- [ ] `additional_procedure_required` 作为独立结论正确显示
- [ ] 首屏综合审计关注卡六区齐全，`unverified_hypothesis` 明确标为「待验证解释」
- [ ] 任一 AI insight 均可一键回到 Evidence ID + PDF 年度 + 页码 + 原文
- [ ] live / cache_replay / deterministic_backup 三态在页面与导出中永不混淆
- [ ] 4 视口 × 4 运行状态逐一实测通过，截图经人工查看
- [ ] 分层测试全部通过，命令与退出码有记录，未引用历史通过数
- [ ] Demo Case Matrix ≥4 类，全部基于当前 HEAD fresh run
- [ ] Golden Demo Run 已冻结并记录全部字段
- [ ] 录制脚本口播与屏幕逐字一致，无 GPT/Gemini 两处事实错误
- [ ] 未提交、未推送、未部署（`AGENTS.md §6`）
- [ ] 未修改人工评分、签字门禁与正式发布状态；本轮工程改造**不等于**竞赛验收通过
