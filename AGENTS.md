# 审迹智链竞赛工作规则

本文件对当前目录及子目录持续生效。这里才是“审迹智链（AuditTrace）”竞赛工作区。

## 1. 先认清项目

- 竞赛：北京市大学生数智会计创新应用竞赛，主题为财会专业领域的 Multi-Agent 应用。
- 当前主文件：`02_最终确定方案/05_审迹智链_项目方案书_V3_全面复盘修订版.md`（同步 Word：同目录 `.docx`）。V2.4.4 保留为内部工程执行底稿和历史版本，不再作为对外方案主稿。
- 当前主展示网页：根目录 `index.html`（绝对路径：`C:\Users\张宏博\Desktop\大二下\大二(1)\plan\outputs\数智会计竞赛\index.html`）。凡是网页内容、视觉、交互、演示或工作台修改，默认只修改此文件及其根目录 `assets/` 资源。
- `03_第一周任务与成果/audittrace-local-static/` 是早期本地原型与图标来源，仅供参考和核对；除非用户明确点名，不把它当作当前主网页，也不在其中继续实现同一套页面。
- 目录归档：早期方案见 `01_早期方案与计划书/`；最终口径见 `02_最终确定方案/`；第一周执行与原型见 `03_第一周任务与成果/`。说明见 `README_目录说明.md`。
- 项目定位：面向会计师事务所审计项目组，在业务承接、续约和审计计划阶段，用公开资料形成收入确认及销售与收款循环的待核查风险线索。
- 输出是预审风险备忘录、Top 5 待核查事项和待索取资料清单；不得认定舞弊、出具审计意见或给投资建议。
- 当前目录已有根目录展示网页、本地 FastAPI、标准股份 DEV_T0 开发资料和 R1/R2 当前工程版；但尚无专业定稿规则、冻结案例、T0/T1 数据包、正式 RAG 验收、B0-B3 实验或效果结论。
- `../../../project/` 是无关的旧 FINTEL 项目。MUST NOT 把其中代码、测试、Demo 或结论用于本竞赛，除非用户以后明确要求评估某个可复用片段。

事实冲突时按以下优先级处理：主办方最新正式规则/更正 > 用户、团队或指导老师的明确决定 > 当前 V3 主方案书 > V2.4.4 工程执行底稿 > 旧版计划和 AI 评估报告。计划书写“拟、计划、建议”的内容不能当成已经实现。

## 2. 每次开始任务

1. 读取本文件和当前 V3 主方案书；实施、状态或历史口径有关时再读 V2.4.4 工程执行底稿；与老师意见有关时再读 `05_审迹智链_老师意见深度解析与对策.md`。
2. 说明本次任务是计划书、资料调研、专业规则、案例准备、未来原型还是竞赛材料。
3. 写清允许使用的文件、要交付的文件和验收方法；保留用户已有文件，不静默覆盖正式版本。
4. 只在本目录内工作。需要创建未来原型时，先征得用户确认目录结构，不自行转到 `project/`。
5. 遇到网页相关任务时，先确认根目录 `index.html` 是否存在并以它为交付文件；不得静默新建第二个展示网页或改写早期原型代替主网页。

## 3. 证据与表述

- 主办方规则、审计准则、监管文件、公司正式披露和论文都要能回到原件、URL/DOI、日期及页码/章节。
- Gemini、Codex、搜索摘要和既有 AI 评估报告只能作为候选线索，不能直接当正式来源。
- 找不到原件或无法核对时写“待核验/UNKNOWN”，不得编造页码、论文、数据、案例或老师意见。
- 清楚区分“当前已有、老师已确认、团队拟做、尚未验证”。两个历史案例不能支持普适的“舞弊识别准确率”。
- 关键专业判断、老师确认、案例选定、T1 解锁和最终发布必须由人决定；AI 不得代签、代批或伪造人工复核。

## 4. Codex 与 Gemini 分工

- Gemini Scout：查候选资料、同义词、反例、正常解释和备选 idea；结果先标“未核验候选”。
- Gemini Critic：在新会话里对计划书或阶段成果做反方审查，只找问题，不替团队裁决。
- Codex：读取本地原件、核对文件、修改计划书、建立清单、以后实现与测试；必须回到来源验证 Gemini 输出。
- 人：决定方向、专业口径、老师确认、数据权限、案例和正式对外表述。
- Gemini 网页端看不到本机 `C:\...` 路径。只上传本次必要文件，不上传整个文件夹、密钥、个人信息、客户资料或未解锁 T1。

具体复制提示词见 `00_Gemini提示词与AI协作流程.md`。

## 5. 阶段边界

- 现在可以：修改计划书、整理老师确认事项、核验规则和文献、建立案例候选资料表、明确专业规则和最小原型范围。
- 现在不能：声称系统已实现、把文档生成脚本当产品原型、生成虚假实验结果、宣称 B3 优于 baseline、查看未来 T1 答案。
- 正式实验前再建立简化 Research Contract，冻结案例、T0、baseline、模型、提示词、指标和预算；当前计划书阶段不需要维护复杂实验脚本。
- 主办方要求关键算法和非平凡逻辑包含中文注释，最低占代码量 10%；在真正开始写竞赛代码后落实并核查。

## 6. 完成任务

- 报告实际修改文件、核验依据、未确认事项和下一步。
- 新文档优先用清楚的中文文件名；不为同一内容制造多个重复版本。
- 未经用户明确要求，不提交、推送、部署、发布或发送外部消息。
