# Gemini、Antigravity 与 Codex 协作流程

## 你有 Antigravity：建议把它作为本地文件工作的主入口

Antigravity 可以直接读取你打开的本地项目，不需要像 Gemini 网页端那样反复上传文件。第一次这样设置：

1. 在 Antigravity 新建 Project 或打开文件夹时，**只选择**：
   `C:\Users\张宏博\Desktop\大二下\大二(1)\plan\outputs\数智会计竞赛`
2. 不要把上级 `大二(1)` 或无关的 `project` 文件夹加入同一个 Antigravity Project。
3. 保持“非工作区文件访问”关闭；终端命令权限优先使用需要你确认的模式，不要对删除、移动、发布操作自动放行。
4. 重新打开该 Project 后，Antigravity 会发现根目录 `AGENTS.md` 和 `.agents/skills/run-audittrace-competition/SKILL.md`。可以在 Skills 面板或 `/skills` 中确认。
5. 每次新任务直接发送下面的开场提示词，不需要上传本目录文件。

```text
请使用 run-audittrace-competition skill。
先读取 AGENTS.md 和 05_审迹智链_项目方案书_V3_全面复盘修订版.md；涉及实现状态时再读取 V2.4.4 工程执行底稿。确认当前已有开发网页、FastAPI 与 DEV_T0 样例，但尚无冻结案例、正式 RAG 验收或实验结果。

当前任务：<只写一个任务>
允许使用：<文件名>
不要做：不要访问工作区外文件；不要使用无关 project；不要把计划写成已完成；不要执行删除、发布或部署。
期望产物：<文件或表格>
验收标准：<怎么判断完成>
```

Antigravity适合直接完成：读取多份本地计划书、修改 Markdown/Python 文档生成脚本、整理资料、以后编写原型和运行测试。Gemini 网页端则更适合另开独立会话做资料 Scout 或反方 Critic，形成与本地执行 Agent 相对独立的第二视角。

## 使用 Gemini 网页端时先记住一件事

Gemini 网页端看不到你电脑里的 `C:\Users\...` 路径。使用方法是：

1. 新建一个 Gemini 会话；
2. 上传本次需要的 1—3 个文件，或粘贴相关内容；
3. 再发送下面对应的提示词；
4. 把 Gemini 的回答原样交给 Codex，并注明“这是未核验候选”；
5. 由 Codex 回到本地原件逐条核验后再修改正式文件。

不要一次上传整个竞赛文件夹。计划书评审通常只上传：

- `05_审迹智链_项目方案书_V3_全面复盘修订版.md` 或对应 DOCX；涉及实现状态时附 V2.4.4 工程执行底稿的相关节选；
- `附件1北京市大学生数智会计创新应用竞赛手册.docx`；
- 只有确实需要时，再上传老师意见或某份审计准则。

## 提示词 1：现在最适合用——计划书反方审查

把 V2 计划书和竞赛手册上传到一个全新会话，然后发送：

```text
你是“数智会计竞赛计划书反方审查员”，不是宣传文案作者。

项目：审迹智链（AuditTrace）。
当前事实：团队现在只有计划书和前置资料，尚无正式原型、冻结案例或实验结果。项目拟服务会计师事务所审计项目组，在业务承接、续约和审计计划阶段，基于公开资料形成收入确认及销售与收款循环的待核查风险线索；不认定舞弊，不出具审计意见，不给投资建议。

任务：结合我上传的竞赛手册，严格审查 V2 计划书是否存在：
1. 与竞赛硬规则冲突；
2. 用户、场景或责任边界不清；
3. 把计划写成已实现；
4. 十周内无法落地的功能；
5. 专业逻辑不够像真实审计工作；
6. baseline、T0/T1、消融或效果指标不公平；
7. 缺少正常解释、证据缺口或人工复核；
8. 答辩时容易被评委追问的薄弱点。

只输出问题表，不重写整篇。每条包含：严重程度、计划书章节/原句、问题、判断依据、需要补充的证据、最小修改建议。明确区分“手册原文”“你的推断”和“无法确认”。如果找不到原文页码就写 UNKNOWN，禁止虚构规则、页码、案例和数据。
```

## 提示词 2：资料和文献侦察

一项问题开一个新会话，例如查“销售与收款循环有哪些可程序化风险规则”：

```text
你是资料侦察员，不是结论作者。

研究问题：<填写一个具体问题>
项目边界：审计业务承接、续约和计划阶段；收入确认及销售与收款循环；只形成待核查线索。
优先来源：中国注册会计师审计准则及中注协文件、财政部/证监会/交易所正式文件、公司正式披露、同行评议论文。
排除来源：论坛、自媒体、无原始链接的二手总结、模型记忆；禁止直接推断某公司舞弊。

只返回候选表：candidate_id、标题、作者或机构、发布日期、官方URL/DOI、资料类型、相关章节/可能页码、与问题的关系、可能支持或反驳什么、是否实际打开原件、仍需核验什么。
找不到就写 UNKNOWN。不得虚构 DOI、URL、页码、准则条款或引文。
```

Gemini 返回后交给 Codex时这样说：

```text
下面是 Gemini 找到的候选资料，全部尚未核验。请只在“数智会计竞赛”文件夹工作，逐条打开原件核对机构、日期、URL、页码和真实内容；不可靠的删除，不要直接写入正式计划书。

<粘贴 Gemini 原始回答>
```

## 提示词 3：idea 发散

```text
你是科研 idea 发散助手。围绕审迹智链当前边界提出 10 个可验证的小改进，不要扩成综合审计平台。

每个 idea 必须给出：解决的具体问题、与当前 V2 的差异、所需输入、最小实现、可比较 baseline、成功信号、失败信号、正常解释、两周内能否做完、可能与已有方法重合之处。

优先提出能被证伪、能做单变量消融、能生成可核验证据链的 idea。不要用“创新性强”“准确率会提升”等空话；没有依据写 UNKNOWN。
```

Gemini 的 10 个 idea 只是候选。让 Codex核对是否重复、是否需要不可获得的数据、是否超出计划书边界，再由团队选。

## 提示词 4：成果反方审查

计划书、规则卡、案例表或以后实验结果完成后，另开一个新 Gemini 会话，不告诉它团队希望得到什么结论：

```text
你是独立反方审查员。不要帮我润色或证明项目成功，只列可能使结论不成立的问题。

检查：证据不支持的表述、时间或答案泄漏、baseline不公平、指标事后修改、正常解释遗漏、引用与数字矛盾、小样本外推、失败样例遗漏、伦理越界和“计划冒充完成”。

每条输出：severity、文件位置/原句、问题、需要的原始证据或反例、建议状态（保留/降级/暂缓/删除）。无法判断写 UNKNOWN。
```

## Codex 日常提示词

以后打开本文件夹后，可以直接对 Codex说：

```text
$run-audittrace-competition

当前任务：<只写一个任务>
允许使用：<文件名>
不要做：<例如不要写代码、不要修改主计划书、不要读取其他目录>
期望产物：<文件或表格>
验收标准：<怎么判断完成>
```

当前最合适的例子：

```text
$run-audittrace-competition

当前任务：把 V3 方案书第八章的关键人工决定整理成一页指导老师确认单。
允许使用：V3 主方案书和老师意见文件。
不要做：不要修改主计划书，不要假设老师已经批准，不要写原型代码。
期望产物：新建一份简洁的老师确认单 Markdown。
验收标准：五项问题均可勾选，并包含“老师意见/需修改内容/确认日期”。
```

## 最简分工

| 角色 | 负责什么 | 不能直接做什么 |
| --- | --- | --- |
| Gemini Scout | 找候选资料、反例和 idea | 不能把候选变成正式证据 |
| Gemini Critic | 找漏洞、过度表述和评委攻击点 | 不能替团队裁决 |
| Codex | 核对本地文件、修改文档、以后实现和测试 | 不能代替老师批准 |
| 团队/老师 | 定方向、专业判断、案例和最终表述 | 不能把 AI 输出不核验就提交 |

不需要 Claude。Codex + Gemini + 人工确认已经足够，关键是让不同会话承担不同角色，并把核验结果写回文件，而不是依赖聊天记忆。

## Antigravity 官方设置依据

- Project 只加入本竞赛文件夹，并使用项目级权限隔离：https://antigravity.google/docs/projects
- 工作区 Skill 默认放在 `.agents/skills/<skill>/SKILL.md`：https://antigravity.google/docs/skills?app=antigravity-ide
- 工作区规则和 Workflow 的用法：https://antigravity.google/docs/rules-workflows
- 非工作区文件访问默认应关闭：https://antigravity.google/docs/agent-settings
