# 🚨 下午录视频前的突击改进方案

## 情况诊断

你的系统**不是**没用的 Python 脚本。你已经有了：
- ✅ 真实 DeepSeek-v4-flash 调用的三 Agent（质疑/反证/复核）链
- ✅ 确定性 R1/R2 规则计算 + 阈值判断
- ✅ RAG 原文回溯 + 证据编号绑定
- ✅ 15 个冻结案例 + 45 份公开年报
- ✅ 结构化 JSON 输出 + 证据可追溯

**问题在于**：当前输出的结论层级太平——无论哪个公司都只有四类定性标签（`risk_candidate` / `no_trigger_confirmed` / `data_gap` / `industry_boundary`），没有量化的风险分级，让人感觉"不管输入什么公司结果都差不多"。

## 核心改进方向：增加风险信用评级体系

> [!IMPORTANT]
> **这是与"直接丢给 DeepSeek/豆包"的关键差异化**：我们用**确定性规则计算**的量化指标（增速差、周转天数趋势、持续期间等），加上 Agent 的结论交叉验证，生成一个**可解释、可追溯的六级风险评级**，而不是让大模型直接给一个不负责任的结论。

### 六级评级体系（类似信用评级）

| 级别 | 标签 | 含义 | 触发条件（确定性规则驱动） |
|------|------|------|---------------------------|
| **A1** | 🟢 常规关注 | 暂未发现异常信号 | R1 未触发 + R2 未触发 + 无数据缺口 |
| **A2** | 🟢 低度关注 | 轻微波动但在合理范围 | R1 未触发，但增速差接近阈值(>0.08) 或有少量数据缺口 |
| **B1** | 🟡 中度关注 | 存在值得跟进的信号 | R1 触发(standard) 或 R2 触发 + 持续期间 ≤1 |
| **B2** | 🟡 重点关注 | 多维信号叠加 | R1 触发(strong) 或 R1+R2 均触发 |
| **C1** | 🔴 高度关注 | 存在突出风险信号 | R1 触发(strong) + 持续期间 ≥2 + 周转天数恶化 |
| **C2** | 🔴 特别关注 | 多维异常叠加 + 数据缺口 | C1 条件 + 有严重数据缺口/重要性超标 |

> [!CAUTION]
> 分级声明必须保持边界：**"本评级为审计计划阶段的程序筛查信号分级，不是审计认定，不构成审计结论或审计意见。"**

---

## 具体改动

### 后端：添加风险评级计算

#### [NEW] `risk_rating.py`（新增模块）
- 纯确定性计算，零模型调用
- 读取 R1/R2 的 `metrics`、`screening_status`、Agent `analysis_conclusion`
- 输出 `risk_grade`（A1-C2）、`risk_label`、`risk_score`（0-100分）、`grade_factors`（每个因素的贡献说明）
- 所有分级因素可追溯到具体指标数值，不是模型生成的

#### [MODIFY] `main.py`
- 在 `_build_run_response` 完成后，调用 `compute_risk_rating()` 把评级结果写入 `run_response` 的 `context` 中
- 评级字段：`risk_grade`, `risk_label`, `risk_score`, `risk_grade_factors`, `risk_grade_boundary`

---

### 前端：展示风险评级

#### [MODIFY] `index.html`
- 在"本次审计计划总览"区域，增加**风险信号评级面板**
  - 大字显示等级标签（如 **B1 · 中度关注**）
  - 下方列出评级因子清单（每个因素为什么加分/扣分）
  - 右侧雷达图/条形图展示各维度得分

#### [MODIFY] `demo-app.js`
- 读取运行结果中的 `risk_grade` 等字段
- 渲染评级面板和因子列表
- 不同等级使用不同颜色调（绿/黄/红）

#### [MODIFY] `styles.css`
- 新增评级面板样式、等级徽章、因子列表

---

## 演示案例建议

> [!IMPORTANT]
> **推荐演示标准股份（STD_DEV_T0）**：这是唯一通过全部 10 项门槛的 Level A 案例，三次新鲜模型运行全部成功，且 R1 会产生实际的指标计算结果（增速差 -13.20%，虽然 R1 未触发 strong，但有数据缺口和净额口径问题，Agent 会给出 `defer` 建议），最终评级预计在 **A2/B1** 之间，能展示系统如何把"看起来没问题"的公司也标出待关注的数据缺口。

五粮液（000858）也是 Level A，如果标准股份跑完后时间允许可以作为对比。

---

## Open Questions

1. **你现在后端能正常启动吗？** 你说"运行时总有问题"——是模型调用超时，还是后端根本起不来？如果 DeepSeek API 不稳定，可以用确定性备用链（`force_deterministic_backup`），评级模块完全不依赖模型调用，一样能展示出分级效果。

2. **风险评级的六级名称你觉得 OK 吗？** 还是你更倾向"无风险/低风险/中风险/高风险"这种直觉名称？六级体系更专业，但四级更简洁。

3. **今天下午几点录视频？** 我需要评估时间窗口来决定改动范围——如果只有 1-2 小时，我会先只改前端展示（用 JS 从现有 metrics 计算评级）而不动后端 Python，这样最快最安全。

---

## 为什么这个改进能让评委觉得有用

| 评委可能的质疑 | 有了风险评级后的回应 |
|---------------|-------------------|
| "这不就是把年报丢给大模型吗？" | 评级 A1-C2 是**确定性规则计算**出来的，不是模型编的——每个因子对应具体的增速差数值、周转天数和持续期间 |
| "换家公司结果有区别吗？" | 不同公司的评级**必然不同**——标准股份可能 A2，五粮液可能 B1，因为指标和数据完整度不一样 |
| "这跟我自己算有什么区别？" | 系统自动完成 8 类指标计算 + RAG 原文回查 + 三 Agent 交叉验证 + 结构化评级，人工做同样的事需要半天 |
| "没有个性化建议" | 每个评级等级对应不同的审计程序建议组合，A1 只需常规程序，C2 需要全面扩大实质性测试范围 |

## 验证计划

1. 后端单元测试：验证 `compute_risk_rating()` 对不同 metrics 组合返回正确等级
2. 前端手动验收：1440×1000 + 390×844 视口，确认评级面板正确渲染
3. 真实运行验证：对 STD_DEV_T0 启动一次运行，确认评级结果写入 JSON 并正确显示
